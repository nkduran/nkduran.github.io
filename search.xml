<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>软件设计的哲学（二）</title>
    <url>/2020/02/03/a-philosophy-of-software-design-c1/</url>
    <content><![CDATA[<blockquote>
<p>翻译自<a href="https://book.douban.com/subject/30218046/" target="_blank" rel="noopener">《A Philosophy of Software Design》</a>，作者<a href="https://en.wikipedia.org/wiki/John_Ousterhout" target="_blank" rel="noopener">John Ousterhout</a>，Tcl脚本语言和Tk工具库的发明人。</p>
</blockquote>
<h2 id="第一章-简介"><a href="#第一章-简介" class="headerlink" title="第一章 简介"></a><center>第一章 简介</center></h2><p>&emsp;&emsp;编写计算机软件是人类历史上最纯粹的创造性活动之一。程序员不会被物理定律一类现实的约束所限，而能够用现实世界中不存在的行为来创造令人兴奋的虚拟世界。编程不像跳芭蕾舞或者打篮球那样需要突出的运动能力和身体协调能力，唯一需要的就是创新的思想，以及对思维的组织能力。如果你能想象出一个系统，就有可能通过一个计算机程序来实现它。</p>
<p>&emsp;&emsp;这意味着，在编写软件方面，最大的限制就是我们对所创建系统的理解能力。程序在逐步发展中，获得的特性越来越多，所包含的组件间会出现很多微妙的依赖关系，本身变得越来越复杂。随着时间的推移，复杂性逐步积累，而对于程序员来讲，在修改系统的时候，把所有相关因素都考虑到变得越来越困难。这些因素拖慢了开发速度，引入了很多问题，而这些问题又反过来进一步地让开发变得更慢，同时增大了成本。复杂性的增长在所有程序的生命周期里都是无可避免的。程序越大，参与其中的人越多，对复杂性的管理就越困难。</p>
<p>&emsp;&emsp;好的开发工具能帮助我们处理复杂性的问题，最近几十年里很多优秀的工具被创造出来，但是我们单独利用工具所做的毕竟有限。如果我们想让编写软件变得更简单，从而能够用更低的成本创建出更强大的系统，我们就必须找出能让软件更简单的方法。虽然在倾尽努力的情况下，复杂性仍然会随时间增加，但是更简单的设计可以让我们在复杂性失控之前，就创建出更大规模和更强大的系统来。</p>
<p>&emsp;&emsp;和复杂性对抗有两类常用的方法，本书会针对这两类方法进行讨论。第一类方法是通过让代码更简单和更清晰来消除复杂性。比如通过消除特殊情况或者使用统一风格的标识符来降低复杂性。</p>
<p>&emsp;&emsp;第二类方法是将复杂性进行封装，从而让程序员在系统上工作的时候，该系统的复杂性不会一下子暴露出来。这类方法常被称为<strong><em>模块化设计</em></strong>。在模块化设计中，一个软件系统会被分解为若干模块，就像面向对象语言中的“类”。这些模块设计成彼此相对独立，以便让程序员在不了解其他模块细节的前提下，能够很好的处理当前模块。</p>
<p>&emsp;&emsp;因为软件的延展性，软件设计是一个跨越软件系统整个生命周期的持续的过程。这会让软件设计与其他物理系统——像建筑、轮船、桥梁等——的设计有所不同。然而业界对软件设计的态度并不是向来如此的。在编程历史的很长一段时间内，像其他工程学科一样，设计更集中在项目的开始。这种方式的极端情况被称为<strong><em>瀑布模型</em></strong>。根据这个模型，一个项目会被分割成独立的几个阶段，如需求定义、设计、编码、测试和维护等。在瀑布模型中，每个阶段完成后，下个阶段才开始。很多情况下，不同人负责不同阶段。在设计阶段，会针对整个系统进行设计。而在该阶段的末尾，整个设计会被冻结。随后的阶段仅是对该设计充实细节和完成实现。</p>
<p>&emsp;&emsp;不幸地是，对软件而言，瀑布模型很少能运转良好。软件系统本质上要比物理系统更复杂。想要在构建所有东西之前，对大型软件系统的设计做足够好的分析，从而理解它的所有含义是不可能的。随之而来的结果是初始设计会包含很多问题，并且这些问题在落地实现顺利进行之前并不会显露出来。而当发现问题时，瀑布模型并不能很好的支持对主要设计的修改（比如说，设计者可能已经转到另外的项目）。因此，开发者只能试着在不改变整个设计的前提下针对问题打补丁，从而导致复杂性的爆发。</p>
<p>&emsp;&emsp;由于这些原因，在今天大多数的软件开发项目都会使用一些增量方法，比如<strong><em>敏捷开发</em></strong>。在敏捷开发中，初始设计聚焦在整个功能集合的一个小的子集。这个功能子集会先被设计，实现和评估。等初始设计中的问题被发现和修正后，更多的新特性会再引入到这个迭代过程——被设计，实现和评估。每次迭代针对现有设计都会暴露一些问题，而这些问题会在下个阶段的新特性设计之前被解决掉。以这种方式展开设计，可以在系统仍然很小的时候就解决掉初始设计中的问题。而后期特性的开发则会受益于早期特性实现过程中获得的经验，问题会更少。</p>
<p>&emsp;&emsp;这种增量方法适用于软件是因为软件具有足够的延展性，从而允许在实现过程中进行重大的设计变更。与此相反，对于物理系统而言，主要设计如果变更的话，挑战就会大很多。比如在施工中期要更改支持一座桥梁的桥塔数量就不切实际。</p>
<p>&emsp;&emsp;增量开发意味着软件设计永远不会完成。设计会贯穿系统的整个生命周期：开发者应该要一直思考关于设计的问题。另外增量开发还意味着不断的重新设计。一个系统或者组件的初始设计永远不会是最好的那个，而过程经验必然能带来更好的做事方法。做为一个软件开发者，应该利用一切机会去改进所开发系统的设计，并计划出一部分时间来做改进设计的工作。</p>
<p>&emsp;&emsp;如果软件开发者需要一直关注设计的问题，而减少复杂性又是软件设计里最重要的一部分，那软件开发者就必须要一直关注软件的复杂性。这本书正是讲有关如何利用复杂性去引导软件整个生命周期内的设计的。</p>
<p>&emsp;&emsp;这本书有两个总体目标。第一个目标是描述软件复杂性的本质：“复杂性”意味着什么？为什么它很重要？如何识别一个程序何时具有必要的复杂性？本书的第二个目标更具有挑战性，介绍一些可以用于在软件开发过程中降低复杂性的技术。不幸地是，没有一个简单的秘诀可以得到优秀的软件设计。相反地，我会介绍一组接近哲学的高级概念，类似于“class应该是深的”，或者“定义不存在的错误”。这些概念可能不会马上让你得到最佳设计，但是利用它们你能够对设计方案进行评估比较，并指导你对设计世界进行探索。</p>
<h3 id="1-1-如何使用这本书"><a href="#1-1-如何使用这本书" class="headerlink" title="1.1 如何使用这本书"></a>1.1 如何使用这本书</h3><p>&emsp;&emsp;本书中描述的许多设计原则都稍微有些抽象，如果不参考具体代码理解起来会有些难度。但想要找到合适的范例代码——小到足够包含在本书中，大到足够阐述真实系统的问题——实在是个挑战（如果你遇到了好的范例代码，请发给我）。因为这个原因，本书可能无法提供足够的范例，让读者领会如何应用这些原则。</p>
<p>&emsp;&emsp;使用本书最好的方法是在代码评审的时候阅读。在你阅读其他人的代码时，可以思考一下这些代码是否符合书中讨论的概念，而这些概念又如何与代码复杂性关联的。通过其他人的而不是自己的代码来研究设计问题会更容易些。你可以用小红旗标出问题并提出改进意见。另外代码评审还会让你了解新的设计方法和编程技术。</p>
<p>&emsp;&emsp;改进设计技能最好的方法之一就是学会识别这些<strong><em>小红旗</em></strong>：那些比实际需要实现地更复杂的代码片段。通观本书我会指出和每个主要设计问题相关的问题，最重要的问题会在书的末尾做个总结。当你在编码的时候有可能会用到这些：当你看到一个小红旗，停下来去找到一种解决问题的替代设计。当你第一次尝试这个方法时，可能要尝试好几个替代方案才能解决，不要轻易地放弃：你在解决问题前尝试地越多，你学到的就会越多。随着时间的积累，你会发现你代码中的小红旗会越来越少，你的设计也会越来越干净。而你积累的经验也会让你发现其他小红旗，以识别出其他设计问题。（我非常乐意听到这些）</p>
<p>&emsp;&emsp;在应用本书的一些方法时，谨慎和适度使用是非常重要的。每一个规则都有针对它的例外情况，每一个原则也有它的局限。如果你把任何一个设计理念推向极端，你可能就会陷入一个糟糕的境地。漂亮的设计呈现地是相互竞争的思想与方法间的一种平衡。书中有几个章节的名字叫“太过分了”，内容描述的就是如何识别什么情况是把一个好东西做得过了火。</p>
<p>&emsp;&emsp;本书中几乎所有的例子都是使用的Java或者C++，而大多数的讨论也是有关面向对象语言class的设计。然而，这些思路在其他领域也是适用的。几乎所有这些方法思路都可以应用在没有面向对象特性语言的函数里，比如C语言。这些设计思路也可以应用到其他非class的模块中，比如子系统或者网络服务。</p>
<p>&emsp;&emsp;在这个背景下，让我们详细讨论一下是什么导致了复杂性，又如何让软件系统变得更简单。</p>
]]></content>
      <tags>
        <tag>软件设计</tag>
      </tags>
  </entry>
  <entry>
    <title>瑞宝趣事（三）</title>
    <url>/2020/02/02/ruibao3/</url>
    <content><![CDATA[<h3 id="巴巴爸爸的颜色世界"><a href="#巴巴爸爸的颜色世界" class="headerlink" title="巴巴爸爸的颜色世界"></a>巴巴爸爸的颜色世界</h3><p>2020.2.2</p>
<p>瑞宝喜欢看巴巴爸爸的故事，包括绘本、动画片喜欢翻来覆去的看。后来我发现瑞宝认颜色的时候，常常说对应巴巴爸爸中的人物，比如指着黄色说巴巴祖，黑色是巴巴伯，名字说的非常遛，反倒是具体的颜色名字记得颠来倒去。直到有一天我指着白色问他，<br>– 这是什么颜色？<br>– 这是巴巴~ 呃 ……</p>
<br>

<h3 id="小白杨"><a href="#小白杨" class="headerlink" title="小白杨"></a>小白杨</h3><p>2020.2.2</p>
<p>在瑞宝一两岁的时候，我哄他睡觉经常唱《小白杨》。最近一段时间，瑞宝想起这首歌，总要让我给他唱，还要手机单曲循环。结果有时循环大半天，姥姥都听得忍无可忍，偷着把音箱给关了。10后喜欢上听70后的军旅歌曲，算不算是给经典老歌的传承做贡献了？！^_^</p>
<h5 id="一段小插曲"><a href="#一段小插曲" class="headerlink" title="一段小插曲"></a>一段小插曲</h5><p>瑞宝学会《小白杨》，经常唱给我们听，因为孩子小，音调和吐字还不够清楚，不过大多数情况都能听明白，只是有时候唱着唱着就叽里咕噜的不知道在唱啥。随着瑞宝吐字越来越清晰，某一天终于听明白了：</p>
<p>– 一棵小白杨~ 长在哨所旁~<br>– 根儿深~ 杆儿壮~ 守卫在北疆~<br>– 北风吹~<br>– 雁雪纷纷<br>– 莫愁前路无知己<br>– 天下谁人不识君<br>– 一棵小白杨~</p>
]]></content>
      <tags>
        <tag>童趣</tag>
      </tags>
  </entry>
  <entry>
    <title>软件设计的哲学（一）</title>
    <url>/2020/01/31/a-philosophy-of-software-design-preface/</url>
    <content><![CDATA[<blockquote>
<p>翻译自<a href="https://book.douban.com/subject/30218046/" target="_blank" rel="noopener">《A Philosophy of Software Design》</a>，作者<a href="https://en.wikipedia.org/wiki/John_Ousterhout" target="_blank" rel="noopener">John Ousterhout</a>，Tcl脚本语言和Tk工具库的发明人。</p>
</blockquote>
<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a><center>前言</center></h2><p>&emsp;&emsp;人们为电子计算机写程序已经有80多年的历史，但令人惊讶的是关于“如何设计程序”，或者“好的程序应该是什么样子”的讨论很少。有很多重要的讨论都是和诸如敏捷开发等软件开发流程，或者调试器、版本管理系统、测试覆盖工具这样的开发工具相关的。还有一些复杂分析是关于面向对象编程、函数式编程等编程技术，或者设计模式及算法的。所有这些讨论都是有价值的，但涉及到软件设计的核心问题始终没有被真正触碰到。David Parnas的经典论文《用于将系统分解成模块的准则》（On the Criteria to be used in Decomposing Systems into Modules）发布于1971年，而在随后的45年里，软件设计的技术水平相比这篇论文并没有多大进步。</p>
<p>&emsp;&emsp;计算机科学里最基础的问题就是<strong><em>问题分解</em></strong>：如何将一个复杂的问题分割成一些能够独立解决的小问题。问题分解是程序员每天需要面对的核心设计任务，而不是描述的所谓那些工作。我还没有在任何一所大学的单独课程里看到把问题分解作为核心论题的。课堂上我们只是在教循环或者面向对象编程，而不是软件设计。</p>
<p>&emsp;&emsp;另一方面，不同程序员间的生产力和产出质量存在着巨大差异。在研究是什么让最好的程序员如此优秀，或者在课程上讲授相关技术方面，我们并没有做出任何尝试。我和一些我认为是伟大的程序员的人聊过，他们中的大多数人都很难清晰地说出是哪些具体的技术给他们带来了优势。许多人都认为软件设计技术是与生俱来的天赋，是无法教授的。然而，有许多科学证据表明，很多领域中杰出的表现都和高质量的练习相关，而不是先天的能力。（有关内容可以参考Geoff Colvin的 <strong><em>《Talent is Overrated》</em></strong> 一书）</p>
<p>&emsp;&emsp;很多年来，这些问题都让我感到困惑和沮丧。我想知道软件设计能否被教授，我猜测设计能力是区分优秀程序员和平庸程序员的关键，最终我决定试着教授一门有关软件设计的课程，我认为这是获取这些问题答案的唯一方法。这就是斯坦福大学CS190课程的来历。在这门课上，我提出了一系列有关软件设计的原则，学生则通过完成一系列实际的项目来练习并掌握这些原则。这门课是以类似于传统英文写作课的方式进行讲授的。在英文课上，学生们会使用一种”拟稿-获取反馈-重写“这样的迭代过程来完成效果改进的。而在CS190中，学生们是从零开始开发一款复杂的软件，随后我们采用大量的代码审查以确定其中的设计问题，学生则修改他们的项目代码来解决这些问题。这个过程会让学生看到如何通过应用设计原则来改进他们的代码。</p>
<p>&emsp;&emsp;至今为止软件设计课程我教过三遍，而本书就是基于课程里所提炼出的设计原则写成的。这些原则是从相当高的层面来思考的，接近于编程哲学（比如“定义不存在的错误”）。对学生来讲这些抽象的想法很难理解，他们更多地是通过编写代码，犯错误，然后根据他们所犯的错误和随后的修复来分析如何与设计原则相关，而获得最佳的学习效果。</p>
<p>&emsp;&emsp;这时候你可能很想知道：是什么让我自以为知道所有有关软件设计的答案？坦白的说，我也不知道。在我学习编程的时代还没有任何有关软件设计的课程，也没有任何一位导师教给过我有关设计的原则。而在那个时代，代码评审也基本不存在。我关于软件设计的所有想法都来自于我个人读写代码这样的实践经验。在我整个职业生涯中，我使用不同语言写了大概有250000行代码。我在很多团队工作过，这些团队曾经从头开始编写了三款操作系统，开发了多文件及存储系统、调试器这样的基础工具、编译系统、GUI工具包、一种脚本语言，用于文本、绘图和演示的交互式编辑器以及集成电路等等。这样一路走来，我亲身体验了开发大型系统所遇到的各类问题，并尝试了各种设计技术。此外，我读过其他人写的相当多的代码，让我接触到了各种各样的方法，有好的也有坏的。</p>
<p>&emsp;&emsp;通过这些经验，我试着找出了一些通用的线索，既包括了要避免的错误，也包括了要使用的技术。这本书就是有关我经验的总结：其中每个描述的问题我个人都曾经历过，每个推荐的技术也都是曾经在我自己的代码中成功应用过的。</p>
<p>&emsp;&emsp;我并不期望这本书能给出软件设计的最终答案。我确信我错过了一些有价值的技术，而从长远来看，我的一些建议可能会变成糟糕的主意。然而，我希望本书可以引发一场关于软件设计的讨论。我建议把书里的想法和你自己的经验做比较，你自己来判断这里描述的方法是否真的降低了软件复杂性。这本书里阐述了很多我个人的观点，可能有的读者会反对我的一些建议。如果你不同意它，就试着去理解它。我感兴趣的是那些对你有用的或者没用的东西，以及你对软件设计的其他任何想法。我希望后续的讨论会改进我们对软件设计的整体理解。我会把讨论中学到的新东西写进这本书的未来版本里。</p>
<p>&emsp;&emsp;和我沟通这本书的最好方式是发邮件到以下地址：<strong><em><a href="mailto:software-design-book@googlegroups.com">software-design-book@googlegroups.com</a></em></strong>。我很想听听针对这本书的具体反馈，像问题或者改进建议一类的，还有和软件设计相关的一般想法和经验。另外我也很想看到一些非常令人信服的例子，能够加入到本书未来的版本中。最好的例子就是用一到两段话给出足够简单的解释，来阐明一个重要的设计原则。如果你想要看看其他人在邮件里说了什么，并想参与到讨论中，可以加入Google Group <strong><em>software-design-book</em></strong>。</p>
<p>&emsp;&emsp;如果未来因为某些原因Google Group <strong><em>software-design-book</em></strong> 不存在了，可以在网络上搜索我的个人主页。在主页上你可以找到关于本书如何交流的最新的指导信息。另外请不要发送和本书相关的邮件到我私人邮箱。</p>
<p>&emsp;&emsp;我建议你用批判的态度来看待书里的内容。软件设计的最终目标是降低复杂性，这比你读过的任何特定的原则或者想法都重要。如果你尝试了书中的一些想法最后发现实际上并没有降低复杂性，就不要觉得有义务再继续使用它。（一定要让我知道你的这些经历，我希望得到反馈，了解哪些可行，哪些不可行）</p>
<p>&emsp;&emsp;许多人提出了批评和建议，这些都有助于改进本书的质量。以下各位对本书的征求意见稿提出了有益的意见：Jeff Dean，Sanjay Ghemawat，John Hartman，Brian Kernighan，James Koppel，Amy Ousterhout，Kay Ousterhout，Rob Pike，Partha Ranganathan，Keith Schwartz和Alex Snaps。Christos Kozyrakis建议用术语“deep”和“shallow”来修饰类和接口，代替以前意义有些模糊的“thick”和“thin”。另外很感谢CS190课程上的学生们，通过阅读他们的代码并和他们进行讨论，帮助我更加明确了我有关设计的一些想法。</p>
]]></content>
      <tags>
        <tag>软件设计</tag>
      </tags>
  </entry>
  <entry>
    <title>瑞宝趣事（二）</title>
    <url>/2020/01/30/ruibao2/</url>
    <content><![CDATA[<h3 id="睡午觉"><a href="#睡午觉" class="headerlink" title="睡午觉"></a>睡午觉</h3><h4 id="2020-1"><a href="#2020-1" class="headerlink" title="2020.1"></a>2020.1</h4><p>哄宝贝睡午觉。<br>– 每天都是妈妈陪你睡午觉，今天爸爸陪好吗？<br>– 让妈妈陪！<br>– 你看，昨天晚上爸爸陪你，睡得多香啊！<br>– 让妈妈陪！<br>商量了半天，死活不同意，最后突然喊了一句，<br>– 游戏的规则不能改，科科！<br>这动画片看多了，台词都能活学活用了！</p>
<br>

<h3 id="夹带私活"><a href="#夹带私活" class="headerlink" title="夹带私活"></a>夹带私活</h3><h4 id="2020-1-1"><a href="#2020-1-1" class="headerlink" title="2020.1"></a>2020.1</h4><p>– 来，给爸爸背首诗吧！<br>– 不要给爸爸背首诗！<br>– 给爸爸背首诗，爸爸给你吃酸奶！<br>– 好～吧～</p>
<p>– 锄禾日当午<br>– 汗滴禾下土<br>– 谁知盘中餐<br>– 奶奶跳牙线舞，咿呀咿呀</p>
<br>

<h3 id="口头禅"><a href="#口头禅" class="headerlink" title="口头禅"></a>口头禅</h3><h4 id="2020-1-2"><a href="#2020-1-2" class="headerlink" title="2020.1"></a>2020.1</h4><p>瑞宝最近多了几个口头禅，遇到拒绝或者要否认的时候，就说“不~是~这~个~意~思~的~”，简直万能语句。还有冷不丁抓个人就问，“妈妈，你相信吗？”，“姥姥，你相信吗？”，“爸爸，你相信吗？”，问他相信什么他也不说，我们只能配合的回答“相~信~”。至于相信什么早已经尽在不言中。</p>
]]></content>
      <tags>
        <tag>童趣</tag>
      </tags>
  </entry>
  <entry>
    <title>瑞宝趣事（一）</title>
    <url>/2020/01/30/ruibao1/</url>
    <content><![CDATA[<h3 id="布娃娃"><a href="#布娃娃" class="headerlink" title="布娃娃"></a>布娃娃</h3><h4 id="2018-1-3"><a href="#2018-1-3" class="headerlink" title="2018-1-3"></a>2018-1-3</h4><p>妈妈拿着小丑布娃娃和瑞宝玩，扮成伤心状，用布娃娃的双手捂住它的眼睛说，“瑞宝……你好久没和我玩儿了……呜呜呜呜”，结果瑞宝宝的眼睛湿润了，眼看是要哭的表情，逗了好几次都是这样，怕他真哭于是就停住不逗了。</p>
<br>

<h3 id="笑声"><a href="#笑声" class="headerlink" title="笑声"></a>笑声</h3><h4 id="2018-1-9"><a href="#2018-1-9" class="headerlink" title="2018-1-9"></a>2018-1-9</h4><p>瑞宝喜欢玩他的脚丫，喜欢用食指捅捅耳朵、鼻子，还有一些细小的东西，爱把书翻来翻去，拨弄自己的耳朵，挑挑眉毛逗大家笑，同时自己发出“卡、卡”的笑声，听完瑞宝笑终于明白了什么叫杠铃般的笑声。//捂脸</p>
<br>

<h3 id="打屁股"><a href="#打屁股" class="headerlink" title="打屁股"></a>打屁股</h3><h4 id="2019-10"><a href="#2019-10" class="headerlink" title="2019-10"></a>2019-10</h4><p>瑞宝最近晚上不再按时睡觉，每次给他洗澡还要跟你搏斗一番，真急了的时候想教训一下他，疾言厉色地让他趴下打屁屁，瑞宝乖乖的过来趴在我腿上，我也没舍得用大力，一下下拍，瑞宝很兴奋，大声地喊一二三四跟着记数，你说气不气人。</p>
]]></content>
      <tags>
        <tag>童趣</tag>
      </tags>
  </entry>
  <entry>
    <title>软件设计的哲学</title>
    <url>/2020/01/21/a-philosophy-of-software-design-catalog/</url>
    <content><![CDATA[<h3 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h3><ol>
<li><p><a href="../../../../2020/02/03/a-philosophy-of-software-design-c1/index.html">简介 Introduction</a></p>
<p>1.1 如何使用这本书 How to use this book</p>
</li>
<li><p>复杂性的特点 The Nature of Complexity</p>
<p>2.1 复杂性的定义 Complexity defined</p>
<p>2.2 复杂性的表现 Symptoms of complexity</p>
<p>2.3 复杂性的原因 Causes of complexity</p>
<p>2.4 复杂性的递增性 Complexity is incremental</p>
<p>2.5 结论 Conclusion</p>
</li>
<li><p>只是能运行的代码是不够的 Working Code Isn’t Enough</p>
<p>3.1 战术性编程 Tactical programming</p>
<p>3.2 战略性编程 Strategy programming </p>
<p>3.3 投资多少？How much to invest?</p>
<p>3.4 创业和投资 Startups and investment</p>
<p>3.5 结论 Conclusion</p>
</li>
<li><p>模块应该是深的 Modules Should Be Deep</p>
<p>4.1 模块化设计 Modular design</p>
<p>4.2 接口里有什么 What’s in an interface?</p>
<p>4.3 抽象 Abstractions</p>
<p>4.4 深层模块 Deep Modules</p>
<p>4.5 浅层模块 Shallow Modules</p>
<p>4.6 Classitis</p>
<p>4.7 例子：Java和Unix I/O  Example: Java and Unix I/O</p>
<p>4.8 结论 Conclusion</p>
</li>
<li><p>信息隐藏和泄露 Information Hiding (and Leakage)</p>
<p>5.1 信息隐藏 Information hiding</p>
<p>5.2 信息泄露 Information leakage</p>
<p>5.3 时间分解 Temporal decomposition</p>
<p>5.4 例子：HTTP服务器 Example: HTTP server</p>
<p>5.5 例子：太多的类 Example: too many classes</p>
<p>5.6 例子：HTTP参数处理 Example: HTTP parameter handling</p>
<p>5.7 例子：HTTP响应中的缺省值 Example: defaults in HTTP response</p>
<p>5.8 类中的信息隐藏 Information hiding within a class</p>
<p>5.9 太过分了 Taking it too far</p>
<p>5.10 结论 Conclusion</p>
</li>
<li><p>通用模块更深入 General-Purpose Modules are Deeper</p>
<p>6.1 让类具有一定的通用性 Make classes somewhat general-purpose</p>
<p>6.2 例子：为编辑器存储文本 Example: storing text for an editor</p>
<p>6.3 更通用的API A more general-purpose API</p>
<p>6.4 通用性带来更好的信息隐藏 Generality leads to better information hiding</p>
<p>6.5 问自己的一些问题 Questions to ask yourself</p>
<p>6.6 结论 Conclusion</p>
</li>
<li><p>不同的层，不同的抽象 Different Layer, Different Abstraction</p>
<p>7.1 透传方法 Pass-through methods</p>
<p>7.2 什么时候接口复制是可以的？ When is interface duplication OK?</p>
<p>7.3 修饰符 Decorators</p>
<p>7.4 接口 vs 实现  Interface versus implementation</p>
<p>7.5 透传变量 Pass-through variables</p>
<p>7.6 结论 Conclusion</p>
</li>
</ol>
<a id="more"></a>

<ol start="8">
<li><p>降低复杂度 Pull Complexity Downwards</p>
<p>8.1 例子：编辑器文本类 Example: editor text class</p>
<p>8.2 例子：配置参数 Examples: configuration parameters</p>
<p>8.3 太过分了 Taking it too far</p>
<p>8.4 结论 Conclusion</p>
</li>
<li><p>在一起好还是分开好？ Better Together Or Better Apart?</p>
<p>9.1 如果信息是共享的就放到一起 Bring together if information is shared</p>
<p>9.2 如果能够简化接口就放到一起 Bring together if it will simplify the interface</p>
<p>9.3 放到一起消除重复 Bring together to eliminate duplication</p>
<p>9.4 把通用代码和专用代码分开 Separate general-purpose and special-purpose code</p>
<p>9.5 例子：游标插入和检索 Example: insertion cursor and selection</p>
<p>9.6 例子：日志单独的类 Example: separate class for log</p>
<p>9.7 例子：编辑器回退机制 Example: editor undo mechanism</p>
<p>9.8 分割和连接方法 Splitting and joining methods</p>
<p>9.9 结论 Conclusion</p>
</li>
<li><p>定义不存在的错误 Define Errors Out Of Existence</p>
<p>10.1 为什么异常增加了复杂性 Why exceptions add complexity</p>
<p>10.2 太多的异常 Too many exceptions</p>
<p>10.3 定义不存在的错误 Define errors out of existence</p>
<p>10.4 例子：Windows中删除文件 Example: file deletion in Windows</p>
<p>10.5 例子：Java库中substring方法 Example: Java substring method</p>
<p>10.6 隐藏异常 Mask exceptions</p>
<p>10.7 异常聚合 Exception aggregation</p>
<p>10.8 只是崩溃？ Just crash?</p>
<p>10.9 设计不存在的特殊情况 Design special cases out of existence</p>
<p>10.10 太过分了 Taking it too far</p>
<p>10.11 结论 Conclusion</p>
</li>
<li><p>设计两次 Design it Twice</p>
</li>
<li><p>为什么要写注释？四个借口 Why Write Comments? The Four Excuses</p>
<p>12.1 好的代码是自我注释型的 Good code is self-documenting</p>
<p>12.2 我没有时间写注释 I don’t have time to write comments</p>
<p>12.3 注释会过期还会误导别人 Comments get out of date and become misleading</p>
<p>12.4 所有我看过的注释都没什么用 All the comments I have seen are worthless</p>
<p>12.5 书写良好的注释带来的好处 Benefits of well-written comments</p>
</li>
<li><p>注释应该描述在代码中体现不明显的内容 Comments Should Describe Things that Aren’t Obvious from the Code</p>
<p>13.1 选取约定 Pick conventions</p>
<p>13.2 不要复述代码内容 Don’t repeat the code</p>
<p>13.3 低层级的注释让理解更精确 Low-level comments add precision</p>
<p>13.4 高层级的注释让理解更直观 High-level comments enhance intuition</p>
<p>13.5 接口文档 interface documentation</p>
<p>13.6 实现注释：是什么和为什么，而不是怎么实现 Implementation comments: what and why, not how</p>
<p>13.7 跨模块的设计决策 Cross-module design decisions</p>
<p>13.8 结论 Conclusion</p>
<p>13.9 回答13.5节中的问题 Answers to questions from Section 13.5</p>
</li>
<li><p>选择命名 Choosing Names</p>
<p>14.1 例子：糟糕的命名导致错误 Example: bad names cause bugs</p>
<p>14.2 建立印象 Create an image</p>
<p>14.3 命名应该精确 Names should be precise</p>
<p>14.4 命名要一致 Use names consistently</p>
<p>14.5 一个不同的意见：Go风格指南 A different opinion: Go style guide</p>
<p>14.6 结论 Conclusion</p>
</li>
<li><p>先写注释 Write The Comments First</p>
<p>15.1 后写的注释是糟糕的注释 Delayed comments are bad comments</p>
<p>15.2 先写注释 Write the comments first</p>
<p>15.3 注释是设计工具 Comments are a design tool</p>
<p>15.4 早写的注释更有趣 Early comments  are fun comments</p>
<p>15.5 早些的注释成本高么？Are early comments expensive?</p>
<p>15.6 结论 Conclusion</p>
</li>
<li><p>修改已有代码 Modifying Existing Code</p>
<p>16.1 保持战略 Stay strategic</p>
<p>16.2 维护注释：保持注释和代码一致 Maintaining comments: keep the comments near the code</p>
<p>16.3 注释属于代码，而不是提交日志 Comments belong in the code, not the commit log</p>
<p>16.4 维护注释：避免重复 Maintaining comments: avoid duplication</p>
<p>16.5 维护注释：检查差别 Maintaining comments: check the diffs</p>
<p>16.6 更高层级的注释更容易维护 Higher-level comments are easier to maintain</p>
</li>
<li><p>一致性 Consistency</p>
<p>17.1 一致性的例子 Examples of consistency</p>
<p>17.2 确保一致性 Ensuring consistency</p>
<p>17.3 太过分了 Taking it too far</p>
<p>17.4 结论 Conclusion</p>
</li>
<li><p>代码应该是易理解的 Code Should be Obvious</p>
<p>18.1 令代码更易理解的事物 Things that make code more obvious</p>
<p>18.2 令代码更难理解的事物 Things that make code less obvious</p>
<p>18.3 结论 Conclusion</p>
</li>
<li><p>软件趋势</p>
<p>19.1 面向对象编程和继承 Object-oriented programming and inheritance</p>
<p>19.2 敏捷开发 Agile development</p>
<p>19.3 单元测试 Unit test</p>
<p>19.4 测试驱动开发 Test-driven development</p>
<p>19.5 设计模式 Design patterns</p>
<p>19.6 变量访问方法 Getters and Setters</p>
<p>19.7 结论 Conclusion</p>
</li>
<li><p>性能设计 Designing for Performance</p>
<p>20.1 如何考虑性能 How to think about performance</p>
<p>20.2 修改前评估 Measure before modifying</p>
<p>20.3 围绕关键路径进行设计 Design around the critical path</p>
<p>20.4 例子：RAMCloud缓存 An example: RAMCloud Buffers</p>
<p>20.5 结论 Conclusion</p>
</li>
<li><p>结论 Conclusion</p>
</li>
</ol>
]]></content>
      <categories>
        <category>编程艺术</category>
      </categories>
      <tags>
        <tag>软件设计</tag>
      </tags>
  </entry>
  <entry>
    <title>Dart语言之旅</title>
    <url>/2020/01/11/dart-tour/</url>
    <content><![CDATA[<blockquote>
<p><font size=3>该文章翻译自<strong><a href="https://dart.dev/guides/language/language-tour#comments" target="_blank" rel="noopener">A tour of the Dart language</a></strong>，仅供学习参考。</font></p>
</blockquote>
<br>

<p>这篇文章展示了Dart语言的主要特性，涉及的内容从变量、操作符直到类型和库，它假设读者已经了解了如何使用一种计算机语言进行编程。对于更简短的语言介绍可以参见<strong><a href="https://dart.dev/samples" target="_blank" rel="noopener">语言示例</a></strong>。</p>
<p>如果想要学习Dart核心库更多的内容，可以参见<strong><a href="https://dart.dev/guides/libraries/library-tour" target="_blank" rel="noopener">库介绍</a></strong>。想要了解语言特性更多的细节，可以参考<strong><a href="https://dart.dev/guides/language/spec" target="_blank" rel="noopener">Dart语言规范</a></strong>。</p>
<blockquote>
<p><font size=2> 注：对于Dart语言的大多数特性都可以使用DartPad来体验（<strong><a href="https://dartpad.dev/" target="_blank" rel="noopener">DartPad</a></strong>），这个页面使用嵌入式DartPad展示了一些范例，如果打开后是空页面，请访问 <a href="https://dart.dev/tools/dartpad/troubleshoot" target="_blank" rel="noopener">DartPad troubleshooting page</a> </font></p>
</blockquote>
<br>

<h3 id="一个基本的Dart程序"><a href="#一个基本的Dart程序" class="headerlink" title="一个基本的Dart程序"></a>一个基本的Dart程序</h3><p>下面的代码用到了Dart很多基本特性：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Define a function.</span></span><br><span class="line">printInteger(<span class="built_in">int</span> aNumber) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'The number is <span class="subst">$aNumber</span>.'</span>); <span class="comment">// Print to console.</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// This is where the app starts executing.</span></span><br><span class="line">main() &#123;</span><br><span class="line">  <span class="keyword">var</span> number = <span class="number">42</span>; <span class="comment">// Declare and initialize a variable.</span></span><br><span class="line">  printInteger(number); <span class="comment">// Call a function.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中，几乎所有app都会涉及到的特性内容有以下几个方面：</p>
<p><font size=2><strong><em>//This is a comment.</em></strong></font></p>
<p>单行注释。另外Dart也支持多行注释和文档注释，关于细节可参考<strong><a href="https://dart.dev/guides/language/language-tour#comments" target="_blank" rel="noopener">Comments</a></strong>.</p>
<p><font size=2><strong><em>int</em></strong></font></p>
<p>一种内建类型，其他<strong><a href="https://dart.dev/guides/language/language-tour#built-in-types" target="_blank" rel="noopener">内建类型</a></strong>还包括<font size=3><strong><em>String，List<em>，</em>bool</em></strong></font></p>
<p><font size=2><strong><em>42</em></strong></font></p>
<p>一个数值字面量，数值字面量是一种编译时常量。</p>
<p><font size=2><strong><em>print()</em></strong></font></p>
<p>一种输出到屏幕的简便方法。</p>
<p><font size=2><strong><em>‘…’ 或者 “…”</em></strong></font></p>
<p>字符串字面量。</p>
<p><font size=2><strong><em>$variableName 或者 $(expression)</em></strong></font></p>
<p>字符串插值：可以在一个字符串字面量中插入一个字符串变量或者等价的字符串表达式。更多信息请参见<strong><a href="https://dart.dev/guides/language/language-tour#strings" target="_blank" rel="noopener">Strings</a></strong>。</p>
<p><font size=2><strong><em>main()</em></strong></font></p>
<p>在应用执行时必需的一个特殊的全局函数。更多信息参见<strong><a href="https://dart.dev/guides/language/language-tour#the-main-function" target="_blank" rel="noopener">The main() function</a></strong>。</p>
<p><font size=2><strong><em>var</em></strong></font></p>
<p>一种不用指定类型声明变量的方式。</p>
<blockquote>
<p>注：这篇文章中的代码遵循了<strong><a href="https://dart.dev/guides/language/effective-dart/style" target="_blank" rel="noopener">Dart风格指南</a></strong>。</p>
</blockquote>
<br>

<h3 id="重要概念"><a href="#重要概念" class="headerlink" title="重要概念"></a>重要概念</h3><p>在学习Dart语言时，以下的事实和概念需要牢记在心：</p>
<ul>
<li><p>赋给变量的任何值都是<strong><em>对象</em></strong>，而每个对象都是一个<strong><em>class</em></strong>的实例，甚至数字、函数以及null都是对象。所有的对象都继承自<strong><em><a href="https://api.dart.dev/stable/dart-core/Object-class.html" target="_blank" rel="noopener">Object</a></em></strong>类。</p>
</li>
<li><p>Dart是强类型语言，因为它同时支持类型推导，所以声明对象时的类型修饰符是可以略掉的。在上面的代码中，number可以被推导出为int类型。如果想显式地指定变量为无类型，要使用<strong><a href="https://dart.dev/guides/language/effective-dart/design#do-annotate-with-object-instead-of-dynamic-to-indicate-any-object-is-allowed" target="_blank" rel="noopener">特殊类型dynamic</a></strong>。</p>
</li>
<li><p>Dart支持泛型，如<strong><em>List&lt;int&gt;</em></strong>（整型对象列表）或者<strong><em>List&lt;dynamic&gt;</em></strong>（无类型对象列表）。</p>
</li>
<li><p>Dart支持全局函数（如<strong>main()</strong>），同时也支持与class或者object相关联的函数定义（也就是静态方法和实例方法）。另外也可以在函数中定义函数（嵌套函数或本地函数）。</p>
</li>
<li><p>类似地，Dart支持全局变量，同时也支持与class或者object相关联的变量定义（也就是静态变量和实例变量）。实例变量也被称作字段或属性。</p>
</li>
<li><p>与Java不同，Dart没有<strong><em>public、protected、private</em></strong>等关键字。如果一个标识符以下划线开头，则它的访问权限就是private。具体信息参见<strong><a href="https://dart.dev/guides/language/language-tour#libraries-and-visibility" target="_blank" rel="noopener">Libraries and visibility</a></strong>。</p>
</li>
<li><p>标识符首字符必须是字母或者下划线，后面则可以跟任意字符或者数字。</p>
</li>
<li><p>Dart同时支持表达式（<strong><em>expressions</em></strong>，有运行时值）和语句（<strong><em>statements</em></strong>，无运行时值），例如条件表达式 <strong><em>condition ? expr1 : expr2</em></strong>可以取值expr1或者expr2，而if-else语句则没有取值。一个语句经常包含多个表达式，而一个表达式不能直接包含语句。</p>
</li>
<li><p>Dart工具支持上报两类问题：<strong><em>warnings</em></strong> 和 <strong><em>errors</em></strong>。Warnings表明代码有可能不会正常工作，但不会妨碍程序运行。Errors又分编译时错误和运行时错误，编译时错误会造成程序无法运行，运行时错误会使程序在运行过程中抛出异常造成运行中断。</p>
</li>
</ul>
<a id="more"></a>

<br>

<h3 id="关键字"><a href="#关键字" class="headerlink" title="关键字"></a>关键字</h3><p>下表列出了Dart语言中所有需要特殊处理的关键字</p>
<table>
<thead>
<tr>
<th><a href="https://dart.dev/guides/language/language-tour#abstract-classes" target="_blank" rel="noopener">abstract</a><sup>2</sup></th>
<th><a href="https://dart.dev/guides/language/language-tour#important-concepts" target="_blank" rel="noopener">dynamic</a><sup>2</sup></th>
<th><a href="https://dart.dev/guides/language/language-tour#implicit-interfaces" target="_blank" rel="noopener">implements</a><sup>2</sup></th>
<th><a href="https://dart.dev/guides/language/language-tour#importing-only-part-of-a-library" target="_blank" rel="noopener">show</a><sup>1</sup></th>
<th><strong><a href="https://dart.dev/guides/language/language-tour#type-test-operators" target="_blank" rel="noopener">as</a></strong><sup>2</sup></th>
<th><a href="https://dart.dev/guides/language/language-tour#if-and-else" target="_blank" rel="noopener">else</a></th>
<th><a href="https://dart.dev/guides/language/language-tour#using-libraries" target="_blank" rel="noopener">import</a><sup>2</sup></th>
<th><a href="https://dart.dev/guides/language/language-tour#class-variables-and-methods" target="_blank" rel="noopener">static</a><sup>2</sup></th>
</tr>
</thead>
<tbody><tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#assert" target="_blank" rel="noopener">assert</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#enumerated-types" target="_blank" rel="noopener">enum</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#for-loops" target="_blank" rel="noopener">in</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#extending-a-class" target="_blank" rel="noopener">super</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#asynchrony-support" target="_blank" rel="noopener">async</a></strong><sup>1</sup></td>
<td><strong><a href="https://dart.dev/guides/libraries/create-library-packages" target="_blank" rel="noopener">export</a></strong><sup>2</sup></td>
<td><strong><a href="https://stackoverflow.com/questions/28595501/was-the-interface-keyword-removed-from-dart" target="_blank" rel="noopener">interface</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#switch-and-case" target="_blank" rel="noopener">switch</a></strong><sup>1</sup></td>
</tr>
<tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#asynchrony-support" target="_blank" rel="noopener">await</a></strong><sup>3</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#extending-a-class" target="_blank" rel="noopener">extends</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#type-test-operators" target="_blank" rel="noopener">is</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#generators" target="_blank" rel="noopener">sync</a></strong><sup>1</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#break-and-continue" target="_blank" rel="noopener">break</a></strong></td>
<td><strong><a href="https://stackoverflow.com/questions/24929659/what-does-external-mean-in-dart" target="_blank" rel="noopener">external</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#libraries-and-visibility" target="_blank" rel="noopener">library</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#constructors" target="_blank" rel="noopener">this</a></strong></td>
</tr>
<tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#switch-and-case" target="_blank" rel="noopener">case</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#factory-constructors" target="_blank" rel="noopener">factory</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#adding-features-to-a-class-mixins" target="_blank" rel="noopener">mixin</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#throw" target="_blank" rel="noopener">throw</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#catch" target="_blank" rel="noopener">catch</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#booleans" target="_blank" rel="noopener">false</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#using-constructors" target="_blank" rel="noopener">new</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#booleans" target="_blank" rel="noopener">true</a></strong></td>
</tr>
<tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#instance-variables" target="_blank" rel="noopener">class</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#final-and-const" target="_blank" rel="noopener">final</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#default-value" target="_blank" rel="noopener">null</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#catch" target="_blank" rel="noopener">try</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#final-and-const" target="_blank" rel="noopener">const</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#finally" target="_blank" rel="noopener">finally</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#catch" target="_blank" rel="noopener">on</a></strong><sup>1</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#typedefs" target="_blank" rel="noopener">typedef</a></strong><sup>2</sup></td>
</tr>
<tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#break-and-continue" target="_blank" rel="noopener">continue</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#for-loops" target="_blank" rel="noopener">for</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#overridable-operators" target="_blank" rel="noopener">operator</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#variables" target="_blank" rel="noopener">var</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/sound-problems#the-covariant-keyword" target="_blank" rel="noopener">covariant</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#functions" target="_blank" rel="noopener">Function</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/libraries/create-library-packages#organizing-a-library-package" target="_blank" rel="noopener">part</a></strong><sup>2</sup></td>
<td><strong><a href="https://medium.com/dartlang/dart-2-legacy-of-the-void-e7afb5f44df0" target="_blank" rel="noopener">void</a></strong></td>
</tr>
<tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#switch-and-case" target="_blank" rel="noopener">default</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#getters-and-setters" target="_blank" rel="noopener">get</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#catch" target="_blank" rel="noopener">rethrow</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#while-and-do-while" target="_blank" rel="noopener">while</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#lazily-loading-a-library" target="_blank" rel="noopener">deferred</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#importing-only-part-of-a-library" target="_blank" rel="noopener">hide</a></strong><sup>1</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#functions" target="_blank" rel="noopener">return</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#adding-features-to-a-class-mixins" target="_blank" rel="noopener">with</a></strong></td>
</tr>
<tr>
<td><strong><a href="https://dart.dev/guides/language/language-tour#while-and-do-while" target="_blank" rel="noopener">do</a></strong></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#if-and-else" target="_blank" rel="noopener">if</a></strong></td>
<td><strong><a href="https://api.dart.dev/stable/dart-core/Set-class.html" target="_blank" rel="noopener">set</a></strong><sup>2</sup></td>
<td><strong><a href="https://dart.dev/guides/language/language-tour#generators" target="_blank" rel="noopener">yield</a></strong><sup>3</sup></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<p>Dart要求编程时避免将这些关键字用作标识符，实在需要的话，可以使用这些有<strong><em>superscripts</em></strong>标记号的关键字：</p>
<ul>
<li><p>标记为1类的关键字称为<strong>上下文关键字</strong>，它们仅在程序特殊的位置才表现出特殊的语义，其他地方都可以作为标识符来使用。</p>
</li>
<li><p>标记为2类的关键字称为<strong>内建标识符</strong>，为了简化从js代码向Dart移植的任务，这些关键字大部分情况下都可以用作标识符，但是不能用在class名或者type名，以及import前缀上。</p>
</li>
<li><p>标记为3类的关键字是在1.0 release之后新增的，主要与<strong><a href="https://dart.dev/guides/language/language-tour#asynchrony-support" target="_blank" rel="noopener">asynchrony support</a></strong>相关的限制保留字，在任何被标记为<strong>async、async*、sync*</strong>的函数体中都不允许使用<strong>await</strong>和<strong>yield</strong>作为标识符。</p>
</li>
</ul>
<p>表中除以上几类之外的<strong>保留字</strong>都不允许作为标识符使用。</p>
<br>

<h3 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h3><p>下面是一个创建变量并初始化的例子</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name = <span class="string">'Bob'</span>;</span><br></pre></td></tr></table></figure>

<p>Dart中变量存储的是一个引用，如<font color=green><em>name</em></font>变量包含的是一个指向<font color=green><em>String</em></font>类型对象，且该对象值为’Bob’的引用。这里Dart推导出<font color=green><em>name</em></font>变量的类型为<font color=green><em>String</em></font>，但也可以为<font color=green><em>name</em></font>赋其他值来改变它的类型。如果一个对象不限于指向单一类型的话，可以遵照<strong>design guidelines</strong>为它指定<font color=green><em>Object</em></font>或者<font color=green><em>dynamic</em></font>类型。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">dynamic</span> name = <span class="string">'Bob'</span>;</span><br></pre></td></tr></table></figure>

<p>另一种方式就是显式地在变量声明中定义它的推导类型。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span> name = <span class="string">'Bob'</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：这篇文章沿用了<strong><a href="https://dart.dev/guides/language/effective-dart/design#types" target="_blank" rel="noopener">style guide recommendation</a></strong>的约定，对于本地变量使用var而不是显式类型声明。</p>
</blockquote>
<br>

<h4 id="缺省值"><a href="#缺省值" class="headerlink" title="缺省值"></a>缺省值</h4><p>对于未初始化的变量会缺省为它提供一个为<font color=green><em>null</em></font>的初始值。未初始化的数值类型的变量也会是null，因为在Dart中数值也是对象。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">int</span> lineCount;</span><br><span class="line"><span class="keyword">assert</span>(lineCount == <span class="keyword">null</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：release版本的代码会忽略掉assert调用，而在debug版本中则相反，如果condition为false的话，<font color=green><strong><em>assert(condition)</em></strong></font>会抛出异常，具体参见<strong><a href="https://dart.dev/guides/language/language-tour#assert" target="_blank" rel="noopener">Assert</a></strong>。</p>
</blockquote>
<br>

<h4 id="Final-amp-const"><a href="#Final-amp-const" class="headerlink" title="Final &amp; const"></a>Final &amp; const</h4><p>如果变量不想被改变的话，可以使用<font color=green><em>final</em></font>或者<font color=green><em>const</em></font>来替代<font color=green><em>var</em></font>以及其他类型声明。final变量只能被赋值一次，const变量实质上是编译时常量（const变量也是隐式的final变量）。final修饰的全局变量或者类变量会在第一次使用时初始化。</p>
<blockquote>
<p>注：实例变量可以用<font color=green><em>final</em></font>修饰但不能用<font color=green><em>const</em></font>。final修饰的实例变量必须在构造函数执行之前初始化——或者在变量声明的位置，或者在构造函数的<strong><a href="https://dart.dev/guides/language/language-tour#initializer-list" target="_blank" rel="noopener">初始化列表</a></strong>中初始化。</p>
</blockquote>
<br>

<p>下面是创建和设置final变量的一个例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> name = <span class="string">'Bob'</span>; <span class="comment">// Without a type annotation </span></span><br><span class="line"><span class="keyword">final</span> <span class="built_in">String</span> nickname = <span class="string">'Bobby'</span>;</span><br></pre></td></tr></table></figure>

<p>不能改变final变量的值：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">name = <span class="string">'Alice'</span>; <span class="comment">// Error: a final variable can only be set once.</span></span><br></pre></td></tr></table></figure>

<p>如果需要编译时常量，可以使用<font color=green><em>const</em></font>关键字。如果是const类变量，请使用<font color=green><em>static const</em></font>来修饰它。无论在哪里声明const常量，都要为它赋上编译时常量值，比如数值或字符串字面量，const变量，或者能够得到常量结果的数值算术式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> bar = <span class="number">1000000</span>; <span class="comment">// Unit of pressure (dynes/cm2)</span></span><br><span class="line"><span class="keyword">const</span> <span class="built_in">double</span> atm = <span class="number">1.01325</span> * bar; <span class="comment">// Standard atmosphere</span></span><br></pre></td></tr></table></figure>

<p><font color=green><em>const</em></font>不仅仅能修饰变量，还可以用来定义常量值，也可以用来在声明构造函数时定义常量值。任何变量都可以赋常量值。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo = <span class="keyword">const</span> [];</span><br><span class="line"><span class="keyword">final</span> bar = <span class="keyword">const</span> [];</span><br><span class="line"><span class="keyword">const</span> baz = []; <span class="comment">// Equivalent to `const []`</span></span><br></pre></td></tr></table></figure>

<p>可以在声明<font color=green><em>const</em></font>变量的初始化表达式中省略掉<font color=green><em>const</em></font>，比如上面的<font color=green><em>baz</em></font>声明。具体细节可以参见<strong><a href="https://dart.dev/guides/language/effective-dart/usage#dont-use-const-redundantly" target="_blank" rel="noopener">DON’T use const redundantly</a></strong>。</p>
<p>对于一个非const、非final变量，即使给它赋了常量值，也可以修改它。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">foo = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; <span class="comment">// Was const []</span></span><br></pre></td></tr></table></figure>

<p>而一个const变量的值不能被修改。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">baz = [<span class="number">42</span>]; <span class="comment">// Error: Constant variables can't be assigned a value.</span></span><br></pre></td></tr></table></figure>

<p>在Dart2.5版本中，可以使用<strong><a href="https://dart.dev/guides/language/language-tour#type-test-operators" target="_blank" rel="noopener">type checks and casts</a></strong>，<strong><a href="https://dart.dev/guides/language/language-tour#collection-operators" target="_blank" rel="noopener">collection if and collection for</a></strong>或者<strong><a href="https://dart.dev/guides/language/language-tour#spread-operator" target="_blank" rel="noopener">spread operators</a></strong>几种方式来定义常量。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Valid compile-time constants as of Dart 2.5.</span></span><br><span class="line"><span class="keyword">const</span> <span class="built_in">Object</span> i = <span class="number">3</span>; <span class="comment">// Where i is a const Object with an int value...</span></span><br><span class="line"><span class="keyword">const</span> list = [i <span class="keyword">as</span> <span class="built_in">int</span>]; <span class="comment">// Use a typecast.</span></span><br><span class="line"><span class="keyword">const</span> map = &#123;<span class="keyword">if</span> (i <span class="keyword">is</span> <span class="built_in">int</span>) i: <span class="string">"int"</span>&#125;; <span class="comment">// Use is and collection if.</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">set</span> = &#123;<span class="keyword">if</span> (list <span class="keyword">is</span> <span class="built_in">List</span>&lt;<span class="built_in">int</span>&gt;) ...list&#125;; <span class="comment">// ...and a spread.</span></span><br></pre></td></tr></table></figure>

<p>关于使用const创建常量的更多信息参见<strong><a href="https://dart.dev/guides/language/language-tour#lists" target="_blank" rel="noopener">Lists</a></strong>，<strong><a href="https://dart.dev/guides/language/language-tour#maps" target="_blank" rel="noopener">Maps</a></strong>和<strong><a href="https://dart.dev/guides/language/language-tour#classes" target="_blank" rel="noopener">Classes</a></strong>。</p>
<br>

<h3 id="内建类型"><a href="#内建类型" class="headerlink" title="内建类型"></a>内建类型</h3><p>Dart语言支持以下类型：</p>
<ul>
<li><p>numbers</p>
</li>
<li><p>strings</p>
</li>
<li><p>booleans</p>
</li>
<li><p>lists </p>
</li>
<li><p>sets</p>
</li>
<li><p>maps</p>
</li>
<li><p>runes</p>
</li>
<li><p>symbols</p>
</li>
</ul>
<p>对于以上类型的对象都可以使用字面量来初始化，比如像<font color=green><em>‘this is a string’</em></font>这样的字符串字面量，或者<font color=green><em>true</em></font>这样的布尔字面量。</p>
<p>在Dart中，每个变量都关联一个对象——一个类的实例——通常使用构造函数来初始化。一些内建类型会有自己的构造函数，比如可以调用构造函数<font color=green><strong>Map()</strong></font>创建map对象。</p>
<br>

<h4 id="Numbers-数值"><a href="#Numbers-数值" class="headerlink" title="Numbers 数值"></a>Numbers 数值</h4><p>Dart的数值类型包括两种：</p>
<p><font color=green><strong>int</strong></font></p>
<p>最长64bit的整数类型，具体位数依赖于平台。在Dart VM中，其取值范围为 -2<sup>63</sup> ~ 2<sup>63</sup>-1。如果Dart编译成Javascript的话，该类型会使用<strong><a href="https://stackoverflow.com/questions/2802957/number-of-bits-in-javascript-numbers/2803010#2803010" target="_blank" rel="noopener">JavaScript numbers</a></strong>定义，取值范围变为 -2<sup>53 </sup> ~ 2<sup>53</sup>-1。</p>
<p><font color=green><strong>double</strong></font></p>
<p>由IEEE 754标准定义的64bit双精度浮点数类型</p>
<p>不管是<font color=green><em>int</em></font>还是<font color=green><em>double</em></font>都是<strong><a href="https://api.dart.dev/stable/dart-core/num-class.html" target="_blank" rel="noopener">num</a></strong>的子类型。num类型支持+、-、*、/等基本操作符，还支持<font color=green><em>abs()</em></font>、<font color=green><em>ceil()</em></font>和<font color=green><em>floor()</em></font>等方法。除此之外，在<font color=green><em>int</em></font>类中还定义了像&gt;&gt;这样的位操作符。如果期望的方法在num和它的子类中没有找到的话，可以去<strong><a href="https://api.dart.dev/stable/dart-math" target="_blank" rel="noopener">dart:math</a></strong>中看一看。</p>
<p>整数是不含小数点的数值类型，可以表示为以下的字面量形式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>; <span class="keyword">var</span> hex = <span class="number">0xDEADBEEF</span>;</span><br></pre></td></tr></table></figure>

<p>包含小数点的属于double类型，可以表示为以下形式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> y = <span class="number">1.1</span>;</span><br><span class="line"><span class="keyword">var</span> exponents = <span class="number">1.42e5</span>;</span><br></pre></td></tr></table></figure>

<p>在Dart2.1中，整数字面量会根据需要自动转换成double类型。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">double</span> z = <span class="number">1</span>; <span class="comment">// Equivalent to double z = 1.0.</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：在Dart2.1之前，在double上下文中使用整数字面量会报错。</p>
</blockquote>
<p>下面这段是字符串和数值互转的范例代码：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// String -&gt; int</span></span><br><span class="line"><span class="keyword">var</span> one = <span class="built_in">int</span>.parse(<span class="string">'1'</span>);</span><br><span class="line"><span class="keyword">assert</span>(one == <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// String -&gt; double</span></span><br><span class="line"><span class="keyword">var</span> onePointOne = <span class="built_in">double</span>.parse(<span class="string">'1.1'</span>);</span><br><span class="line"><span class="keyword">assert</span>(onePointOne == <span class="number">1.1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// int -&gt; String</span></span><br><span class="line"><span class="built_in">String</span> oneAsString = <span class="number">1.</span>toString();</span><br><span class="line"><span class="keyword">assert</span>(oneAsString == <span class="string">'1'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// double -&gt; String</span></span><br><span class="line"><span class="built_in">String</span> piAsString = <span class="number">3.14159</span>.toStringAsFixed(<span class="number">2</span>);</span><br><span class="line"><span class="keyword">assert</span>(piAsString == <span class="string">'3.14'</span>);</span><br></pre></td></tr></table></figure>

<p>如下所示，int类型定义了位移（&lt;&lt;，&gt;&gt;）、AND（&amp;）、OR（|）等操作符。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>((<span class="number">3</span> &lt;&lt; <span class="number">1</span>) == <span class="number">6</span>); <span class="comment">// 0011 &lt;&lt; 1 == 0110</span></span><br><span class="line"><span class="keyword">assert</span>((<span class="number">3</span> &gt;&gt; <span class="number">1</span>) == <span class="number">1</span>); <span class="comment">// 0011 &gt;&gt; 1 == 0001</span></span><br><span class="line"><span class="keyword">assert</span>((<span class="number">3</span> | <span class="number">4</span>) == <span class="number">7</span>); <span class="comment">// 0011 | 0100 == 0111</span></span><br></pre></td></tr></table></figure>

<p>字面量数值会被处理成编译时常量。对于算术表达式，如果其中的操作符是常量的话，表达式会直接求值转换为编译时常量。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> msPerSecond = <span class="number">1000</span>;</span><br><span class="line"><span class="keyword">const</span> secondsUntilRetry = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">const</span> msUntilRetry = secondsUntilRetry * msPerSecond;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Strings-字符串"><a href="#Strings-字符串" class="headerlink" title="Strings 字符串"></a>Strings 字符串</h4><p>Dart字符串是utf-16编码的字符序列，语法上可以用单引号或双引号来定义。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s1 = <span class="string">'Single quotes work well for string literals.'</span>;</span><br><span class="line"><span class="keyword">var</span> s2 = <span class="string">"Double quotes work just as well."</span>;</span><br><span class="line"><span class="keyword">var</span> s3 = <span class="string">'It\'s easy to escape the string delimiter.'</span>;</span><br><span class="line"><span class="keyword">var</span> s4 = <span class="string">"It's even easier to use the other delimiter."</span>;</span><br></pre></td></tr></table></figure>

<p>可以以<font color=green><em>${expression}</em></font>的形式在字符串中插入表达式的值。如果表达式只是一个简单的标识符，大括号可以直接省略掉。如果想得到object对应的字符串，可以调用object类的<font color=green><em>toString()</em></font>方法实现。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">'string interpolation'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>(<span class="string">'Dart has <span class="subst">$s</span>, which is very handy.'</span> ==</span><br><span class="line">    <span class="string">'Dart has string interpolation, '</span> +</span><br><span class="line">        <span class="string">'which is very handy.'</span>);</span><br><span class="line"><span class="keyword">assert</span>(<span class="string">'That deserves all caps. '</span> +</span><br><span class="line">        <span class="string">'<span class="subst">$&#123;s.toUpperCase()&#125;</span> is very handy!'</span> ==</span><br><span class="line">    <span class="string">'That deserves all caps. '</span> +</span><br><span class="line">        <span class="string">'STRING INTERPOLATION is very handy!'</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：<font color=green><em>==</em></font>操作符是判断两个对象是否相等，对字符串对象来讲，如果它们包含的字符序列相同则等式成立。</p>
</blockquote>
<p>可以用<font color=green><em>+</em></font>操作符或者邻近定义的方式拼接字符串：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s1 = <span class="string">'String '</span></span><br><span class="line">    <span class="string">'concatenation'</span></span><br><span class="line">    <span class="string">" works even over line breaks."</span>;</span><br><span class="line"><span class="keyword">assert</span>(s1 ==</span><br><span class="line">    <span class="string">'String concatenation works even over '</span></span><br><span class="line">        <span class="string">'line breaks.'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> s2 = <span class="string">'The + operator '</span> + <span class="string">'works, as well.'</span>;</span><br><span class="line"><span class="keyword">assert</span>(s2 == <span class="string">'The + operator works, as well.'</span>);</span><br></pre></td></tr></table></figure>

<p>另一种方式是使用三引号（’’’或者”””）来创建多行的字符串：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s1 = <span class="string">'''</span></span><br><span class="line"><span class="string">You can create</span></span><br><span class="line"><span class="string">multi-line strings like this one.</span></span><br><span class="line"><span class="string">'''</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> s2 = <span class="string">"""This is also a</span></span><br><span class="line"><span class="string">multi-line string."""</span>;</span><br></pre></td></tr></table></figure>

<p>还可以用<font color=green><em>r</em></font>前缀创建“raw”字符串：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">r'In a raw string, not even \n gets special treatment.'</span>;</span><br></pre></td></tr></table></figure>

<p>阅读<strong><a href="https://dart.dev/guides/language/language-tour#characters" target="_blank" rel="noopener">Runes and grapheme clusters</a></strong>了解如何在字符串中表示unicode字符。</p>
<p>字符串字面量还会被处理成编译时常量。对于嵌入表达式的字符串，如果表达式求值后是null、数值、字符串或者布尔值，整个字符串会在求值后转换为编译时常量。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// These work in a const string. </span></span><br><span class="line"><span class="keyword">const</span> aConstNum = <span class="number">0</span>; </span><br><span class="line"><span class="keyword">const</span> aConstBool = <span class="keyword">true</span>; </span><br><span class="line"><span class="keyword">const</span> aConstString = <span class="string">'a constant string'</span>; </span><br><span class="line"><span class="comment">// These do NOT work in a const string. </span></span><br><span class="line"><span class="keyword">var</span> aNum = <span class="number">0</span>; </span><br><span class="line"><span class="keyword">var</span> aBool = <span class="keyword">true</span>; </span><br><span class="line"><span class="keyword">var</span> aString = <span class="string">'a string'</span>; </span><br><span class="line"><span class="keyword">const</span> aConstList = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; </span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> validConstString = <span class="string">'<span class="subst">$aConstNum</span> <span class="subst">$aConstBool</span> <span class="subst">$aConstString</span>'</span>; </span><br><span class="line"><span class="comment">// const invalidConstString = '$aNum $aBool $aString $aConstList';</span></span><br></pre></td></tr></table></figure>

<p>对于字符串相关的更多内容可以参考<strong><a href="https://dart.dev/guides/libraries/library-tour#strings-and-regular-expressions" target="_blank" rel="noopener">Strings and regular expressions</a></strong>。</p>
<br>

<h4 id="Booleans-布尔量"><a href="#Booleans-布尔量" class="headerlink" title="Booleans 布尔量"></a>Booleans 布尔量</h4><p>为了表示布尔值，Dart支持名为<font color=green><em>bool</em></font>的布尔类型。布尔值字符量对象仅有两个：<font color=green><em>true</em></font>和<font color=green><em>false</em></font>，均为编译时常量。</p>
<p>考虑类型安全，像<font color=green><em>if（nonbooleanValue）</em></font>或者<font color=green><em>assert（nonbooleanValue）</em></font>这样的代码都不被允许，而需要显式地检查相应变量值，如下所示：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Check for an empty string. </span></span><br><span class="line"><span class="keyword">var</span> fullName = <span class="string">''</span>; </span><br><span class="line"><span class="keyword">assert</span>(fullName.isEmpty); </span><br><span class="line"></span><br><span class="line"><span class="comment">// Check for zero. </span></span><br><span class="line"><span class="keyword">var</span> hitPoints = <span class="number">0</span>; </span><br><span class="line"><span class="keyword">assert</span>(hitPoints &lt;= <span class="number">0</span>); </span><br><span class="line"></span><br><span class="line"><span class="comment">// Check for null. </span></span><br><span class="line"><span class="keyword">var</span> unicorn; </span><br><span class="line"><span class="keyword">assert</span>(unicorn == <span class="keyword">null</span>); </span><br><span class="line"></span><br><span class="line"><span class="comment">// Check for NaN. </span></span><br><span class="line"><span class="keyword">var</span> iMeantToDoThis = <span class="number">0</span> / <span class="number">0</span>; </span><br><span class="line"><span class="keyword">assert</span>(iMeantToDoThis.isNaN);</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Lists-列表"><a href="#Lists-列表" class="headerlink" title="Lists 列表"></a>Lists 列表</h4><p>数组或者说有序对象，在几乎每个程序语言中都是最基本的集合类型。在Dart中，数组被定义为<font color=green><em>List</em></font>对象，大多数人称其为列表。Dart列表字面量看起来像javascript数组，下面是个简单的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：对上面的list变量Dart会推导出类型List<int>，如果尝试给list插入非int类型的对象，运行时会抛出错误。更多内容可参考<strong><a href="https://dart.dev/guides/language/sound-dart#type-inference" target="_blank" rel="noopener">type inference</a></strong>。</p>
</blockquote>
<p>列表使用从零开始的下标规则，0是第一个元素的下标，list.length - 1是最后一个元素的下标。与javascript类似，可以参考下面的例子获取list的长度和引用list元素：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">assert</span>(list.length == <span class="number">3</span>);</span><br><span class="line"><span class="keyword">assert</span>(list[<span class="number">1</span>] == <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">list[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">assert</span>(list[<span class="number">1</span>] == <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>在列表前加上<font color=green><em>const</em></font>修饰符，可以定义一个列表编译时常量：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> constantList = <span class="keyword">const</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="comment">// constantList[1] = 1; // Uncommenting this causes an error.</span></span><br></pre></td></tr></table></figure>

<p>Dart引入了 <strong>spread operator</strong>（<font color=green><em>…</em></font>）和 <strong>null-aware spread operator</strong>（<font color=green><em>…?</em></font>)，为向集合插入多个元素提供了一个简单的方法。</p>
<p>比如可以调用 spread operator（<font color=green><em>…</em></font>）把一个list的所有元素插入到另一个list中。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]; <span class="keyword">var</span> list2 = [<span class="number">0</span>, ...list]; <span class="keyword">assert</span>(list2.length == <span class="number">4</span>);</span><br></pre></td></tr></table></figure>

<p>如果spread operator右边的表达式可能为空，为了避免出现异常可以使用null-aware spread operator（<font color=green><em>…?</em></font>）。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list; <span class="keyword">var</span> list2 = [<span class="number">0</span>, ...?list]; <span class="keyword">assert</span>(list2.length == <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>关于spread operator细节用法，可参见<strong><a href="https://github.com/dart-lang/language/blob/master/accepted/2.3/spread-collections/feature-specification.md" target="_blank" rel="noopener">spread operator proposal</a></strong>。</p>
<p>另外Dart2.3引入了<strong>collection if</strong>和<strong>collection for</strong>语法，支持使用<font color=green><em>if</em></font>和<font color=green><em>for</em></font>生成collection。</p>
<p>下面是使用<strong>collection if</strong>创建一个包含3个或4个元素的list：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> nav = [  <span class="string">'Home'</span>,  <span class="string">'Furniture'</span>,  <span class="string">'Plants'</span>,  <span class="keyword">if</span> (promoActive) <span class="string">'Outlet'</span> ];</span><br></pre></td></tr></table></figure>

<p>下面这个例子是用<strong>collection for</strong>在将一个list的元素加入到另一个list前对每个元素循环处理：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> listOfInts = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">var</span> listOfStrings = [</span><br><span class="line">  <span class="string">'#0'</span>,</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i <span class="keyword">in</span> listOfInts) <span class="string">'#<span class="subst">$i</span>'</span></span><br><span class="line">];</span><br><span class="line"><span class="keyword">assert</span>(listOfStrings[<span class="number">1</span>] == <span class="string">'#1'</span>);</span><br></pre></td></tr></table></figure>

<p>collection if和collection for的更多细节用法，可参见<strong><a href="https://github.com/dart-lang/language/blob/master/accepted/2.3/control-flow-collections/feature-specification.md" target="_blank" rel="noopener">control flow collections proposal</a></strong>。</p>
<p>list类型提供了很多处理列表的简便方法，具体可参考<strong><a href="https://dart.dev/guides/language/language-tour#generics" target="_blank" rel="noopener">Generics</a></strong>和<strong><a href="https://dart.dev/guides/libraries/library-tour#collections" target="_blank" rel="noopener">Collections</a></strong>。</p>
<br>

<h4 id="Sets-集合"><a href="#Sets-集合" class="headerlink" title="Sets 集合"></a>Sets 集合</h4><p>Dart中set定义为</p>
<p>包含一组无重复元素的无序集合，Dart提供了set字面量以及<strong><a href="https://api.dart.dev/stable/dart-core/Set-class.html" target="_blank" rel="noopener">Set</a></strong>类型支持。</p>
<blockquote>
<p>注：虽然Set类型一直是Dart核心的一部分，但是直到Dart2.2才引入set字面量的支持。</p>
</blockquote>
<p>下面给了一个使用set字面量定义Dart set的简单例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> halogens = &#123;<span class="string">'fluorine'</span>, <span class="string">'chlorine'</span>, <span class="string">'bromine'</span>, <span class="string">'iodine'</span>, <span class="string">'astatine'</span>&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：其中<font color=green><em>halogens</em></font>推导类型是<font color=green><em>Set&lt;String&gt;</em></font>。如果在set中插入错误的类型数据，分析器或者运行时会抛出一个错误。详情可见<strong><a href="https://dart.dev/guides/language/sound-dart#type-inference" target="_blank" rel="noopener">type inference</a></strong>。</p>
</blockquote>
<p>使用前面带有类型参数的<font color=green><em>{}</em></font>可以创建一个空的set，另外对<font color=green><em>Set</em></font>类型的变量也可以用<font color=green><em>{}</em></font>直接赋值。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> names = &lt;<span class="built_in">String</span>&gt;&#123;&#125;;</span><br><span class="line"><span class="comment">// Set&lt;String&gt; names = &#123;&#125;; // This works, too.</span></span><br><span class="line"><span class="comment">// var names = &#123;&#125;; // Creates a map, not a set.</span></span><br></pre></td></tr></table></figure>

<p><strong>Set还是Map</strong>？map字面量语法和set字面量语法相似，<font color=green><em>{}</em></font>也可以作为<font color=green><em>Map</em></font>类型缺省值使用。如果忘了给<font color=green><em>{}</em></font>或者变量加上类型描述，Dart会为Map变量创建一个类型为<font color=green><em>Map&lt;dynamic, dynamic&gt;</em></font>的对象。</p>
<p>可以用<font color=green><em>add()</em></font>或者<font color=green><em>addAll()</em></font>方法为已存在的set变量添加元素：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> elements = &lt;<span class="built_in">String</span>&gt;&#123;&#125;;</span><br><span class="line">elements.add(<span class="string">'fluorine'</span>);</span><br><span class="line">elements.addAll(halogens);</span><br></pre></td></tr></table></figure>

<p>访问<font color=green><em>length</em></font>属性获取set包含的元素个数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> elements = &lt;<span class="built_in">String</span>&gt;&#123;&#125;;</span><br><span class="line">elements.add(<span class="string">'fluorine'</span>);</span><br><span class="line">elements.addAll(halogens);</span><br><span class="line"><span class="keyword">assert</span>(elements.length == <span class="number">5</span>);</span><br></pre></td></tr></table></figure>

<p>在set字面量前加上<font color=green><em>const</em></font>修饰，可以创建一个set编译时常量：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> constantSet = <span class="keyword">const</span> &#123;</span><br><span class="line">  <span class="string">'fluorine'</span>,</span><br><span class="line">  <span class="string">'chlorine'</span>,</span><br><span class="line">  <span class="string">'bromine'</span>,</span><br><span class="line">  <span class="string">'iodine'</span>,</span><br><span class="line">  <span class="string">'astatine'</span>,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// constantSet.add('helium'); // Uncommenting this causes an error.</span></span><br></pre></td></tr></table></figure>

<p>同样地，在Dart2.3中，set也支持spread operators（<font color=green><em>…</em></font>和<font color=green><em>…?</em></font>）以及collection ifs&amp;fors，更多细节可参考<strong><a href="https://dart.dev/guides/language/language-tour#spread-operator" target="_blank" rel="noopener">list spread operator</a></strong>和<strong><a href="https://dart.dev/guides/language/language-tour#collection-operators" target="_blank" rel="noopener">list collection operator</a></strong>的讨论。</p>
<p>关于set的其他细节可参考<strong><a href="https://dart.dev/guides/language/language-tour#generics" target="_blank" rel="noopener">Generics</a></strong>和<strong><a href="https://dart.dev/guides/libraries/library-tour#sets" target="_blank" rel="noopener">Sets</a></strong>。</p>
<br>

<h4 id="Maps-映射"><a href="#Maps-映射" class="headerlink" title="Maps 映射"></a>Maps 映射</h4><p>通常map定义为包含若干相关联的key和value的对象。这里的key和value可以为任意类型。在一个map对象中，每个<em>key<em>只能出现一次，而相同的</em>value<em>可以出现多次。Dart提供了map字面量和<font color=green></em>Map</em></font>类型的支持。</p>
<p>下面是一对简单的Dart map对象，使用map字面量初始化：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gifts = &#123;</span><br><span class="line">  <span class="comment">// Key:    Value</span></span><br><span class="line">  <span class="string">'first'</span>: <span class="string">'partridge'</span>,</span><br><span class="line">  <span class="string">'second'</span>: <span class="string">'turtledoves'</span>,</span><br><span class="line">  <span class="string">'fifth'</span>: <span class="string">'golden rings'</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> nobleGases = &#123;</span><br><span class="line">  <span class="number">2</span>: <span class="string">'helium'</span>,</span><br><span class="line">  <span class="number">10</span>: <span class="string">'neon'</span>,</span><br><span class="line">  <span class="number">18</span>: <span class="string">'argon'</span>,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：Dart推导出<font color=green><em>gifts</em></font>的类型为<font color=green><em>Map&lt;String, String&gt;</em></font>，<font color=green><em>nobleGases</em></font>的类型为<font color=green><em>Map&lt;int, String&gt;</em></font>。如果在map中插入错误类型的值，分析器和运行时会抛出一个错误。详情可见<strong><a href="https://dart.dev/guides/language/sound-dart#type-inference" target="_blank" rel="noopener">type inference</a></strong>。</p>
</blockquote>
<p>使用Map的构造函数可以为map变量创建拥有相同值的对象。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gifts = <span class="built_in">Map</span>();</span><br><span class="line">gifts[<span class="string">'first'</span>] = <span class="string">'partridge'</span>;</span><br><span class="line">gifts[<span class="string">'second'</span>] = <span class="string">'turtledoves'</span>;</span><br><span class="line">gifts[<span class="string">'fifth'</span>] = <span class="string">'golden rings'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> nobleGases = <span class="built_in">Map</span>();</span><br><span class="line">nobleGases[<span class="number">2</span>] = <span class="string">'helium'</span>;</span><br><span class="line">nobleGases[<span class="number">10</span>] = <span class="string">'neon'</span>;</span><br><span class="line">nobleGases[<span class="number">18</span>] = <span class="string">'argon'</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：有人可能习惯使用<font color=green><em>new Map()</em></font>而不是<font color=green><em>Map()</em></font>，而在Dart2.0中<font color=green><em>new</em></font>操作符是可选的，关于细节参见<strong><a href="https://dart.dev/guides/language/language-tour#using-constructors" target="_blank" rel="noopener">Using constructors</a></strong>。</p>
</blockquote>
<p>可以用类似Javascript的语法为一个已存在的map对象添加一组新的键值对：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gifts = &#123;<span class="string">'first'</span>: <span class="string">'partridge'</span>&#125;;</span><br><span class="line">gifts[<span class="string">'fourth'</span>] = <span class="string">'calling birds'</span>; <span class="comment">// Add a key-value pair</span></span><br></pre></td></tr></table></figure>

<p>同样地像js一样，从map对象中提取一个值：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gifts = &#123;<span class="string">'first'</span>: <span class="string">'partridge'</span>&#125;;</span><br><span class="line"><span class="keyword">assert</span>(gifts[<span class="string">'first'</span>] == <span class="string">'partridge'</span>);</span><br></pre></td></tr></table></figure>

<p>如果查询的key值在map中不存在，则会返回一个null：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gifts = &#123;<span class="string">'first'</span>: <span class="string">'partridge'</span>&#125;;</span><br><span class="line"><span class="keyword">assert</span>(gifts[<span class="string">'fifth'</span>] == <span class="keyword">null</span>);</span><br></pre></td></tr></table></figure>

<p>通过<font color=green><em>length</em></font>属性获取map中的键值对数量：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gifts = &#123;<span class="string">'first'</span>: <span class="string">'partridge'</span>&#125;;</span><br><span class="line">gifts[<span class="string">'fourth'</span>] = <span class="string">'calling birds'</span>;</span><br><span class="line"><span class="keyword">assert</span>(gifts.length == <span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<p>通过在map字面量前加上<font color=green><em>const</em></font>修饰，可以创建一个map编译时常量：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> constantMap = <span class="keyword">const</span> &#123;</span><br><span class="line">  <span class="number">2</span>: <span class="string">'helium'</span>,</span><br><span class="line">  <span class="number">10</span>: <span class="string">'neon'</span>,</span><br><span class="line">  <span class="number">18</span>: <span class="string">'argon'</span>,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// constantMap[2] = 'Helium'; // Uncommenting this causes an error.</span></span><br></pre></td></tr></table></figure>

<p>Dart2.3中，像list一样，map也支持spread operators（<font color=green><em>…</em></font> 和 <font color=green><em>…?</em></font>）以及collection if&amp;for。细节内容可以参考<strong><a href="https://github.com/dart-lang/language/blob/master/accepted/2.3/spread-collections/feature-specification.md" target="_blank" rel="noopener">spread operator proposal</a></strong>和<strong><a href="https://github.com/dart-lang/language/blob/master/accepted/2.3/control-flow-collections/feature-specification.md" target="_blank" rel="noopener">control flow collections proposal</a></strong>。</p>
<p>关于map的其他信息参见<strong><a href="https://dart.dev/guides/language/language-tour#generics" target="_blank" rel="noopener">Generics</a></strong>和<strong><a href="https://dart.dev/guides/libraries/library-tour#maps" target="_blank" rel="noopener">Maps</a></strong>。</p>
<br>

<h4 id="Runes和grapheme-clusters"><a href="#Runes和grapheme-clusters" class="headerlink" title="Runes和grapheme clusters"></a>Runes和grapheme clusters</h4><p>Dart中可以通过<strong><a href="https://api.dart.dev/stable/dart-core/Runes-class.html" target="_blank" rel="noopener">runes</a></strong>显示一个字串的unicode code point。Dart2.6中，可以使用<strong><a href="https://pub.dev/packages/characters" target="_blank" rel="noopener">characters package</a></strong>去查看或者处理<em>user-perceived characters</em>，其又被称作<strong><a href="https://unicode.org/reports/tr29/#Grapheme_Cluster_Boundaries" target="_blank" rel="noopener">Unicode (extended) grapheme clusters</a></strong>。</p>
<p>Unicode为每个字母、每个数字和每个在世界书写体系中用到的字符都定义了唯一的数值，每个Dart字符串都是一个utf-16编码序列，而使用一个字串来表示unicode code point需要特殊的语法。通常的unicode code point表示法是<font color=green><em>\uXXXX</em></font>，其中XXXX是个4位的16进制数。例如桃心字符(♥)的unicode code point是<font color=green><em>\u2665</em></font>。如果要表示多于4位或少于4位的16进制数，则需要用大括号把值括起来，比如笑脸emoji是<font color=green><em>\u{1f600}</em></font>。</p>
<p>如果要读写特殊的unicode字符，要使用characters package中定义的<font color=green><em>characters</em></font>属性，它会返回一个<font color=green><em>Characters</em></font>对象，一组grapheme clusters定义的字串。下面是使用characters API的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'package:characters/characters.dart'</span>;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">var</span> hi = <span class="string">'Hi 🇩🇰'</span>;</span><br><span class="line"><span class="built_in">print</span>(hi);</span><br><span class="line"><span class="built_in">print</span>(<span class="string">'The end of the string: <span class="subst">$&#123;hi.substring(hi.length - <span class="number">1</span>)&#125;</span>'</span>);</span><br><span class="line"><span class="built_in">print</span>(<span class="string">'The last character: <span class="subst">$&#123;hi.characters.last&#125;</span>\n'</span>);</span><br></pre></td></tr></table></figure>

<p>具体的执行结果依赖于运行环境，类似于以下输出：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> dart bin/main.dart</span></span><br><span class="line">Hi 🇩🇰</span><br><span class="line">The end of the string: ???</span><br><span class="line">The last character: 🇩🇰</span><br></pre></td></tr></table></figure>

<p>关于使用characters package处理字符串的细节说明，请参考<strong><a href="https://pub.dev/packages/characters#-example-tab-" target="_blank" rel="noopener">example</a></strong>和characters pacakage的<strong><a href="https://pub.dev/documentation/characters" target="_blank" rel="noopener">API reference</a></strong>。</p>
<br>

<h4 id="Symbols"><a href="#Symbols" class="headerlink" title="Symbols"></a>Symbols</h4><p><strong><a href="https://api.dart.dev/stable/dart-core/Symbol-class.html" target="_blank" rel="noopener">Symbol</a></strong>对象在Dart语言里体现为声明的操作符或者标识符。虽然有可能一直不会被用到，但对于按名称引用标识符的API来说，它是非常有价值的，因为minification会更改标识符名称，但不会更改标识符Symbol。如果要获取一个标识符的Symbol，可以使用Symbol字面量，只需要在标识符前加上’#’：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">#radix</span><br><span class="line">#bar</span><br></pre></td></tr></table></figure>

<p>Symbol字面量是编译时常量。</p>
<br>

<h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><p>Dart是实实在在的面向对象语言，即使函数也是对象，类型为<strong><a href="https://api.dart.dev/stable/dart-core/Function-class.html" target="_blank" rel="noopener">Function</a></strong>。这就意味着函数可以赋值给变量，也可以作为参数传给其他函数使用。你还可以像调用函数一样调用一个Dart类的实例。具体内容参见<strong><a href="https://dart.dev/guides/language/language-tour#callable-classes" target="_blank" rel="noopener">Callable classes</a></strong>。</p>
<p>下面是一个函数实现的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">bool</span> isNoble(<span class="built_in">int</span> atomicNumber) &#123;</span><br><span class="line">  <span class="keyword">return</span> _nobleGases[atomicNumber] != <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>虽然《Effective Dart》建议遵照 <strong><a href="https://dart.dev/guides/language/effective-dart/design#prefer-type-annotating-public-fields-and-top-level-variables-if-the-type-isnt-obvious" target="_blank" rel="noopener">type annotations for public APIs</a></strong> 规范来实现，但把所有类型说明略去也不会影响函数的运行。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">isNoble(atomicNumber) &#123;</span><br><span class="line">  <span class="keyword">return</span> _nobleGases[atomicNumber] != <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果函数体只包含了一个表达式，那么语法上可以略写为以下形式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">bool</span> isNoble(<span class="built_in">int</span> atomicNumber) =&gt; _nobleGases[atomicNumber] != <span class="keyword">null</span>;</span><br></pre></td></tr></table></figure>

<p><font color=green><em>=&gt; expr</em></font>是针对<font color=green><em>{ return expr; }</em></font>的一种语法缩写。<font color=green><em>=&gt;</em></font>表示法有时也被称为箭头语法。</p>
<blockquote>
<p>注：在箭头（=&gt;）和分号（;）之间的只能是表达式（expression），不能是语句（statement）。比如这里不能用if-else语句（<strong><a href="https://dart.dev/guides/language/language-tour#if-and-else" target="_blank" rel="noopener">if statement</a></strong>），但可以使用条件表达式（<strong><a href="https://dart.dev/guides/language/language-tour#conditional-expressions" target="_blank" rel="noopener">conditional expression</a></strong>）。</p>
</blockquote>
<p>函数的参数可以分为两类：必选参数（<em>required</em>）和可选参数（<em>optional</em>）。参数表中必选参数排在前面，可选参数列在后面。可选参数可以以命名（<em>named</em>）或者位置（<em>positional</em>）的方式来实现。</p>
<blockquote>
<p>注：某些API——典型地是Flutter中widget的构造函数——只使用命名参数的方式，甚至对于那些强制提供的参数也是如此，详情可见下一章节。</p>
</blockquote>
<br>

<h4 id="Optional-parameters-可选参数"><a href="#Optional-parameters-可选参数" class="headerlink" title="Optional parameters 可选参数"></a>Optional parameters 可选参数</h4><p>可选参数可以以命名参数（<em>named</em>）或者位置参数（<em>positional</em>）的方式来实现，但不能同时使用这两种方式。</p>
<br>

<h5 id="Named-parameters-命名参数"><a href="#Named-parameters-命名参数" class="headerlink" title="Named parameters 命名参数"></a>Named parameters 命名参数</h5><p>在调用函数时，可以使用<font color=green><em>paramName: value</em></font>的方式来指定命名参数。比如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">enableFlags(bold: <span class="keyword">true</span>, hidden: <span class="keyword">false</span>);</span><br></pre></td></tr></table></figure>

<p>在定义函数时，使用<font color=green><em>{param1, param2, …}</em></font>的方式指定命名参数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// <span class="markdown">Sets the [bold] and [hidden] flags ...</span></span></span><br><span class="line"><span class="keyword">void</span> enableFlags(&#123;<span class="built_in">bool</span> bold, <span class="built_in">bool</span> hidden&#125;) &#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>虽然命名参数是可选的，但可以通过给参数添加<strong><a href="https://pub.dev/documentation/meta/latest/meta/required-constant.html" target="_blank" rel="noopener">@required</a></strong>注解表明它是强制类型的，要求调用方必须给这个参数提供值，比如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Scrollbar(&#123;Key key, <span class="meta">@required</span> Widget child&#125;)</span><br></pre></td></tr></table></figure>

<p>如果有人尝试在调用这个函数创建<font color=green><strong>Scrollbar</strong></font>时不给<font color=green><strong>child</strong></font>传参，分析器会报错。</p>
<p><strong><a href="https://pub.dev/documentation/meta/latest/meta/required-constant.html" target="_blank" rel="noopener">@required</a></strong>注解依赖于<strong><a href="https://pub.dev/packages/meta" target="_blank" rel="noopener">meta</a></strong>包，需要 <font color=green><strong>import pacakge:meta/meta.dart</strong></font>。</p>
<br>

<h5 id="Positional-parameters-位置参数"><a href="#Positional-parameters-位置参数" class="headerlink" title="Positional parameters 位置参数"></a>Positional parameters 位置参数</h5><p>可以使用 <font color=green><strong>[ ]</strong></font> 将一组函数参数括在一起，以表明它们是可选的位置参数。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span> say(<span class="built_in">String</span> from, <span class="built_in">String</span> msg, [<span class="built_in">String</span> device]) &#123;</span><br><span class="line">  <span class="keyword">var</span> result = <span class="string">'<span class="subst">$from</span> says <span class="subst">$msg</span>'</span>;</span><br><span class="line">  <span class="keyword">if</span> (device != <span class="keyword">null</span>) &#123;</span><br><span class="line">    result = <span class="string">'<span class="subst">$result</span> with a <span class="subst">$device</span>'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面这个例子显示地是没有带可选参数的调用方式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>(say(<span class="string">'Bob'</span>, <span class="string">'Howdy'</span>) == <span class="string">'Bob says Howdy'</span>);</span><br></pre></td></tr></table></figure>

<p>这个是传入可选参数的调用方式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>(say(<span class="string">'Bob'</span>, <span class="string">'Howdy'</span>, <span class="string">'smoke signal'</span>) ==</span><br><span class="line">    <span class="string">'Bob says Howdy with a smoke signal'</span>);</span><br></pre></td></tr></table></figure>

<br>

<h4 id="缺省参数值"><a href="#缺省参数值" class="headerlink" title="缺省参数值"></a>缺省参数值</h4><p>在函数定义里，可以使用 <font color=green><strong>=</strong></font> 为可选参数或者必选参数赋缺省值。缺省值必须是编译时常量。如果没有赋值，则缺省值默认为null。</p>
<p>下面是一个给命名参数赋缺省值的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// <span class="markdown">Sets the [bold] and [hidden] flags ...</span></span></span><br><span class="line"><span class="keyword">void</span> enableFlags(&#123;<span class="built_in">bool</span> bold = <span class="keyword">false</span>, <span class="built_in">bool</span> hidden = <span class="keyword">false</span>&#125;) &#123;...&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// bold will be true; hidden will be false.</span></span><br><span class="line">enableFlags(bold: <span class="keyword">true</span>);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：老版本代码可能会用 : 而不是 = 为命名参数赋值，是因为之前只支持使用 : 来赋值，最新版本这个支持已被废弃，建议<strong><a href="https://dart.dev/guides/language/effective-dart/usage#do-use--to-separate-a-named-parameter-from-its-default-value" target="_blank" rel="noopener">统一使用 = 来指定缺省值</a></strong>。</p>
</blockquote>
<p>下面这个例子展示了如何为位置参数赋缺省值：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span> say(<span class="built_in">String</span> from, <span class="built_in">String</span> msg,</span><br><span class="line">    [<span class="built_in">String</span> device = <span class="string">'carrier pigeon'</span>, <span class="built_in">String</span> mood]) &#123;</span><br><span class="line">  <span class="keyword">var</span> result = <span class="string">'<span class="subst">$from</span> says <span class="subst">$msg</span>'</span>;</span><br><span class="line">  <span class="keyword">if</span> (device != <span class="keyword">null</span>) &#123;</span><br><span class="line">    result = <span class="string">'<span class="subst">$result</span> with a <span class="subst">$device</span>'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (mood != <span class="keyword">null</span>) &#123;</span><br><span class="line">    result = <span class="string">'<span class="subst">$result</span> (in a <span class="subst">$mood</span> mood)'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>(say(<span class="string">'Bob'</span>, <span class="string">'Howdy'</span>) ==</span><br><span class="line">    <span class="string">'Bob says Howdy with a carrier pigeon'</span>);</span><br></pre></td></tr></table></figure>

<p>还可以传入list或者map做为缺省值。在下面这个例子里，函数 <font color=green><em>doStuff()</em></font> 为<font color=green><em>list</em></font>参数指定了一个缺省的list对象，为<font color=green><em>gifts</em></font>参数指定了一个缺省的map对象。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> doStuff(</span><br><span class="line">    &#123;<span class="built_in">List</span>&lt;<span class="built_in">int</span>&gt; list = <span class="keyword">const</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>],</span><br><span class="line">    <span class="built_in">Map</span>&lt;<span class="built_in">String</span>, <span class="built_in">String</span>&gt; gifts = <span class="keyword">const</span> &#123;</span><br><span class="line">      <span class="string">'first'</span>: <span class="string">'paper'</span>,</span><br><span class="line">      <span class="string">'second'</span>: <span class="string">'cotton'</span>,</span><br><span class="line">      <span class="string">'third'</span>: <span class="string">'leather'</span></span><br><span class="line">    &#125;&#125;) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'list:  <span class="subst">$list</span>'</span>);</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'gifts: <span class="subst">$gifts</span>'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="main主函数"><a href="#main主函数" class="headerlink" title="main主函数"></a>main主函数</h4><p>每个app必须提供一个全局的 <font color=green><em>main()</em></font> 函数做为这个app的入口。<font color=green><em>main</em></font>主函数返回<font color=green><em>void</em></font>值，参数表为<font color=green><em>List<String></em></font>类型的可选参数。</p>
<p>下面是一个web app的主函数例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="built_in">querySelector</span>(<span class="string">'#sample_text_id'</span>)</span><br><span class="line">    ..text = <span class="string">'Click me!'</span></span><br><span class="line">    ..onClick.listen(reverseText);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：以上代码中用到的 <font color=green><em>..</em></font> 语法叫做<strong><a href="https://dart.dev/guides/language/language-tour#cascade-notation-" target="_blank" rel="noopener">cascade</a></strong>，使用cascade可以针对单一对象的成员做多重操作。</p>
</blockquote>
<p>下面是一个命令行app的主函数例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Run the app like this: dart args.dart 1 test</span></span><br><span class="line"><span class="keyword">void</span> main(<span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt; arguments) &#123;</span><br><span class="line">  <span class="built_in">print</span>(arguments);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">assert</span>(arguments.length == <span class="number">2</span>);</span><br><span class="line">  <span class="keyword">assert</span>(<span class="built_in">int</span>.parse(arguments[<span class="number">0</span>]) == <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">assert</span>(arguments[<span class="number">1</span>] == <span class="string">'test'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以使用<strong><a href="https://pub.dev/packages/args" target="_blank" rel="noopener">args library</a></strong>来定义和解析命令行参数。</p>
<br>

<h4 id="做为一级对象的函数"><a href="#做为一级对象的函数" class="headerlink" title="做为一级对象的函数"></a>做为一级对象的函数</h4><p>可以将一个函数做为参数传给另一个函数。例如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> printElement(<span class="built_in">int</span> element) &#123;</span><br><span class="line">  <span class="built_in">print</span>(element);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> list = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// Pass printElement as a parameter.</span></span><br><span class="line">list.forEach(printElement);</span><br></pre></td></tr></table></figure>

<p>还可以用函数给变量赋值，比如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> loudify = (msg) =&gt; <span class="string">'!!! <span class="subst">$&#123;msg.toUpperCase()&#125;</span> !!!'</span>;</span><br><span class="line"><span class="keyword">assert</span>(loudify(<span class="string">'hello'</span>) == <span class="string">'!!! HELLO !!!'</span>);</span><br></pre></td></tr></table></figure>

<p>这个例子使用了匿名函数，更多内容会在下个部分介绍。</p>
<br>

<h4 id="Anonymous-functions-匿名函数"><a href="#Anonymous-functions-匿名函数" class="headerlink" title="Anonymous functions 匿名函数"></a>Anonymous functions 匿名函数</h4><p>像 <font color=green><em>main()</em></font> 或者 <font color=green><em>printElement()</em></font> 一样，大多数函数都会在定义时命名。在Dart中还可以定义一种没有名字的函数叫<em>匿名函数</em>，有时也叫<em>lambda</em>函数或闭包（<em>closure</em>）。可以把匿名函数赋值给一个变量进行其他操作，比如可以在collection中添加或者删除一个函数对象。</p>
<p>匿名函数看起来和命名函数很像，括号间包含0个或多个参数，通过逗号或者可选类型注释分隔开。</p>
<p>内部的代码块包含函数体：</p>
<p><font color=green>( [ [ Type ] param1 [ , … ] ] ) {<br>codeBlock;<br>};</font></p>
<p>下面这个例子定义了一个带有一个无类型参数<font color=green><em>item</em></font>的匿名函数。这个函数遍历列表中的每一项，打印出该项的值以及它在列表中相应的下标值。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> list = [<span class="string">'apples'</span>, <span class="string">'bananas'</span>, <span class="string">'oranges'</span>];</span><br><span class="line">list.forEach((item) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'<span class="subst">$&#123;list.indexOf(item)&#125;</span>: <span class="subst">$item</span>'</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>如果函数只包含一个语句，可以使用箭头语法进行缩写。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">list.forEach(</span><br><span class="line">    (item) =&gt; <span class="built_in">print</span>(<span class="string">'<span class="subst">$&#123;list.indexOf(item)&#125;</span>: <span class="subst">$item</span>'</span>));</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Lexical-scope-词法作用域"><a href="#Lexical-scope-词法作用域" class="headerlink" title="Lexical scope 词法作用域"></a>Lexical scope 词法作用域</h4><p>Dart是一种支持词法作用域的语言，这就意味着变量的作用域是由代码布局静态决定的。你可以试试“调试跟踪一个变量到花括号外”看一下变量是否还在作用域内。</p>
<p>下面给出了一个嵌套函数的例子，每个作用域层级都有变量。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">bool</span> topLevel = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">var</span> insideMain = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">void</span> myFunction() &#123;</span><br><span class="line">    <span class="keyword">var</span> insideFunction = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span> nestedFunction() &#123;</span><br><span class="line">      <span class="keyword">var</span> insideNestedFunction = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">assert</span>(topLevel);</span><br><span class="line">      <span class="keyword">assert</span>(insideMain);</span><br><span class="line">      <span class="keyword">assert</span>(insideFunction);</span><br><span class="line">      <span class="keyword">assert</span>(insideNestedFunction);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意<font color=green><em>nestedFunction()</em></font>是怎么引用到每个层级的变量，直到最外层。</p>
<br>

<h4 id="Lexical-closure-词法闭包"><a href="#Lexical-closure-词法闭包" class="headerlink" title="Lexical closure 词法闭包"></a>Lexical closure 词法闭包</h4><p><em>闭包</em>是一个函数对象，它可以访问词法作用域内的变量，即使该函数在其原始作用域之外被调用时也是如此。</p>
<p>函数可以隐藏绑定在周边作用域中定义的变量。下面这个例子中，<font color=green><em>makeAddr()</em> </font>绑定了变量 <font color=green><em>addBy</em></font>。而无论函数返回的位置在哪里，它都会记住<font color=green><em>addBy</em></font>。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// <span class="markdown">Returns a function that adds [addBy] to the </span></span></span><br><span class="line"><span class="comment">/// <span class="markdown">function's argument. </span></span></span><br><span class="line"><span class="built_in">Function</span> makeAdder(<span class="built_in">num</span> addBy) &#123;  </span><br><span class="line">  <span class="keyword">return</span> (<span class="built_in">num</span> i) =&gt; addBy + i; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;  </span><br><span class="line">  <span class="comment">// Create a function that adds 2.  </span></span><br><span class="line">  <span class="keyword">var</span> add2 = makeAdder(<span class="number">2</span>);   </span><br><span class="line">  </span><br><span class="line">  <span class="comment">// Create a function that adds 4.  </span></span><br><span class="line">  <span class="keyword">var</span> add4 = makeAdder(<span class="number">4</span>);   </span><br><span class="line">  </span><br><span class="line">  <span class="keyword">assert</span>(add2(<span class="number">3</span>) == <span class="number">5</span>);  </span><br><span class="line">  <span class="keyword">assert</span>(add4(<span class="number">3</span>) == <span class="number">7</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="检验函数是否相等"><a href="#检验函数是否相等" class="headerlink" title="检验函数是否相等"></a>检验函数是否相等</h4><p>下面这个例子检验了全局函数、静态方法、实例方法的相等性。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> foo() &#123;&#125; <span class="comment">// A top-level function </span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;  </span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">void</span> bar() &#123;&#125; <span class="comment">// A static method  </span></span><br><span class="line">  <span class="keyword">void</span> baz() &#123;&#125; <span class="comment">// An instance method </span></span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;  </span><br><span class="line">  <span class="keyword">var</span> x;   </span><br><span class="line">  </span><br><span class="line">  <span class="comment">// Comparing top-level functions.  </span></span><br><span class="line">  x = foo;  </span><br><span class="line">  <span class="keyword">assert</span>(foo == x);   </span><br><span class="line">  </span><br><span class="line">  <span class="comment">// Comparing static methods.  </span></span><br><span class="line">  x = A.bar;  </span><br><span class="line">  <span class="keyword">assert</span>(A.bar == x);   </span><br><span class="line">  </span><br><span class="line">  <span class="comment">// Comparing instance methods.  </span></span><br><span class="line">  <span class="keyword">var</span> v = A(); <span class="comment">// Instance #1 of A  </span></span><br><span class="line">  <span class="keyword">var</span> w = A(); <span class="comment">// Instance #2 of A  </span></span><br><span class="line">  <span class="keyword">var</span> y = w;  </span><br><span class="line">  x = w.baz;   </span><br><span class="line">  </span><br><span class="line">  <span class="comment">// These closures refer to the same instance (#2),  </span></span><br><span class="line">  <span class="comment">// so they're equal.  </span></span><br><span class="line">  <span class="keyword">assert</span>(y.baz == x);   </span><br><span class="line">  </span><br><span class="line">  <span class="comment">// These closures refer to different instances,  </span></span><br><span class="line">  <span class="comment">// so they're unequal.  </span></span><br><span class="line">  <span class="keyword">assert</span>(v.baz != w.baz); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Return-value-返回值"><a href="#Return-value-返回值" class="headerlink" title="Return value 返回值"></a>Return value 返回值</h4><p>所有函数都有返回值，如果返回值没有具体指定，则 <font color=green><em>return null;</em></font> 语句会被隐式地加到函数体最后。（返回null对象）</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">foo() &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>(foo() == <span class="keyword">null</span>);</span><br></pre></td></tr></table></figure>

<br>

<h3 id="Operators-运算符"><a href="#Operators-运算符" class="headerlink" title="Operators 运算符"></a>Operators 运算符</h3><p>下表列出了Dart定义的运算符，其中很多都可以被覆写。具体可参考<strong><a href="https://dart.dev/guides/language/language-tour#overridable-operators" target="_blank" rel="noopener">Overridable operators</a></strong>。</p>
<table>
<thead>
<tr>
<th><strong>Description</strong></th>
<th><strong>Operator</strong></th>
</tr>
</thead>
<tbody><tr>
<td>unary postfix</td>
<td><font color=green><strong>expr++ &ensp;&ensp; expr– &ensp;&ensp; ( ) &ensp;&ensp; [ ] &ensp;&ensp; . &ensp;&ensp; ?.</strong></font></td>
</tr>
<tr>
<td>unary prefix</td>
<td><font color=green><strong>-expr &ensp;&ensp; !expr &ensp;&ensp; ~expr &ensp;&ensp; ++expr &ensp;&ensp; –expr &ensp;&ensp; await expr</strong></font></td>
</tr>
<tr>
<td>multiplicative</td>
<td><font color=green><strong>* &ensp;&ensp; / &ensp;&ensp; % &ensp;&ensp; ~/</strong></font></td>
</tr>
<tr>
<td>additive</td>
<td><font color=green>*<em>+ &ensp;&ensp; - *</em></font></td>
</tr>
<tr>
<td>shift</td>
<td><font color=green><strong>&lt;&lt; &ensp;&ensp; &gt;&gt; &ensp;&ensp;  &gt;&gt;&gt;</strong></font></td>
</tr>
<tr>
<td>bitwise AND</td>
<td><font color=green><strong>&amp;</strong></font></td>
</tr>
<tr>
<td>bitwise XOR</td>
<td><font color=green><strong>^</strong></font></td>
</tr>
<tr>
<td>bitwise OR</td>
<td><font color=green><strong>|</strong></font></td>
</tr>
<tr>
<td>relational and type test</td>
<td><font color=green><strong>&gt;= &ensp;&ensp; &gt; &ensp;&ensp; &lt;= &ensp;&ensp; &lt; &ensp;&ensp; as &ensp;&ensp; is &ensp;&ensp; is!</strong></font></td>
</tr>
<tr>
<td>equality</td>
<td><font color=green><strong>== &ensp;&ensp; !=</strong></font></td>
</tr>
<tr>
<td>logical AND</td>
<td><font color=green><strong>&amp;&amp;</strong></font></td>
</tr>
<tr>
<td>logical OR</td>
<td><font color=green><strong>||</strong></font></td>
</tr>
<tr>
<td>if null</td>
<td><font color=green><strong>??</strong></font></td>
</tr>
<tr>
<td>conditional</td>
<td><font color=green><strong>expr1 ? expr2 : expr3</strong></font></td>
</tr>
<tr>
<td>cascade</td>
<td><font color=green><strong>..</strong></font></td>
</tr>
<tr>
<td>assignment</td>
<td><font color=green><strong>= &ensp;&ensp; *= &ensp;&ensp; /= &ensp;&ensp; += &ensp;&ensp; -= &ensp;&ensp; &amp;= &ensp;&ensp; ^=</strong></font>  etc</td>
</tr>
</tbody></table>
<blockquote>
<p>警告：这里的运算符优先级顺序是针对Dart解析器行为的近似，至于准确解释，请参考<strong><a href="https://dart.dev/guides/language/spec" target="_blank" rel="noopener">Dart language specification</a></strong>中的语法定义。</p>
</blockquote>
<p>可以使用运算符构建表达式，下面是一些运算符表达式的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">a++</span><br><span class="line">a + b</span><br><span class="line">a = b</span><br><span class="line">a == b</span><br><span class="line">c ? a : b</span><br><span class="line">a <span class="keyword">is</span> T</span><br></pre></td></tr></table></figure>

<p>在<strong><a href="https://dart.dev/guides/language/language-tour#operators" target="_blank" rel="noopener">运算符表</a></strong>中，每行的运算符优先级都高于后续行中运算符的优先级。比如multicative运算符<font color=green><strong>%</strong></font>比equality运算符<font color=green><strong>==</strong></font>的优先级要高，而<font color=green><strong>==</strong></font>的优先级要高于logical AND运算符 <font color=green><strong>&amp;&amp;</strong></font>。这种优先级定义意味着下面两行代码的执行过程是相同的：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Parentheses improve readability. </span></span><br><span class="line"><span class="keyword">if</span> ((n % i == <span class="number">0</span>) &amp;&amp; (d % i == <span class="number">0</span>)) ... </span><br><span class="line">  </span><br><span class="line"><span class="comment">// Harder to read, but equivalent. </span></span><br><span class="line"><span class="keyword">if</span> (n % i == <span class="number">0</span> &amp;&amp; d % i == <span class="number">0</span>) ...</span><br></pre></td></tr></table></figure>

<blockquote>
<p>警告：对于二元运算符来讲，左操作数决定了这次运算使用了哪个版本的实现。例如，一个Vector对象和一个Point对象相加，aVector+aPoint意味着是使用了Vector对+运算符的覆写实现。</p>
</blockquote>
<br>

<h4 id="Arithmetic-operators-算术运算符"><a href="#Arithmetic-operators-算术运算符" class="headerlink" title="Arithmetic operators 算术运算符"></a>Arithmetic operators 算术运算符</h4><p>Dart支持常见的算术运算符，如下表所列：</p>
<table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><strong>+</strong></font></td>
<td>Add 加</td>
</tr>
<tr>
<td><font color=green><strong>-</strong></font></td>
<td>Subtract 减</td>
</tr>
<tr>
<td><font color=green><strong>-expr</strong></font></td>
<td>Unary minus 一元负号, 求负值（表达式反号）</td>
</tr>
<tr>
<td><font color=green><strong>*</strong></font></td>
<td>Multiply 乘</td>
</tr>
<tr>
<td><font color=green><strong>/</strong></font></td>
<td>Divide 除</td>
</tr>
<tr>
<td><font color=green><strong>~/</strong></font></td>
<td>Divide 整除，返回一个整数结果</td>
</tr>
<tr>
<td><font color=green><strong>%</strong></font></td>
<td>取模求余</td>
</tr>
</tbody></table>
<p>例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> + <span class="number">3</span> == <span class="number">5</span>); </span><br><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> - <span class="number">3</span> == <span class="number">-1</span>); </span><br><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> * <span class="number">3</span> == <span class="number">6</span>); </span><br><span class="line"><span class="keyword">assert</span>(<span class="number">5</span> / <span class="number">2</span> == <span class="number">2.5</span>); <span class="comment">// Result is a double </span></span><br><span class="line"><span class="keyword">assert</span>(<span class="number">5</span> ~/ <span class="number">2</span> == <span class="number">2</span>); <span class="comment">// Result is an int </span></span><br><span class="line"><span class="keyword">assert</span>(<span class="number">5</span> % <span class="number">2</span> == <span class="number">1</span>); <span class="comment">// Remainder </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>(<span class="string">'5/2 = <span class="subst">$&#123;<span class="number">5</span> ~/ <span class="number">2</span>&#125;</span> r <span class="subst">$&#123;<span class="number">5</span> % <span class="number">2</span>&#125;</span>'</span> == <span class="string">'5/2 = 2 r 1'</span>);</span><br></pre></td></tr></table></figure>

<p>同时Dart还支持前缀、后缀的自增和自减运算符。</p>
<table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><strong>++var</strong></font></td>
<td>var = var + 1 (表达式值为 var + 1）</td>
</tr>
<tr>
<td><font color=green><strong>var++</strong></font></td>
<td>var = var + 1 (表达式值为 var）</td>
</tr>
<tr>
<td><font color=green><strong>–var</strong></font></td>
<td>var = var - 1 (表达式值为 var - 1）</td>
</tr>
<tr>
<td><font color=green><strong>var–</strong></font></td>
<td>var = var - 1 (表达式值为 var）</td>
</tr>
</tbody></table>
<p>例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a, b;</span><br><span class="line"></span><br><span class="line">a = <span class="number">0</span>;</span><br><span class="line">b = ++a; <span class="comment">// Increment a before b gets its value.</span></span><br><span class="line"><span class="keyword">assert</span>(a == b); <span class="comment">// 1 == 1</span></span><br><span class="line"></span><br><span class="line">a = <span class="number">0</span>;</span><br><span class="line">b = a++; <span class="comment">// Increment a AFTER b gets its value.</span></span><br><span class="line"><span class="keyword">assert</span>(a != b); <span class="comment">// 1 != 0</span></span><br><span class="line"></span><br><span class="line">a = <span class="number">0</span>;</span><br><span class="line">b = --a; <span class="comment">// Decrement a before b gets its value.</span></span><br><span class="line"><span class="keyword">assert</span>(a == b); <span class="comment">// -1 == -1</span></span><br><span class="line"></span><br><span class="line">a = <span class="number">0</span>;</span><br><span class="line">b = a--; <span class="comment">// Decrement a AFTER b gets its value.</span></span><br><span class="line"><span class="keyword">assert</span>(a != b); <span class="comment">// -1 != 0</span></span><br></pre></td></tr></table></figure>

<br>

<h4 id="Equality-and-relational-operators-关系运算符"><a href="#Equality-and-relational-operators-关系运算符" class="headerlink" title="Equality and relational operators 关系运算符"></a>Equality and relational operators 关系运算符</h4><p>下表列出了关系运算符的定义：</p>
<table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><strong>==</strong></font></td>
<td>相等</td>
</tr>
<tr>
<td><font color=green><strong>!=</strong></font></td>
<td>不相等</td>
</tr>
<tr>
<td><font color=green><strong>&gt;</strong></font></td>
<td>大于</td>
</tr>
<tr>
<td><font color=green><strong>&lt;</strong></font></td>
<td>小于</td>
</tr>
<tr>
<td><font color=green><strong>&gt;=</strong></font></td>
<td>大于等于</td>
</tr>
<tr>
<td><font color=green><strong>&lt;=</strong></font></td>
<td>小于等于</td>
</tr>
</tbody></table>
<p>如果要判断两个对象x和y是否为同一值，使用 <font color=green><strong>==</strong></font> 运算符。（在极少数情况下，需要知道两个对象是否为同一对象，则需要调用<font color=green><strong>identical()</strong></font>函数来判断。）下面是<font color=green><strong>==</strong></font>的判断规则：</p>
<ol>
<li><p>如果x和y均为null则返回true，如果只有一个为null则返回false；</p>
</li>
<li><p>调用<font color=green><strong>x.==(y)</strong></font>方法返回结果。（很多运算符都可以被覆写包括<font color=green><strong>==</strong></font>，而判断时调用的方法则取决于左操作数的实现，具体参考<strong><a href="https://dart.dev/guides/language/language-tour#overridable-operators" target="_blank" rel="noopener">Overridable operators</a></strong>）</p>
</li>
</ol>
<p>下面是每个关系运算符的使用范例：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> == <span class="number">2</span>);</span><br><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> != <span class="number">3</span>);</span><br><span class="line"><span class="keyword">assert</span>(<span class="number">3</span> &gt; <span class="number">2</span>);</span><br><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> &lt; <span class="number">3</span>);</span><br><span class="line"><span class="keyword">assert</span>(<span class="number">3</span> &gt;= <span class="number">3</span>);</span><br><span class="line"><span class="keyword">assert</span>(<span class="number">2</span> &lt;= <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Type-test-operator-类型检测运算符"><a href="#Type-test-operator-类型检测运算符" class="headerlink" title="Type test operator 类型检测运算符"></a>Type test operator 类型检测运算符</h4><p><font color=green><strong>as</strong></font>、<font color=green><strong>is</strong></font>和<font color=green><strong>is !</strong></font>是用来便于在运行时进行类型检测的运算符。</p>
<table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><strong>as</strong></font></td>
<td>类型转换，也用来指定<strong><a href="https://dart.dev/guides/language/language-tour#specifying-a-library-prefix" target="_blank" rel="noopener">库前缀</a></strong></td>
</tr>
<tr>
<td><font color=green><strong>is</strong></font></td>
<td>如果对象为指定的类型则为真</td>
</tr>
<tr>
<td><font color=green><strong>is !</strong></font></td>
<td>如果对象为指定的类型则为假</td>
</tr>
</tbody></table>
<p>如果对象 <font color=green><em>obj</em></font>实现了类型<font color=green><em>T</em></font>指定的接口，则<font color=green><em>obj is T</em></font>的结果为真。比如，<font color=green><em>obj is Object</em></font>总是为真。</p>
<p>使用<font color=green><em>as</em></font>运算符可以将对象转换成期望类型，一般在调用某个对象的表达式前都会调用<font color=green><em>is</em></font>针对该对象做类型检测，使用<font color=green><em>as</em></font>运算符可以提供一个缩写方式。如下面的代码所示：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (emp <span class="keyword">is</span> Person) &#123;</span><br><span class="line">  <span class="comment">// Type check</span></span><br><span class="line">  emp.firstName = <span class="string">'Bob'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//shorthand using as operator</span></span><br><span class="line">(emp <span class="keyword">as</span> Person).firstName = <span class="string">'Bob'</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：以上代码并不完全等价，如果<font color=green><em>emp</em></font>为空或者不是Person对象，使用<font color=green><em>is</em></font>的代码块会什么也不做，而后面那个语句会抛出异常。</p>
</blockquote>
<br>

<h4 id="Assignment-operators"><a href="#Assignment-operators" class="headerlink" title="Assignment operators"></a>Assignment operators</h4><p>使用<font color=green><em>=</em></font>运算符可以给变量赋值。仅当被赋值的变量可能为空时，才使用<font color=green><em>??=</em></font>运算符。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Assign value to a </span></span><br><span class="line">a = value; </span><br><span class="line"></span><br><span class="line"><span class="comment">// Assign value to b if b is null; otherwise, b stays the same </span></span><br><span class="line">b ??= value;</span><br></pre></td></tr></table></figure>

<p>复合赋值运算符，比如<font color=green><em>+=</em></font>，把其他计算操作和赋值操作绑定在了一起。</p>
<table>
<thead>
<tr>
<th><font color=green><em>=</em></font></th>
<th><font color=green><em>-=</em></font></th>
<th><font color=green><em>/=</em></font></th>
<th><font color=green><em>%=</em></font></th>
<th><font color=green><em>&gt;&gt;=</em></font></th>
<th><font color=green><em>^=</em></font></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><em>+=</em></font></td>
<td><font color=green><em>\</em>=*</font></td>
<td><font color=green><em>~/=</em></font></td>
<td><font color=green>*&lt;&lt;=*</font></td>
<td><font color=green><em>&amp;=</em></font></td>
<td><font color=green>*\</td>
</tr>
</tbody></table>
<p>复合赋值运算符的计算规则：</p>
<table>
<thead>
<tr>
<th><strong>Title</strong></th>
<th><strong>Compound assignment</strong></th>
<th><strong>Equivalent expression</strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong>For an operator op:</strong></td>
<td><font color=green><em>a op=b</em></font></td>
<td><font color=green><em>a = a op b</em></font></td>
</tr>
<tr>
<td>举例</td>
<td><font color=green><em>a += b</em></font></td>
<td><font color=green><em>a = a + b</em></font></td>
</tr>
</tbody></table>
<p>赋值运算符和复合赋值运算符的应用举例：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">2</span>; <span class="comment">// Assign using =</span></span><br><span class="line">a *= <span class="number">3</span>; <span class="comment">// Assign and multiply: a = a * 3</span></span><br><span class="line"><span class="keyword">assert</span>(a == <span class="number">6</span>);</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Logical-operators-逻辑运算符"><a href="#Logical-operators-逻辑运算符" class="headerlink" title="Logical operators 逻辑运算符"></a>Logical operators 逻辑运算符</h4><p>可以使用逻辑运算符对布尔表达式进行组合或者反转。</p>
<table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green>!expr</font></td>
<td>将布尔值反转 （True =&gt; False 或者相反）</td>
</tr>
<tr>
<td><font color=green>||</font></td>
<td>逻辑 OR</td>
</tr>
<tr>
<td><font color=green>&amp;&amp;</font></td>
<td>逻辑 AND</td>
</tr>
</tbody></table>
<p>下面是逻辑表达式应用的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!done &amp;&amp; (col == <span class="number">0</span> || col == <span class="number">3</span>)) &#123;</span><br><span class="line">  <span class="comment">// ...Do something...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Bitwise-and-shift-operators-位操作符"><a href="#Bitwise-and-shift-operators-位操作符" class="headerlink" title="Bitwise and shift operators 位操作符"></a>Bitwise and shift operators 位操作符</h4><p>Dart里可以对数值的每一bit进行单独的处理，一般使用以下运算符对整数进行操作。</p>
<table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green>&amp;</font></td>
<td>AND</td>
</tr>
<tr>
<td><font color=green>|</font></td>
<td>OR</td>
</tr>
<tr>
<td><font color=green>^</font></td>
<td>XOR</td>
</tr>
<tr>
<td><font color=green>~expr</font></td>
<td>1 =&gt; 0, 0 =&gt; 1</td>
</tr>
<tr>
<td><font color=green>&lt;&lt;</font></td>
<td>左移</td>
</tr>
<tr>
<td><font color=green>&gt;&gt;</font></td>
<td>右移</td>
</tr>
</tbody></table>
<p>下面是位操作符的应用例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> value = <span class="number">0x22</span>;</span><br><span class="line"><span class="keyword">final</span> bitmask = <span class="number">0x0f</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>((value &amp; bitmask) == <span class="number">0x02</span>); <span class="comment">// AND</span></span><br><span class="line"><span class="keyword">assert</span>((value &amp; ~bitmask) == <span class="number">0x20</span>); <span class="comment">// AND NOT</span></span><br><span class="line"><span class="keyword">assert</span>((value | bitmask) == <span class="number">0x2f</span>); <span class="comment">// OR</span></span><br><span class="line"><span class="keyword">assert</span>((value ^ bitmask) == <span class="number">0x2d</span>); <span class="comment">// XOR</span></span><br><span class="line"><span class="keyword">assert</span>((value &lt;&lt; <span class="number">4</span>) == <span class="number">0x220</span>); <span class="comment">// Shift left</span></span><br><span class="line"><span class="keyword">assert</span>((value &gt;&gt; <span class="number">4</span>) == <span class="number">0x02</span>); <span class="comment">// Shift right</span></span><br></pre></td></tr></table></figure>

<br>

<h4 id="Conditional-expressions-条件表达式"><a href="#Conditional-expressions-条件表达式" class="headerlink" title="Conditional expressions 条件表达式"></a>Conditional expressions 条件表达式</h4><p>Dart有两个操作符可以让你在不使用 <font color=green><em>if-else</em></font> 语句的前提下简洁地对表达式求值。</p>
<p><font color=green><em>condition ? expr1 : expr2</em></font></p>
<p>&ensp;&ensp; 如果condition为真，则求值expr1并返回，否则求值expr2并返回。</p>
<p><font color=green><em>expr1 ?? expr2</em></font></p>
<p>&ensp;&ensp; 如果expr1非空，返回它的值，否则求值expr2并返回。</p>
<p>当你基于一个布尔表达式为变量赋值的时候，可以考虑使用<font color=green><em>?:</em></font>：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> visibility = isPublic ? <span class="string">'public'</span> : <span class="string">'private'</span>;</span><br></pre></td></tr></table></figure>

<p>如果布尔表达式涉及到是否为空，可以使用<font color=green><em>??</em></font>：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span> playerName(<span class="built_in">String</span> name) =&gt; name ?? <span class="string">'Guest'</span>;</span><br></pre></td></tr></table></figure>

<p>上面的例子都可以用其他至少两种方式实现，但是都不够简洁：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Slightly longer version uses ?: operator.</span></span><br><span class="line"><span class="built_in">String</span> playerName(<span class="built_in">String</span> name) =&gt; name != <span class="keyword">null</span> ? name : <span class="string">'Guest'</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Very long version uses if-else statement.</span></span><br><span class="line"><span class="built_in">String</span> playerName(<span class="built_in">String</span> name) &#123;</span><br><span class="line">  <span class="keyword">if</span> (name != <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> name;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'Guest'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Cascade-notation"><a href="#Cascade-notation" class="headerlink" title="Cascade notation (..)"></a>Cascade notation (..)</h4><p>层叠表示法允许对同一对象执行一系列的运算符计算。除了函数调用外，还可以访问同一对象的字段。这种表示方式可以省去创建一系列临时变量的步骤，便于写出流畅的代码。</p>
<p>考虑以下代码：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">querySelector</span>(<span class="string">'#confirm'</span>) <span class="comment">// Get an object.</span></span><br><span class="line">  ..text = <span class="string">'Confirm'</span> <span class="comment">// Use its members.</span></span><br><span class="line">  ..classes.add(<span class="string">'important'</span>)</span><br><span class="line">  ..onClick.listen((e) =&gt; <span class="built_in">window</span>.alert(<span class="string">'Confirmed!'</span>));</span><br></pre></td></tr></table></figure>

<p>第一个方法调用，<font color=green><em>querySelector()</em></font>，返回一个selector对象。这段代码遵循层叠表示法直接处理这个selector对象，而忽略了可能返回的任何后续值。</p>
<p>以上例子的等价形式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> button = <span class="built_in">querySelector</span>(<span class="string">'#confirm'</span>);</span><br><span class="line">button.text = <span class="string">'Confirm'</span>;</span><br><span class="line">button.classes.add(<span class="string">'important'</span>);</span><br><span class="line">button.onClick.listen((e) =&gt; <span class="built_in">window</span>.alert(<span class="string">'Confirmed!'</span>));</span><br></pre></td></tr></table></figure>

<p>另外，层叠也支持嵌套，例如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> addressBook = (AddressBookBuilder()</span><br><span class="line">      ..name = <span class="string">'jenny'</span></span><br><span class="line">      ..email = <span class="string">'jenny@example.com'</span></span><br><span class="line">      ..phone = (PhoneNumberBuilder()</span><br><span class="line">            ..number = <span class="string">'415-555-0100'</span></span><br><span class="line">            ..label = <span class="string">'home'</span>)</span><br><span class="line">          .build())</span><br><span class="line">    .build();</span><br></pre></td></tr></table></figure>

<p>在构建层叠的时候要注意函数返回的实际对象类型。例如，下面的代码运行会失败：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sb = <span class="built_in">StringBuffer</span>();</span><br><span class="line">sb.write(<span class="string">'foo'</span>)</span><br><span class="line">  ..write(<span class="string">'bar'</span>); <span class="comment">// Error: method 'write' isn't defined for 'void'.</span></span><br></pre></td></tr></table></figure>

<p><font color=green><em>sb.write()</em></font>调用会返回void，在<font color=green><em>void</em></font>类型上无法构建层叠。</p>
<blockquote>
<p>注：严格来讲，层叠的“双点”表示法并不是运算符，它属于Dart语法的一部分。</p>
</blockquote>
<br>

<h4 id="其他运算符"><a href="#其他运算符" class="headerlink" title="其他运算符"></a>其他运算符</h4><table>
<thead>
<tr>
<th><strong>Operator</strong></th>
<th><strong>Name</strong></th>
<th><strong>Meaning</strong></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><strong>( )</strong></font></td>
<td>函数应用</td>
<td>表示函数调用</td>
</tr>
<tr>
<td><font color=green><strong>[ ]</strong></font></td>
<td>列表访问</td>
<td>引用列表中指定索引对应的元素值</td>
</tr>
<tr>
<td><font color=green><strong>.</strong></font></td>
<td>成员访问</td>
<td>引用表达式的属性值，比如：<font color=green><strong>foo.bar</strong></font> 从表达式 <font color=green><strong>foo</strong></font>中提取属性<font color=green><strong>bar</strong></font>的值</td>
</tr>
<tr>
<td><font color=green><strong>?.</strong></font></td>
<td>有条件地成员访问</td>
<td>类似<font color=green><strong>.</strong></font> ，只是左操作符可能为空；比如：<font color=green><strong>foo?.bar</strong></font>， 同样是从表达式 <font color=green><strong>foo</strong></font>中提取属性<font color=green><strong>bar</strong></font>的值，除非<font color=green><strong>foo</strong></font>的值为空（若为空则<font color=green><strong>foo?.bar</strong></font>整个值为空）</td>
</tr>
</tbody></table>
<p>关于<font color=green><strong>.</strong></font>、<font color=green><strong>?.</strong></font>、<font color=green><strong>..</strong></font>运算符的更多信息可以参考<strong><a href="https://dart.dev/guides/language/language-tour#classes" target="_blank" rel="noopener">Classes</a></strong>。</p>
<br>

<h3 id="Control-flow-statement-控制流语句"><a href="#Control-flow-statement-控制流语句" class="headerlink" title="Control flow statement 控制流语句"></a>Control flow statement 控制流语句</h3><p>可以使用下面任何一种方法来控制Dart代码的流程：</p>
<ul>
<li><p><font color=green><strong>if</strong></font> 和 <font color=green><strong>else</strong></font> </p>
</li>
<li><p><font color=green><strong>for</strong></font> 循环</p>
</li>
<li><p><font color=green><strong>while</strong></font> 和 <font color=green><strong>do-while</strong></font> 循环</p>
</li>
<li><p><font color=green><strong>break</strong></font> 和 <font color=green><strong>continue</strong></font> </p>
</li>
<li><p><font color=green><strong>switch</strong></font> 和 <font color=green><strong>case</strong></font> </p>
</li>
<li><p><font color=green><strong>assert</strong></font> </p>
</li>
</ul>
<p>还可以使用<font color=green><em>try-catch</em></font> 和 <font color=green><em>throw</em></font>影响控制流，具体解释见<strong><a href="https://dart.dev/guides/language/language-tour#exceptions" target="_blank" rel="noopener">Exceptions</a></strong>。</p>
<br>

<h4 id="if-和-else"><a href="#if-和-else" class="headerlink" title="if 和 else"></a>if 和 else</h4><p>Dart支持带有可选<font color=green><em>else</em></font>语句的<font color=green><em>if</em></font>语句，如下面例子所示，还可以参考<strong><a href="https://dart.dev/guides/language/language-tour#conditional-expressions" target="_blank" rel="noopener">conditional expressions</a></strong>。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (isRaining()) &#123;  </span><br><span class="line">  you.bringRainCoat(); </span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (isSnowing()) &#123;</span><br><span class="line">  you.wearJacket(); </span><br><span class="line">&#125; <span class="keyword">else</span> &#123;  </span><br><span class="line">  car.putTopDown(); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>和Javascript不同，这里的条件判断只能使用布尔值，更多信息参考<strong><a href="https://dart.dev/guides/language/language-tour#booleans" target="_blank" rel="noopener">Booleans</a></strong>。</p>
<br>

<h4 id="For循环"><a href="#For循环" class="headerlink" title="For循环"></a>For循环</h4><p>可以使用标准的for循环进行迭代，比如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> message = <span class="built_in">StringBuffer</span>(<span class="string">'Dart is fun'</span>);</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">  message.write(<span class="string">'!'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Dart循环中的闭包能够正常获取索引值，避免了Javascript中常见的一个陷阱。例如，看一下这段代码，</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> callbacks = [];</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123;</span><br><span class="line">  callbacks.add(() =&gt; <span class="built_in">print</span>(i));</span><br><span class="line">&#125;</span><br><span class="line">callbacks.forEach((c) =&gt; c());</span><br></pre></td></tr></table></figure>

<p>结果会按预期先输出0，然后是1，而这个例子在Javascript中会输出2和2。</p>
<p>如果你要迭代的是一个可迭代的对象，可以使用<strong><a href="https://api.dart.dev/stable/dart-core/Iterable/forEach.html" target="_blank" rel="noopener">forEach()</a></strong>方法。如果不关心当前的迭代计数，使用<font color=green><em>forEach()</em></font>方法是个不错的选择。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">candidates.forEach((candidate) =&gt; candidate.interview());</span><br></pre></td></tr></table></figure>

<p>另外像List、Set这些可迭代类型，也支持<font color=green><em>for-in</em></font>的<a href="https://dart.dev/guides/libraries/library-tour#iteration" target="_blank" rel="noopener">迭代</a>形式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> collection = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>]; </span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> x <span class="keyword">in</span> collection) &#123;  </span><br><span class="line">  <span class="built_in">print</span>(x); <span class="comment">// 0 1 2 </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="while和do-while"><a href="#while和do-while" class="headerlink" title="while和do-while"></a>while和do-while</h4><p><font color=green><em>while</em></font>循环是在<em>循环前</em>对条件求值：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (!isDone()) &#123;  </span><br><span class="line">  doSomething(); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><font color=green><em>do-while</em></font>循环是在<em>循环后</em>对条件求值：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span> &#123;  </span><br><span class="line">  printLine(); </span><br><span class="line">&#125; <span class="keyword">while</span> (!atEndOfPage());</span><br></pre></td></tr></table></figure>

<br>

<h4 id="break和continue"><a href="#break和continue" class="headerlink" title="break和continue"></a>break和continue</h4><p>使用<font color=green><em>break</em></font>可以跳出循环：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (shutDownRequested()) <span class="keyword">break</span>;</span><br><span class="line">  processIncomingRequests();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用<font color=green><em>continue</em></font>可以跳到下一次循环迭代：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; candidates.length; i++) &#123;</span><br><span class="line">  <span class="keyword">var</span> candidate = candidates[i];</span><br><span class="line">  <span class="keyword">if</span> (candidate.yearsExperience &lt; <span class="number">5</span>) &#123;</span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  candidate.interview();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果操作的是List或者Set这种<strong><a href="https://api.dart.dev/stable/dart-core/Iterable-class.html" target="_blank" rel="noopener">Iterable</a></strong>对象，还可以像下面的例子这样实现：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">candidates</span><br><span class="line">    .where((c) =&gt; c.yearsExperience &gt;= <span class="number">5</span>)</span><br><span class="line">    .forEach((c) =&gt; c.interview());</span><br></pre></td></tr></table></figure>

<br>

<h4 id="switch-和-case"><a href="#switch-和-case" class="headerlink" title="switch 和 case"></a>switch 和 case</h4><p>Dart的switch语句中，使用<font color=green><em>==</em></font>来比较整数、字符串，或者其他编译时常量。被比较的对象类型必须相同（不能是任何其他子类型），而该类型无需重写<font color=green><em>==</em></font>运算符。<strong><a href="https://dart.dev/guides/language/language-tour#enumerated-types" target="_blank" rel="noopener">枚举类型</a></strong>在<font color=green><em>switch</em></font>语句中会运行良好。</p>
<blockquote>
<p>注：Dart中的switch语句适用于有限的环境，比如解释器和扫描器。</p>
</blockquote>
<p>按规定每个非空的<font color=green><em>case</em></font>子句必须以<font color=green><em>break</em></font>结尾。结束非空case子句的其他有效方法是<font color=green><em>continue</em></font>、<font color=green><em>throw</em></font>或者<font color=green><em>return</em></font>语句。</p>
<p>当没有匹配的<font color=green><em>case</em></font>子句时，则执行<font color=green><em>default</em></font>子句的代码。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> command = <span class="string">'OPEN'</span>;</span><br><span class="line"><span class="keyword">switch</span> (command) &#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">'CLOSED'</span>:</span><br><span class="line">    executeClosed();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">'PENDING'</span>:</span><br><span class="line">    executePending();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">'APPROVED'</span>:</span><br><span class="line">    executeApproved();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">'DENIED'</span>:</span><br><span class="line">    executeDenied();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">'OPEN'</span>:</span><br><span class="line">    executeOpen();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">default</span>:</span><br><span class="line">    executeUnknown();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面这个例子中，<font color=green><em>case</em></font>子句中漏掉了一个<font color=green><em>break</em></font>语句，从而产生了一个error：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> command = <span class="string">'OPEN'</span>;</span><br><span class="line"><span class="keyword">switch</span> (command) &#123;</span><br><span class="line">  <span class="keyword">case</span> <span class="string">'OPEN'</span>:</span><br><span class="line">    executeOpen();</span><br><span class="line">    <span class="comment">// ERROR: Missing break</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">case</span> <span class="string">'CLOSED'</span>:</span><br><span class="line">    executeClosed();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然而，Dart支持空的<font color=green><em>case</em></font>子句，允许这种fall-through的形式：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> command = <span class="string">'CLOSED'</span>; </span><br><span class="line"><span class="keyword">switch</span> (command) &#123;  </span><br><span class="line">  <span class="keyword">case</span> <span class="string">'CLOSED'</span>: <span class="comment">// Empty case falls through.  </span></span><br><span class="line">  <span class="keyword">case</span> <span class="string">'NOW_CLOSED'</span>:    <span class="comment">// Runs for both CLOSED and NOW_CLOSED.    </span></span><br><span class="line">    executeNowClosed();    </span><br><span class="line">    <span class="keyword">break</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果想要真正地跳转，可以使用<font color=green><em>continue</em></font>语句结合标签来实现：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> command = <span class="string">'CLOSED'</span>; </span><br><span class="line"><span class="keyword">switch</span> (command) &#123;  </span><br><span class="line"><span class="keyword">case</span> <span class="string">'CLOSED'</span>:    </span><br><span class="line">    executeClosed();    </span><br><span class="line">    <span class="keyword">continue</span> nowClosed;  </span><br><span class="line">    <span class="comment">// Continues executing at the nowClosed label.   </span></span><br><span class="line">    nowClosed:  </span><br><span class="line">  <span class="keyword">case</span> <span class="string">'NOW_CLOSED'</span>:    <span class="comment">// Runs for both CLOSED and NOW_CLOSED.    </span></span><br><span class="line">    executeNowClosed();    </span><br><span class="line">    <span class="keyword">break</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><font color=green><em>case</em></font>子句可以支持local变量，且local变量只在该子句的作用范围内可见。</p>
<br>

<h3 id="Assert-断言"><a href="#Assert-断言" class="headerlink" title="Assert 断言"></a>Assert 断言</h3><p>开发过程中，可以使用断言——<font color=green><em>assert(condition, optionalMessage)</em></font>——来中断正常的执行，如果condition参数为False的话。在这篇文章中可以找到很多应用assert语句的例子，下面再给出一些例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Make sure the variable has a non-null value.</span></span><br><span class="line"><span class="keyword">assert</span>(text != <span class="keyword">null</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Make sure the value is less than 100.</span></span><br><span class="line"><span class="keyword">assert</span>(number &lt; <span class="number">100</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Make sure this is an https URL.</span></span><br><span class="line"><span class="keyword">assert</span>(urlString.startsWith(<span class="string">'https'</span>));</span><br></pre></td></tr></table></figure>

<p>想要给断言附加消息，可以添加一个字符串作为<font color=green><em>assert</em></font>的第二个参数。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>(urlString.startsWith(<span class="string">'https'</span>),</span><br><span class="line">    <span class="string">'URL (<span class="subst">$urlString</span>) should start with "https".'</span>);</span><br></pre></td></tr></table></figure>

<p><font color=green><em>assert</em></font>的第一个参数可以是任意能够解析为布尔值的表达式，如果表达式为true，断言通过，执行后续代码。如果为false，断言失败，会抛出<strong><a href="https://api.dart.dev/stable/dart-core/AssertionError-class.html" target="_blank" rel="noopener">AssertionError</a></strong>异常。</p>
<p>什么情况下断言会生效？具体结果依赖于我们所使用的工具和框架：</p>
<ul>
<li>在<strong><a href="https://flutter.dev/docs/testing/debugging#debug-mode-assertions" target="_blank" rel="noopener">debug mode</a></strong>下，flutter会启用断言</li>
<li>仅面向开发的工具，如<strong><a href="https://dart.dev/tools/dartdevc" target="_blank" rel="noopener">dartdevc</a></strong>通常默认启用断言</li>
<li>一些工具，如<strong><a href="https://dart.dev/server/tools/dart-vm" target="_blank" rel="noopener">dart</a></strong>和<strong><a href="https://dart.dev/tools/dart2js" target="_blank" rel="noopener">dart2js</a></strong>，支持通过命令行参数<font color=green><em>–enable-asserts</em></font>启用断言</li>
</ul>
<p>在产品代码中，断言会被忽略掉，针对<font color=green><em>assert</em></font>的启动参数也不会生效。</p>
<br>

<h3 id="Exception-异常"><a href="#Exception-异常" class="headerlink" title="Exception 异常"></a>Exception 异常</h3><p>Dart代码可以抛出和捕获异常。异常表明一些期望之外的事情出现引发了错误。如果异常未被捕获，抛出异常的<strong><a href="https://dart.dev/guides/language/language-tour#isolates" target="_blank" rel="noopener">isolate</a></strong>将会被挂起，通常isolate和它所在的程序会被终止。</p>
<p>与Java相反，所有Dart异常都是unchecked异常，方法不会声明它们可能会抛出哪些异常，也不需要你捕获任何异常。</p>
<p>Dart提供了<strong><a href="https://api.dart.dev/stable/dart-core/Exception-class.html" target="_blank" rel="noopener">Exception</a></strong>和<strong><a href="https://api.dart.dev/stable/dart-core/Error-class.html" target="_blank" rel="noopener">Error</a></strong>类型，以及许多预定义的子类型。当然，你可以定义自己的异常，然而Dart程序可以把任何非空对象作为异常对象抛出，而不必一定是Exception和Error对象。</p>
<br>

<h4 id="Throw"><a href="#Throw" class="headerlink" title="Throw"></a>Throw</h4><p>下面是抛出异常的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">throw</span> FormatException(<span class="string">'Expected at least 1 section'</span>);</span><br></pre></td></tr></table></figure>

<p>你还可以抛出任意对象：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">throw</span> <span class="string">'Out of llamas!'</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：生产质量代码通常会抛出实现了<strong><a href="https://api.dart.dev/stable/dart-core/Exception-class.html" target="_blank" rel="noopener">Exception</a></strong>和<strong><a href="https://api.dart.dev/stable/dart-core/Error-class.html" target="_blank" rel="noopener">Error</a></strong>的类型。</p>
</blockquote>
<p>因为异常抛出是一个表达式，你可以在=&gt;语句中，也可以在任何允许使用表达式的位置抛出异常：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> distanceTo(Point other) =&gt; <span class="keyword">throw</span> UnimplementedError();</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Catch"><a href="#Catch" class="headerlink" title="Catch"></a>Catch</h4><p>捕获异常可以防止异常向上层传播，除非你再次抛出。它给你提供了处理异常的机会：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  breedMoreLlamas();</span><br><span class="line">&#125; <span class="keyword">on</span> OutOfLlamasException &#123;</span><br><span class="line">  buyMoreLlamas();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于那些可能会抛出多种类型异常的代码，你可以指定多个catch子句来处理。第一个匹配上所抛出对象类型的catch子句会处理相应的异常，如果catch子句没有指定类型，则它可以处理任何类型的被抛出的对象。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  breedMoreLlamas();</span><br><span class="line">&#125; <span class="keyword">on</span> OutOfLlamasException &#123;</span><br><span class="line">  <span class="comment">// A specific exception</span></span><br><span class="line">  buyMoreLlamas();</span><br><span class="line">&#125; <span class="keyword">on</span> Exception <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">  <span class="comment">// Anything else that is an exception</span></span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Unknown exception: <span class="subst">$e</span>'</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">  <span class="comment">// No specified type, handles all</span></span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Something really unknown: <span class="subst">$e</span>'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从上面的代码可见，捕获异常时可以使用<font color=green><em>on</em></font>或者<font color=green><em>catch</em></font>，或者两者兼用。当你需要指定异常类型时可以用<font color=green><em>on</em></font>，当异常处理程序需要Exception对象时使用<font color=green><em>catch</em></font>。</p>
<p>你可以为<font color=green><em>catch</em></font>指定一或两个参数。第一个参数是所抛出的异常，第二个是栈trace（<strong><a href="https://api.dart.dev/stable/dart-core/StackTrace-class.html" target="_blank" rel="noopener">StackTrace</a></strong>对象）。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125; <span class="keyword">on</span> Exception <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Exception details:\n <span class="subst">$e</span>'</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (e, s) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Exception details:\n <span class="subst">$e</span>'</span>);</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Stack trace:\n <span class="subst">$s</span>'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果处理部分异常的同时允许异常往外传播，可以使用<font color=green><em>rethrow</em></font>关键字。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> misbehave() &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="built_in">dynamic</span> foo = <span class="keyword">true</span>;</span><br><span class="line">    <span class="built_in">print</span>(foo++); <span class="comment">// Runtime error</span></span><br><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'misbehave() partially handled <span class="subst">$&#123;e.runtimeType&#125;</span>.'</span>);</span><br><span class="line">    <span class="keyword">rethrow</span>; <span class="comment">// Allow callers to see the exception.</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    misbehave();</span><br><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'main() finished handling <span class="subst">$&#123;e.runtimeType&#125;</span>.'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Finally"><a href="#Finally" class="headerlink" title="Finally"></a>Finally</h4><p>要确保一些代码无论是否有异常抛出都能运行，可以使用<font color=green><em>finally</em></font>子句。如果没有<font color=green><em>catch</em></font>子句匹配上异常，则异常会在<font color=green><em>finally</em></font>子句运行后继续向上层传播：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  breedMoreLlamas();</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">  <span class="comment">// Always clean up, even if an exception is thrown.</span></span><br><span class="line">  cleanLlamaStalls();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在任何匹配的<font color=green><em>catch</em></font>子句运行之后，<font color=green><em>finally</em></font>子句会继续运行。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  breedMoreLlamas();</span><br><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Error: <span class="subst">$e</span>'</span>); <span class="comment">// Handle the exception first.</span></span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">  cleanLlamaStalls(); <span class="comment">// Then clean up.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>更多信息可以参考<strong><a href="https://dart.dev/guides/libraries/library-tour#exceptions" target="_blank" rel="noopener">Exceptions</a></strong>。</p>
<br>

<h3 id="Classes"><a href="#Classes" class="headerlink" title="Classes"></a>Classes</h3><p>Dart是一种支持类和基于mixin继承的面向对象语言。每个对象都是类的实例，所有类都派生自<strong><a href="https://api.dart.dev/stable/dart-core/Object-class.html" target="_blank" rel="noopener">Object</a></strong>。基于mixin的继承意味着虽然每个类（除了<strong><a href="https://api.dart.dev/stable/dart-core/Object-class.html" target="_blank" rel="noopener">Object</a></strong>）只有一个超类，但一个类主体可以在多个类层次结构中重用。<strong><a href="https://dart.dev/guides/language/language-tour#extension-methods" target="_blank" rel="noopener">Extension methods</a></strong>是可以不用改变类或者创建子类而为类添加功能的一种方法。</p>
<br>

<h4 id="使用类成员"><a href="#使用类成员" class="headerlink" title="使用类成员"></a>使用类成员</h4><p>对象的成员由函数和数据（分别是方法和实例变量）组成。当你访问一个方法时，可以基于对象来调用它：方法可以访问对象的函数和数据。</p>
<p>使用点（<font color=green><em>.</em></font>）来引用实例变量和方法：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = Point(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Set the value of the instance variable y.</span></span><br><span class="line">p.y = <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Get the value of y.</span></span><br><span class="line"><span class="keyword">assert</span>(p.y == <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Invoke distanceTo() on p.</span></span><br><span class="line"><span class="built_in">num</span> distance = p.distanceTo(Point(<span class="number">4</span>, <span class="number">4</span>));</span><br></pre></td></tr></table></figure>

<p>使用<font color=green><em>?.</em></font>代替<font color=green><em>.</em></font>以避免左操作数为空引发异常：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// If p is non-null, set its y value to 4.</span></span><br><span class="line">p?.y = <span class="number">4</span>;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="使用构造函数"><a href="#使用构造函数" class="headerlink" title="使用构造函数"></a>使用构造函数</h4><p>你可以使用构造函数创建一个对象。构造函数名可以用<font color=green><em>ClassName</em></font>或者<font color=green><em>ClassName.identifier</em></font>。比如下面的代码中，使用<font color=green><em>Point()</em></font>和<font color=green><em>Point.fromJson()</em></font>构造函数创建了<font color=green><em>Point</em></font>对象：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = Point(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line"><span class="keyword">var</span> p2 = Point.fromJson(&#123;<span class="string">'x'</span>: <span class="number">1</span>, <span class="string">'y'</span>: <span class="number">2</span>&#125;);</span><br></pre></td></tr></table></figure>

<p>下面这段代码在构造函数名字前使用了可选的<font color=green><em>new</em></font>关键字，具有同样的效果：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p1 = <span class="keyword">new</span> Point(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line"><span class="keyword">var</span> p2 = <span class="keyword">new</span> Point.fromJson(&#123;<span class="string">'x'</span>: <span class="number">1</span>, <span class="string">'y'</span>: <span class="number">2</span>&#125;);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：<font color=green><em>new</em></font>关键字在Dart2.0变为可选的。</p>
</blockquote>
<p>一些类提供了<strong><a href="https://dart.dev/guides/language/language-tour#constant-constructors" target="_blank" rel="noopener">constant constructors</a></strong>，把<font color=green><em>const</em></font>关键字放在了构造函数名字前，可以用来创建一个编译时常量：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = <span class="keyword">const</span> ImmutablePoint(<span class="number">2</span>, <span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<p>构造两个相同的编译时常量会得到同一个规范化的对象实例：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="keyword">const</span> ImmutablePoint(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line"><span class="keyword">var</span> b = <span class="keyword">const</span> ImmutablePoint(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>(identical(a, b)); <span class="comment">// They are the same instance!</span></span><br></pre></td></tr></table></figure>

<p>在常量上下文中，可以略去构造函数或者字面量前面的<font color=green><em>const</em></font>。比如这段创建const map的代码：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Lots of const keywords here.</span></span><br><span class="line"><span class="keyword">const</span> pointAndLine = <span class="keyword">const</span> &#123;</span><br><span class="line">  <span class="string">'point'</span>: <span class="keyword">const</span> [<span class="keyword">const</span> ImmutablePoint(<span class="number">0</span>, <span class="number">0</span>)],</span><br><span class="line">  <span class="string">'line'</span>: <span class="keyword">const</span> [<span class="keyword">const</span> ImmutablePoint(<span class="number">1</span>, <span class="number">10</span>), <span class="keyword">const</span> ImmutablePoint(<span class="number">-2</span>, <span class="number">11</span>)],</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>可以保留第一个略去其他的<font color=green><em>const</em></font>关键字：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Only one const, which establishes the constant context.</span></span><br><span class="line"><span class="keyword">const</span> pointAndLine = &#123;</span><br><span class="line">  <span class="string">'point'</span>: [ImmutablePoint(<span class="number">0</span>, <span class="number">0</span>)],</span><br><span class="line">  <span class="string">'line'</span>: [ImmutablePoint(<span class="number">1</span>, <span class="number">10</span>), ImmutablePoint(<span class="number">-2</span>, <span class="number">11</span>)],</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果一个constant constructor在常量上下文之外，并且调用时没有使用<font color=green><em>const</em></font>，它会创建一个<strong>非常量对象</strong>：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="keyword">const</span> ImmutablePoint(<span class="number">1</span>, <span class="number">1</span>); <span class="comment">// Creates a constant</span></span><br><span class="line"><span class="keyword">var</span> b = ImmutablePoint(<span class="number">1</span>, <span class="number">1</span>); <span class="comment">// Does NOT create a constant</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">assert</span>(!identical(a, b)); <span class="comment">// NOT the same instance!</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p> 注：常量上下文中<font color=green><em>const</em></font>关键字是在Dart2.0变为可选的。</p>
</blockquote>
<br>

<h4 id="获取对象的类型"><a href="#获取对象的类型" class="headerlink" title="获取对象的类型"></a>获取对象的类型</h4><p>可以用对象的<font color=green><em>runtimeType</em></font>属性，在运行时获取一个对象的类型，它会返回一个<strong><a href="https://api.dart.dev/stable/dart-core/Type-class.html" target="_blank" rel="noopener">Type</a></strong>对象。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">'The type of a is <span class="subst">$&#123;a.runtimeType&#125;</span>'</span>);</span><br></pre></td></tr></table></figure>

<p>到此为止，你已经了解了如何使用类。本节的其他部分会展示如何实现类。</p>
<br>

<h4 id="Instance-variables-实例变量"><a href="#Instance-variables-实例变量" class="headerlink" title="Instance variables 实例变量"></a>Instance variables 实例变量</h4><p>下面展示了如何声明实例变量：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x; <span class="comment">// Declare instance variable x, initially null.</span></span><br><span class="line">  <span class="built_in">num</span> y; <span class="comment">// Declare y, initially null.</span></span><br><span class="line">  <span class="built_in">num</span> z = <span class="number">0</span>; <span class="comment">// Declare z, initially 0.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所有未初始化的实例变量都会拥有<font color=green><em>null</em></font>值。</p>
<p>所有实例变量都会生成一个隐式的<em>getter</em>方法，非final实例变量还会生成一个隐式的<em>setter</em>方法。具体细节参见<strong><a href="https://dart.dev/guides/language/language-tour#getters-and-setters" target="_blank" rel="noopener">Getters and setters</a></strong>。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x;</span><br><span class="line">  <span class="built_in">num</span> y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">var</span> point = Point();</span><br><span class="line">  point.x = <span class="number">4</span>; <span class="comment">// Use the setter method for x.</span></span><br><span class="line">  <span class="keyword">assert</span>(point.x == <span class="number">4</span>); <span class="comment">// Use the getter method for x.</span></span><br><span class="line">  <span class="keyword">assert</span>(point.y == <span class="keyword">null</span>); <span class="comment">// Values default to null.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果你在声明实例变量的同时初始化（而不是在构造函数或方法中），则在构造函数和它的初始化列表执行前，实例对象创建的时候实例变量就会被赋值。</p>
<br>

<h4 id="Constructors-构造函数"><a href="#Constructors-构造函数" class="headerlink" title="Constructors 构造函数"></a>Constructors 构造函数</h4><p>通过创建一个与类同名的函数来声明构造函数（另外，如<strong><a href="https://dart.dev/guides/language/language-tour#named-constructors" target="_blank" rel="noopener">Named constructors</a></strong>所述，还有一个可选的标识符）。最常见的构造函数形式是生成构造函数，它创建一个类的新实例：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x, y;</span><br><span class="line"></span><br><span class="line">  Point(<span class="built_in">num</span> x, <span class="built_in">num</span> y) &#123;</span><br><span class="line">    <span class="comment">// There's a better way to do this, stay tuned.</span></span><br><span class="line">    <span class="keyword">this</span>.x = x;</span><br><span class="line">    <span class="keyword">this</span>.y = y;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中的<font color=green><em>this</em></font>关键字指向当前的实例。</p>
<blockquote>
<p>注：仅当出现名字冲突的时候<font color=green><em>this</em></font>关键字是必要的，否则Dart的代码风格会略掉<font color=green><em>this</em></font>。</p>
</blockquote>
<p>将构造函数参数赋值给实例变量的模式非常常见，Dart利用语法糖做了简化支持：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x, y;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Syntactic sugar for setting x and y</span></span><br><span class="line">  <span class="comment">// before the constructor body runs.</span></span><br><span class="line">  Point(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="缺省构造函数"><a href="#缺省构造函数" class="headerlink" title="缺省构造函数"></a>缺省构造函数</h5><p>如果没有声明构造函数，类会提供一个缺省的构造函数。缺省构造函数没有参数，且只支持调用superclass的无参构造函数。</p>
<h5 id="构造函数不可继承"><a href="#构造函数不可继承" class="headerlink" title="构造函数不可继承"></a>构造函数不可继承</h5><p>子类不能从它的superclass继承构造函数。没有声明构造函数的子类只会提供一个没有参数没有名字的默认构造函数。</p>
<h5 id="Named-Constructors-命名构造函数"><a href="#Named-Constructors-命名构造函数" class="headerlink" title="Named Constructors 命名构造函数"></a>Named Constructors 命名构造函数</h5><p>使用命名构造函数的方式可以为一个类提供多个构造函数实现，或者提供额外更清晰的信息。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x, y;</span><br><span class="line"></span><br><span class="line">  Point(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Named constructor</span></span><br><span class="line">  Point.origin() &#123;</span><br><span class="line">    x = <span class="number">0</span>;</span><br><span class="line">    y = <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>记住构造函数是不可继承的，这就意味着superclass的命名构造函数也不能被子类所继承。如果想要使用superclass中定义的命名构造函数来创建子类的对象，则必须在子类中实现该构造函数。</p>
<h5 id="调用superclass的非默认构造函数"><a href="#调用superclass的非默认构造函数" class="headerlink" title="调用superclass的非默认构造函数"></a>调用superclass的非默认构造函数</h5><p>默认情况下，子类的构造函数可以调用superclass的无名无参的构造函数。并且superclass的构造函数需要在子类的构造函数开头调用。而<strong><a href="https://dart.dev/guides/language/language-tour#initializer-list" target="_blank" rel="noopener">initializer list</a></strong>会在superclass的构造函数被调用前执行。综上所述，整个执行顺序如下：</p>
<ol>
<li>初始化列表</li>
<li>superclass的无参构造函数</li>
<li>主类的无参构造函数</li>
</ol>
<p>如果superclass没有定义无名无参的构造函数，那必须手动调用superclass的某个构造函数。而superclass的构造函数调用被指定写在构造函数体（如果有的话）之前，冒号（:）之后。</p>
<p>下面这个例子中，Employee类的构造函数调用了它的superclass “Person”的命名构造函数。</p>
<p>因为superclass的构造函数参数求值是先于构造函数调用，所以参数可以是一个表达式，就像函数调用那样：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Employee</span> <span class="keyword">extends</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  Employee() : <span class="keyword">super</span>.fromJson(defaultData);</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>警告：superclass的构造函数的参数没有访问<font color=green><em>this</em></font>的权限，例如，参数可以调用类的静态方法，但不可以调用实例方法。</p>
</blockquote>
<h5 id="Initializer-list-初始化列表"><a href="#Initializer-list-初始化列表" class="headerlink" title="Initializer list 初始化列表"></a>Initializer list 初始化列表</h5><p>除了调用superclass构造函数外，还可以在构造函数体运行前初始化实例变量。初始化器间用逗号分隔。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Initializer list sets instance variables before</span></span><br><span class="line"><span class="comment">// the constructor body runs.</span></span><br><span class="line">Point.fromJson(<span class="built_in">Map</span>&lt;<span class="built_in">String</span>, <span class="built_in">num</span>&gt; json)</span><br><span class="line">    : x = json[<span class="string">'x'</span>],</span><br><span class="line">      y = json[<span class="string">'y'</span>] &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'In Point.fromJson(): (<span class="subst">$x</span>, <span class="subst">$y</span>)'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>警告：初始化赋值语句右侧表达式没有访问<font color=green><em>this</em></font>的权限。</p>
</blockquote>
<p>在开发过程中，可以在初始化列表中使用<font color=green><em>assert</em></font>来验证输入。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Point.withAssert(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y) : <span class="keyword">assert</span>(x &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'In Point.withAssert(): (<span class="subst">$x</span>, <span class="subst">$y</span>)'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>用初始化列表的方式来创建final字段很方便，后面的例子会有介绍。</p>
<br>

<h4 id="Redirecting-constructors"><a href="#Redirecting-constructors" class="headerlink" title="Redirecting constructors"></a>Redirecting constructors</h4><p>有时一个构造函数的唯一目的是重定向到同一个类中的另一个构造函数。重定向目标构造函数的函数体是空的，在冒号（:）后面跟着被重定向的构造函数。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x, y;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// The main constructor for this class.</span></span><br><span class="line">  Point(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Delegates to the main constructor.</span></span><br><span class="line">  Point.alongXAxis(<span class="built_in">num</span> x) : <span class="keyword">this</span>(x, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Constant-constructors"><a href="#Constant-constructors" class="headerlink" title="Constant constructors"></a>Constant constructors</h4><p>如果类中生成的对象从未被更改，可以将这些对象设置为编译时常量。针对这个需要，dart定义了<font color=green><em>const</em></font>构造函数，确保所有的实例变量都是<font color=green><em>final</em></font>的。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ImmutablePoint</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">final</span> ImmutablePoint origin =</span><br><span class="line">      <span class="keyword">const</span> ImmutablePoint(<span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">final</span> <span class="built_in">num</span> x, y;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> ImmutablePoint(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Constant constructors不总是创建常量，详情可参见<strong><a href="https://dart.dev/guides/language/language-tour#using-constructors" target="_blank" rel="noopener">using constructors</a></strong>。</p>
<br>

<h4 id="Factory-constructors-工厂构造函数"><a href="#Factory-constructors-工厂构造函数" class="headerlink" title="Factory constructors 工厂构造函数"></a>Factory constructors 工厂构造函数</h4><p>在实现构造函数时使用<font color=green><em>factory</em></font>关键字并不总是创建一个类的新实例。比如工厂构造函数可能会从缓存返回已存在的实例对象，或者返回一个子类的对象。</p>
<p>下面这个例子演示了工厂构造函数从缓存返回对象：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Logger</span> </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> <span class="built_in">String</span> name;</span><br><span class="line">  <span class="built_in">bool</span> mute = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// _cache is library-private, thanks to</span></span><br><span class="line">  <span class="comment">// the _ in front of its name.</span></span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">final</span> <span class="built_in">Map</span>&lt;<span class="built_in">String</span>, Logger&gt; _cache =</span><br><span class="line">      &lt;<span class="built_in">String</span>, Logger&gt;&#123;&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">factory</span> Logger(<span class="built_in">String</span> name) &#123;</span><br><span class="line">    <span class="keyword">return</span> _cache.putIfAbsent(</span><br><span class="line">        name, () =&gt; Logger._internal(name));</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  Logger._internal(<span class="keyword">this</span>.name);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">void</span> log(<span class="built_in">String</span> msg) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!mute) <span class="built_in">print</span>(msg);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：工厂构造函数没有权限访问<font color=green><em>this</em></font>。</p>
</blockquote>
<p>工厂构造函数的调用方式与其他构造函数相同：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> logger = Logger(<span class="string">'UI'</span>);</span><br><span class="line">logger.log(<span class="string">'Button clicked'</span>);</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Methods-方法"><a href="#Methods-方法" class="headerlink" title="Methods 方法"></a>Methods 方法</h4><p>方法是为对象提供行为的函数。</p>
<br>

<h4 id="实例方法"><a href="#实例方法" class="headerlink" title="实例方法"></a>实例方法</h4><p>对象的实例方法可以访问实例变量以及<font color=green><em>this</em></font>。下面代码中的<font color=green><em>distanceTo()</em></font>方法就是一个实例方法的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'dart:math'</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x, y;</span><br><span class="line"></span><br><span class="line">  Point(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">num</span> distanceTo(Point other) &#123;</span><br><span class="line">    <span class="keyword">var</span> dx = x - other.x;</span><br><span class="line">    <span class="keyword">var</span> dy = y - other.y;</span><br><span class="line">    <span class="keyword">return</span> sqrt(dx * dx + dy * dy);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Getters-and-setters"><a href="#Getters-and-setters" class="headerlink" title="Getters and setters"></a>Getters and setters</h4><p>Getters和setters是为对象属性提供读写访问的特殊方法。回想一下，每个实例变量都会有一个隐式的getter，如果适合的话还会有一个setter。可以通过使用<font color=green><em>get</em></font>以及<font color=green><em>set</em></font>关键字实现getter和setter，从而创建额外的属性：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Rectangle</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> left, top, width, height;</span><br><span class="line"></span><br><span class="line">  Rectangle(<span class="keyword">this</span>.left, <span class="keyword">this</span>.top, <span class="keyword">this</span>.width, <span class="keyword">this</span>.height);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Define two calculated properties: right and bottom.</span></span><br><span class="line">  <span class="built_in">num</span> <span class="keyword">get</span> right =&gt; left + width;</span><br><span class="line">  <span class="keyword">set</span> right(<span class="built_in">num</span> value) =&gt; left = value - width;</span><br><span class="line">  <span class="built_in">num</span> <span class="keyword">get</span> bottom =&gt; top + height;</span><br><span class="line">  <span class="keyword">set</span> bottom(<span class="built_in">num</span> value) =&gt; top = value - height;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">var</span> rect = Rectangle(<span class="number">3</span>, <span class="number">4</span>, <span class="number">20</span>, <span class="number">15</span>);</span><br><span class="line">  <span class="keyword">assert</span>(rect.left == <span class="number">3</span>);</span><br><span class="line">  rect.right = <span class="number">12</span>;</span><br><span class="line">  <span class="keyword">assert</span>(rect.left == <span class="number">-8</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过getters和setters，可以将实例变量用方法进行封装，而不会影响到客户代码。</p>
<blockquote>
<p>注：无论getter是否显式定义，诸如increment（++）这样的操作符都会以预期的方式运行。为了避免任何意外的副作用，操作符只调用getter一次，并将值保存在一个临时变量中。</p>
</blockquote>
<br>

<h4 id="Abstract-methods-抽象方法"><a href="#Abstract-methods-抽象方法" class="headerlink" title="Abstract methods 抽象方法"></a>Abstract methods 抽象方法</h4><p>实例方法、getters和setters都可以是抽象的，只定义接口而将其具体实现留给其他类。抽象方法只能存在于<strong><a href="https://dart.dev/guides/language/language-tour#abstract-classes" target="_blank" rel="noopener">abstract classes</a></strong>。</p>
<p>定义抽象方法，只需要使用分号（;）代替函数体。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Doer</span> </span>&#123;</span><br><span class="line">  <span class="comment">// Define instance variables and methods...</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">void</span> doSomething(); <span class="comment">// Define an abstract method.</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">EffectiveDoer</span> <span class="keyword">extends</span> <span class="title">Doer</span> </span>&#123;</span><br><span class="line">  <span class="keyword">void</span> doSomething() &#123;</span><br><span class="line">    <span class="comment">// Provide an implementation, so the method is not abstract here...</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Abstract-class-抽象类"><a href="#Abstract-class-抽象类" class="headerlink" title="Abstract class 抽象类"></a>Abstract class 抽象类</h4><p>可以使用<font color=green><em>abstract</em></font>修饰符来定义不能被实例化的抽象类。抽象类对于定义接口非常有用，通常带有一些实现。如果想要让抽象类看起来是可实例化的，需要定义<strong><a href="https://dart.dev/guides/language/language-tour#factory-constructors" target="_blank" rel="noopener">factory constructor</a></strong>。</p>
<p>抽象类通常包含抽象方法。下面这个例子声明了一个带有抽象方法的抽象类：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// This class is declared abstract and thus</span></span><br><span class="line"><span class="comment">// can't be instantiated.</span></span><br><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractContainer</span> </span>&#123;</span><br><span class="line">  <span class="comment">// Define constructors, fields, methods...</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">void</span> updateChildren(); <span class="comment">// Abstract method.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Implicit-interfaces-隐式接口"><a href="#Implicit-interfaces-隐式接口" class="headerlink" title="Implicit interfaces 隐式接口"></a>Implicit interfaces 隐式接口</h4><p>每个类都隐式定义了一个接口，该接口包含了所有该类以及所有其实现的接口的实例变量。如果想要创建一个类A，它支持类B的API而不继承类B的实现，则类A应该实现类B的接口。</p>
<p>一个类如果要实现一个和多个接口，可以通过在<font color=green><em>implements</em></font>子句中声明这些接口，并提供它们所需的API实现。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// A person. The implicit interface contains greet().</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  <span class="comment">// In the interface, but visible only in this library.</span></span><br><span class="line">  <span class="keyword">final</span> _name;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Not in the interface, since this is a constructor.</span></span><br><span class="line">  Person(<span class="keyword">this</span>._name);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// In the interface.</span></span><br><span class="line">  <span class="built_in">String</span> greet(<span class="built_in">String</span> who) =&gt; <span class="string">'Hello, <span class="subst">$who</span>. I am <span class="subst">$_name</span>.'</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// An implementation of the Person interface.</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Impostor</span> <span class="keyword">implements</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">  <span class="keyword">get</span> _name =&gt; <span class="string">''</span>;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">String</span> greet(<span class="built_in">String</span> who) =&gt; <span class="string">'Hi <span class="subst">$who</span>. Do you know who I am?'</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">String</span> greetBob(Person person) =&gt; person.greet(<span class="string">'Bob'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="built_in">print</span>(greetBob(Person(<span class="string">'Kathy'</span>)));</span><br><span class="line">  <span class="built_in">print</span>(greetBob(Impostor()));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是一个指定类实现多个接口的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> <span class="keyword">implements</span> <span class="title">Comparable</span>, <span class="title">Location</span> </span>&#123;...&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="扩展了一个类"><a href="#扩展了一个类" class="headerlink" title="扩展了一个类"></a>扩展了一个类</h4><p>可以使用<font color=green><em>extends</em></font>创建一个子类，其中<font color=green><em>super</em></font>指向superclass：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Television</span> </span>&#123;</span><br><span class="line">  <span class="keyword">void</span> turnOn() &#123;</span><br><span class="line">    _illuminateDisplay();</span><br><span class="line">    _activateIrSensor();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SmartTelevision</span> <span class="keyword">extends</span> <span class="title">Television</span> </span>&#123;</span><br><span class="line">  <span class="keyword">void</span> turnOn() &#123;</span><br><span class="line">    <span class="keyword">super</span>.turnOn();</span><br><span class="line">    _bootNetworkInterface();</span><br><span class="line">    _initializeMemory();</span><br><span class="line">    _upgradeApps();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="Overriding-members-成员覆写"><a href="#Overriding-members-成员覆写" class="headerlink" title="Overriding members 成员覆写"></a>Overriding members 成员覆写</h4><p>子类可以覆写实例成员、getters和setters。可以使用<font color=green><em>@override</em></font>来声明要覆写一个成员：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SmartTelevision</span> <span class="keyword">extends</span> <span class="title">Television</span> </span>&#123;</span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  <span class="keyword">void</span> turnOn() &#123;...&#125;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果要在<strong><a href="https://dart.dev/guides/language/sound-dart" target="_blank" rel="noopener">类型安全</a></strong>的代码中缩小（ narrow）方法参数或者实例变量的类型，可以使用<strong><a href="https://dart.dev/guides/language/sound-problems#the-covariant-keyword" target="_blank" rel="noopener">covariant关键字</a></strong>。</p>
<br>

<h4 id="Overridable-operators-操作符重载"><a href="#Overridable-operators-操作符重载" class="headerlink" title="Overridable operators 操作符重载"></a>Overridable operators 操作符重载</h4><p>你可以重载下表中列出的操作符，比如，如果定义了一个Vector类，可以定义<font color=green><em>+</em></font>操作符方法来实现两个向量的求和计算。</p>
<table>
<thead>
<tr>
<th><font color=green>*&lt;*</font></th>
<th><font color=green><em>+</em></font></th>
<th><font color=green><em>|</em></font></th>
<th><font color=green><strong>[ ]</strong></font></th>
</tr>
</thead>
<tbody><tr>
<td><font color=green><strong>&gt;</strong></font></td>
<td><font color=green><strong>/</strong></font></td>
<td><font color=green><strong>^</strong></font></td>
<td><font color=green><strong>[ ]=</strong></font></td>
</tr>
<tr>
<td><font color=green><strong>&lt;=</strong></font></td>
<td><font color=green><strong>~/</strong></font></td>
<td><font color=green><strong>&amp;</strong></font></td>
<td><font color=green><strong>~</strong></font></td>
</tr>
<tr>
<td><font color=green><strong>&gt;=</strong></font></td>
<td><font color=green><strong>*</strong></font></td>
<td><font color=green><strong>&lt;&lt;</strong></font></td>
<td><font color=green><strong>==</strong></font></td>
</tr>
<tr>
<td><font color=green><strong>-</strong></font></td>
<td><font color=green><strong>%</strong></font></td>
<td><font color=green><strong>&gt;&gt;</strong></font></td>
<td></td>
</tr>
</tbody></table>
<blockquote>
<p>注：你可能注意到 <font color=green><strong>!=</strong></font> 没有在表中，它不是可重载的操作符，因为表达式<font color=green><em>e1 != e2</em></font>只是<font color=green><em>!(e1 == e2)</em></font>的语法糖形式。</p>
</blockquote>
<p>下面是一个类中<font color=green><em>+</em></font>和<font color=green><em>-</em></font>操作符重载的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Vector</span> </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> <span class="built_in">int</span> x, y;</span><br><span class="line"></span><br><span class="line">  Vector(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line"></span><br><span class="line">  Vector <span class="keyword">operator</span> +(Vector v) =&gt; Vector(x + v.x, y + v.y);</span><br><span class="line">  Vector <span class="keyword">operator</span> -(Vector v) =&gt; Vector(x - v.x, y - v.y);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Operator == and hashCode not shown. For details, see note below.</span></span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">final</span> v = Vector(<span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">  <span class="keyword">final</span> w = Vector(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">assert</span>(v + w == Vector(<span class="number">4</span>, <span class="number">5</span>));</span><br><span class="line">  <span class="keyword">assert</span>(v - w == Vector(<span class="number">0</span>, <span class="number">1</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果你重载了<font color=green><em>==</em></font>操作符，你应该同时覆写对象的<font color=green><em>hashCode</em></font> getter方法。相关例子参见<strong><a href="https://dart.dev/guides/libraries/library-tour#implementing-map-keys" target="_blank" rel="noopener">Implementing map keys</a></strong>。</p>
<p>更多关于重载的内容可以参考<strong><a href="https://dart.dev/guides/language/language-tour#extending-a-class" target="_blank" rel="noopener">Extending a class</a></strong>。</p>
<br>

<h4 id="noSuchMethod"><a href="#noSuchMethod" class="headerlink" title="noSuchMethod()"></a>noSuchMethod()</h4><p>如果要检测或者反馈代码试图访问一个不存在的方法或者实例变量的情况，可以覆写<font color=green><em>noSuchMethod()</em></font>方法：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">  <span class="comment">// Unless you override noSuchMethod, using a</span></span><br><span class="line">  <span class="comment">// non-existent member results in a NoSuchMethodError.</span></span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  <span class="keyword">void</span> noSuchMethod(Invocation invocation) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'You tried to use a non-existent member: '</span> +</span><br><span class="line">        <span class="string">'<span class="subst">$&#123;invocation.memberName&#125;</span>'</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>你<strong>不能调用</strong>一个未实现的方法除非下列条件之一成立：</p>
<ul>
<li>接收方拥有静态类型<font color=green><em>dynamic</em></font></li>
<li>接收方拥有一个静态类型，该类型定义了未实现的方法（比如abstract方法），同时接收方的动态类型包含一个不同于<font color=green><em>Object</em></font>类的<font color=green><em>noSuchMethod()</em></font>方法实现。</li>
</ul>
<p>更多信息参见<strong><a href="https://github.com/dart-lang/sdk/blob/master/docs/language/informal/nosuchmethod-forwarding.md" target="_blank" rel="noopener">noSuchMethod forwarding specification</a></strong>。</p>
<br>

<h4 id="Extension-Methods-扩展方法"><a href="#Extension-Methods-扩展方法" class="headerlink" title="Extension Methods 扩展方法"></a>Extension Methods 扩展方法</h4><p>在Dart2.7引入的扩展方法是一种为已存在的库添加功能的方式。你可能在不知道的情况下已经使用了扩展方法。比如，当在IDE中使用代码补全时，IDE会在提示常规方法的同时显示扩展方法。</p>
<p>下面这个例子使用了<font color=green><em>String</em></font>类的名叫<font color=green><em>parseInt()</em></font>的扩展方法，该方法是在<font color=green><em>string_apis.dart</em></font>中定义的：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> string_apis.dart<span class="string">';</span></span><br><span class="line"><span class="string">...</span></span><br><span class="line"><span class="string">print('</span><span class="number">42</span><span class="string">'.padLeft(5)); // Use a String method.</span></span><br><span class="line"><span class="string">print('</span><span class="number">42</span><span class="string">'.parseInt()); // Use an extension method.</span></span><br></pre></td></tr></table></figure>

<p>使用和实现扩展方法的具体介绍可参考<strong><a href="https://dart.dev/guides/language/extension-methods" target="_blank" rel="noopener">extension methods page</a></strong>。</p>
<br>

<h4 id="Enumerated-types-枚举类型"><a href="#Enumerated-types-枚举类型" class="headerlink" title="Enumerated types 枚举类型"></a>Enumerated types 枚举类型</h4><p>枚举类型，通常称为 <em>enumerations</em> 或者 <em>enums</em>，是一种特殊的类型，用于表示固定数量的常量值。</p>
<br>

<h4 id="Using-enums-使用枚举"><a href="#Using-enums-使用枚举" class="headerlink" title="Using enums 使用枚举"></a>Using enums 使用枚举</h4><p>使用<font color=green><em>enum</em></font>关键字声明一个枚举类型：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Color &#123; red, green, blue &#125;</span><br></pre></td></tr></table></figure>

<p>枚举类型中的每个值都有一个<font color=green><em>index</em></font> getter，它会返回该值在枚举声明中的下标位置。比如，第一个值的index为0，第二个值的index为1。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">assert</span>(Color.red.index == <span class="number">0</span>);</span><br><span class="line"><span class="keyword">assert</span>(Color.green.index == <span class="number">1</span>);</span><br><span class="line"><span class="keyword">assert</span>(Color.blue.index == <span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<p>使用枚举的<font color=green><em>value</em></font>常量，可以获得包含枚举中所有值的列表。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">List</span>&lt;Color&gt; colors = Color.values;</span><br><span class="line"><span class="keyword">assert</span>(colors[<span class="number">2</span>] == Color.blue);</span><br></pre></td></tr></table></figure>

<p>可以在<strong><a href="https://dart.dev/guides/language/language-tour#switch-and-case" target="_blank" rel="noopener">switch语句</a></strong>中使用枚举，如果没有处理完枚举包含的所有的值，你会得到一个警告：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> aColor = Color.blue;</span><br><span class="line"></span><br><span class="line"><span class="keyword">switch</span> (aColor) &#123;</span><br><span class="line">  <span class="keyword">case</span> Color.red:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'Red as roses!'</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> Color.green:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">'Green as grass!'</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">default</span>: <span class="comment">// Without this, you see a WARNING.</span></span><br><span class="line">    <span class="built_in">print</span>(aColor); <span class="comment">// 'Color.blue'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>枚举类型有以下限制：</p>
<ul>
<li>不能子类化，mix in或者实现枚举</li>
<li>不能显式地初始化枚举</li>
</ul>
<p>更多信息参见<strong><a href="https://dart.dev/guides/language/spec" target="_blank" rel="noopener">Dart language specification</a></strong>。</p>
<br>

<h4 id="Adding-features-to-a-class-mixins"><a href="#Adding-features-to-a-class-mixins" class="headerlink" title="Adding features to a class: mixins"></a>Adding features to a class: mixins</h4><p>mixins是在多类层次结构中复用类代码的一种方式。</p>
<p>如果要用mixins，需要使用<font color=green><em>with</em></font>关键字，后跟一个或多个mixin名。下面的例子展示了使用mixins的两个类：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Musician</span> <span class="keyword">extends</span> <span class="title">Performer</span> <span class="title">with</span> <span class="title">Musical</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Maestro</span> <span class="keyword">extends</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">    <span class="title">with</span> <span class="title">Musical</span>, <span class="title">Aggressive</span>, <span class="title">Demented</span> </span>&#123;</span><br><span class="line">  Maestro(<span class="built_in">String</span> maestroName) &#123;</span><br><span class="line">    name = maestroName;</span><br><span class="line">    canConduct = <span class="keyword">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果要实现一个mixin，需要创建一个扩展自Object的类，且不要声明构造函数。除非要把mixin当做一个普通类来使用，否则就用<font color=green><em>mixin</em></font>关键字来代替<font color=green><em>class</em></font>。例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">mixin</span> Musical &#123;</span><br><span class="line">  <span class="built_in">bool</span> canPlayPiano = <span class="keyword">false</span>;</span><br><span class="line">  <span class="built_in">bool</span> canCompose = <span class="keyword">false</span>;</span><br><span class="line">  <span class="built_in">bool</span> canConduct = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">void</span> entertainMe() &#123;</span><br><span class="line">    <span class="keyword">if</span> (canPlayPiano) &#123;</span><br><span class="line">      <span class="built_in">print</span>(<span class="string">'Playing piano'</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (canConduct) &#123;</span><br><span class="line">      <span class="built_in">print</span>(<span class="string">'Waving hands'</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="built_in">print</span>(<span class="string">'Humming to self'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要指定只有某些类型可以使用mixin —— 这样mixin就可以调用它没有定义的方法 —— 使用<font color=green><em>on</em></font>来指定所需的superclass：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">mixin</span> MusicalPerformer <span class="keyword">on</span> Musician &#123;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>版本提示：<font color=green><em>mixin</em></font>关键字的支持是在Dart2.1的时候引入。而在早期release的代码中通常使用<font color=green><em>abstract class</em></font>来实现。关于2.1版本mixin相关变更内容可参考<strong><a href="https://github.com/dart-lang/sdk/blob/master/CHANGELOG.md" target="_blank" rel="noopener">Dart SDK changelog</a></strong>和<strong><a href="https://github.com/dart-lang/language/blob/master/accepted/2.1/super-mixins/feature-specification.md#dart-2-mixin-declarations" target="_blank" rel="noopener">2.1 mixin specification</a></strong>。</p>
</blockquote>
<br>

<h4 id="类变量和方法"><a href="#类变量和方法" class="headerlink" title="类变量和方法"></a>类变量和方法</h4><p>使用<font color=green><em>static</em></font>关键字可以实现类范围的变量和方法。</p>
<br>

<h4 id="静态变量"><a href="#静态变量" class="headerlink" title="静态变量"></a>静态变量</h4><p>静态变量（类变量）对于类范围内的状态和常量非常有用：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Queue</span> </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">const</span> initialCapacity = <span class="number">16</span>;</span><br><span class="line">  <span class="comment">// ···</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">assert</span>(Queue.initialCapacity == <span class="number">16</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>静态变量会在它们第一次被调用的时候才做初始化。</p>
<blockquote>
<p>注：这篇文章遵循了<strong><a href="https://dart.dev/guides/language/effective-dart/style#identifiers" target="_blank" rel="noopener">style guide recommendation</a></strong>，使用<font color=green><em>lowerCamelCase</em></font>方式为常量命名。</p>
</blockquote>
<br>

<h4 id="静态方法"><a href="#静态方法" class="headerlink" title="静态方法"></a>静态方法</h4><p>静态方法（类方法）不会对单个实例进行操作，因此不能访问<font color=green><em>this</em></font>。例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'dart:math'</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">  <span class="built_in">num</span> x, y;</span><br><span class="line">  Point(<span class="keyword">this</span>.x, <span class="keyword">this</span>.y);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">static</span> <span class="built_in">num</span> distanceBetween(Point a, Point b) &#123;</span><br><span class="line">    <span class="keyword">var</span> dx = a.x - b.x;</span><br><span class="line">    <span class="keyword">var</span> dy = a.y - b.y;</span><br><span class="line">    <span class="keyword">return</span> sqrt(dx * dx + dy * dy);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">var</span> a = Point(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line">  <span class="keyword">var</span> b = Point(<span class="number">4</span>, <span class="number">4</span>);</span><br><span class="line">  <span class="keyword">var</span> distance = Point.distanceBetween(a, b);</span><br><span class="line">  <span class="keyword">assert</span>(<span class="number">2.8</span> &lt; distance &amp;&amp; distance &lt; <span class="number">2.9</span>);</span><br><span class="line">  <span class="built_in">print</span>(distance);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：考虑使用全局函数而不是静态方法，来实现通用或者广泛使用的实用程序库和功能。</p>
</blockquote>
<p>可以使用静态方法作为编译时常量，比如，可以将静态方法作为参数传给常量构造函数。</p>
<br>

<h3 id="Generics-泛型"><a href="#Generics-泛型" class="headerlink" title="Generics 泛型"></a>Generics 泛型</h3><p>如果你看过基本数组类型 <strong><a href="https://api.dart.dev/stable/dart-core/List-class.html" target="_blank" rel="noopener">List</a></strong> 的API文档，你会发现实际类型是 <font color=green><em>List<E></em></font>。&lt;…&gt;表示法将List标记为泛型（或参数化）类型——具有正式类型参数的类型。<strong><a href="https://dart.dev/guides/language/effective-dart/design#do-follow-existing-mnemonic-conventions-when-naming-type-parameters" target="_blank" rel="noopener">按照惯例</a></strong>，大多数类型参数都以单字母来表示，比如E、T、S、K或者V等。</p>
<br>

<h4 id="为什么使用泛型"><a href="#为什么使用泛型" class="headerlink" title="为什么使用泛型"></a>为什么使用泛型</h4><p> 泛型常用于类型安全，但除了能让你的代码正常运行外还有更多的好处：</p>
<ul>
<li>正确指定泛型类型可以生成更好的代码</li>
<li>使用泛型可以减少重复代码</li>
</ul>
<p>如果你想要一个列表只包含字符串，可以用 <font color=green><em>List<String></em></font>（读作字符串列表）来声明这个列表。这样你、你的同事还有你使用的工具都可以在给这个列表添加非字符串元素时，发现这可能是个错误。例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> names = <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt;();</span><br><span class="line">names.addAll([<span class="string">'Seth'</span>, <span class="string">'Kathy'</span>, <span class="string">'Lars'</span>]);</span><br><span class="line">names.add(<span class="number">42</span>); <span class="comment">// Error</span></span><br></pre></td></tr></table></figure>

<p>使用泛型的另一个原因是减少代码重复。泛型可以让许多类型共享统一的接口和实现，同时还支持静态分析。比如，你为对象缓存创建了一个接口：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">ObjectCache</span> </span>&#123;</span><br><span class="line">  <span class="built_in">Object</span> getByKey(<span class="built_in">String</span> key);</span><br><span class="line">  <span class="keyword">void</span> setByKey(<span class="built_in">String</span> key, <span class="built_in">Object</span> value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当你发现需要这个接口的字符串版本时，你又创建了另一个接口：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">StringCache</span> </span>&#123;</span><br><span class="line">  <span class="built_in">String</span> getByKey(<span class="built_in">String</span> key);</span><br><span class="line">  <span class="keyword">void</span> setByKey(<span class="built_in">String</span> key, <span class="built_in">String</span> value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>后来，你决定还要支持这个接口的数值类型版本，你产生了一个想法。。。</p>
<p>泛型可以省去所有创建这些接口的麻烦，你可以创建一个带有类型参数的统一接口：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Cache</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">  T getByKey(<span class="built_in">String</span> key);</span><br><span class="line">  <span class="keyword">void</span> setByKey(<span class="built_in">String</span> key, T value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在这段代码里，T是一个替代类型（stand-in type），你可以把它看做是一个类型占位符，开发者会在使用时定义具体的类型。</p>
<br>

<h4 id="使用集合字面量"><a href="#使用集合字面量" class="headerlink" title="使用集合字面量"></a>使用集合字面量</h4><p>列表、集合和映射字面量支持参数化。参数化字面量和你之前看到的字面量很像，除了要在括号前加上<font color=green>*<type>*</font>（针对List和Set），或者<font color=green>*&lt;keyType, valueType&gt;*</font>（针对Map）。例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> names = &lt;<span class="built_in">String</span>&gt;[<span class="string">'Seth'</span>, <span class="string">'Kathy'</span>, <span class="string">'Lars'</span>];</span><br><span class="line"><span class="keyword">var</span> uniqueNames = &lt;<span class="built_in">String</span>&gt;&#123;<span class="string">'Seth'</span>, <span class="string">'Kathy'</span>, <span class="string">'Lars'</span>&#125;;</span><br><span class="line"><span class="keyword">var</span> pages = &lt;<span class="built_in">String</span>, <span class="built_in">String</span>&gt;&#123;</span><br><span class="line">  <span class="string">'index.html'</span>: <span class="string">'Homepage'</span>,</span><br><span class="line">  <span class="string">'robots.txt'</span>: <span class="string">'Hints for web robots'</span>,</span><br><span class="line">  <span class="string">'humans.txt'</span>: <span class="string">'We are people, not machines'</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="在构造函数中使用参数化类型"><a href="#在构造函数中使用参数化类型" class="headerlink" title="在构造函数中使用参数化类型"></a>在构造函数中使用参数化类型</h4><p>使用构造函数时可以指定一个或多个类型，要将类型放在类名后的尖括号中。例如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> nameSet = <span class="built_in">Set</span>&lt;<span class="built_in">String</span>&gt;.from(names);</span><br></pre></td></tr></table></figure>

<p>下面的代码创建了一个Map类，包含的key类型为整型，value类型为View：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> views = <span class="built_in">Map</span>&lt;<span class="built_in">int</span>, View&gt;();</span><br></pre></td></tr></table></figure>

<br>

<h4 id="泛型集合以及它们包含的类型"><a href="#泛型集合以及它们包含的类型" class="headerlink" title="泛型集合以及它们包含的类型"></a>泛型集合以及它们包含的类型</h4><p>Dart泛型是具体化的（refied），这意味着泛型在运行时携带着具体的类型信息。比如你可以检测集合的类型：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> names = <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt;();</span><br><span class="line">names.addAll([<span class="string">'Seth'</span>, <span class="string">'Kathy'</span>, <span class="string">'Lars'</span>]);</span><br><span class="line"><span class="built_in">print</span>(names <span class="keyword">is</span> <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt;); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：相反地，在Java中泛型使用的是擦除机制，这意味着在运行时泛型类型参数会被删除。Java中你可以检测一个对象是不是List，但不能判断它是不是 <font color=green><em>List<String></em></font>。</p>
</blockquote>
<br>

<h4 id="对参数化类型的限定"><a href="#对参数化类型的限定" class="headerlink" title="对参数化类型的限定"></a>对参数化类型的限定</h4><p>在实现一个泛型时，你可以使用 <font color=green><em>extends</em></font> 对它的参数化类型做限定。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">SomeBaseClass</span>&gt; </span>&#123;</span><br><span class="line">  <span class="comment">// Implementation goes here...</span></span><br><span class="line">  <span class="built_in">String</span> toString() =&gt; <span class="string">"Instance of 'Foo&lt;<span class="subst">$T</span>&gt;'"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Extender</span> <span class="keyword">extends</span> <span class="title">SomeBaseClass</span> </span>&#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>这里可以使用 <font color=green><em>SomeBaseClass</em></font> 或者它的任何一个子类作为泛型的类型参数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> someBaseClassFoo = Foo&lt;SomeBaseClass&gt;();</span><br><span class="line"><span class="keyword">var</span> extenderFoo = Foo&lt;Extender&gt;();</span><br></pre></td></tr></table></figure>

<p>也可以不指定泛型参数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo = Foo();</span><br><span class="line"><span class="built_in">print</span>(foo); <span class="comment">// Instance of 'Foo&lt;SomeBaseClass&gt;'</span></span><br></pre></td></tr></table></figure>

<p>指定任何一个非 <font color=green><em>SomeBaseClass</em></font> 类型都会导致错误：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo = Foo&lt;<span class="built_in">Object</span>&gt;();</span><br></pre></td></tr></table></figure>

<br>

<h4 id="使用泛型方法"><a href="#使用泛型方法" class="headerlink" title="使用泛型方法"></a>使用泛型方法</h4><p>最开始Dart泛型支持仅限于类。后来引入了新的语法叫 <em>generic methods</em>，允许方法和函数也支持类型参数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">T first&lt;T&gt;(<span class="built_in">List</span>&lt;T&gt; ts) &#123;</span><br><span class="line">  <span class="comment">// Do some initial work or error checking, then...</span></span><br><span class="line">  T tmp = ts[<span class="number">0</span>];</span><br><span class="line">  <span class="comment">// Do some additional checking or processing...</span></span><br><span class="line">  <span class="keyword">return</span> tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中, <font color=green><em>first<T></em></font> 的泛型类型允许在以下几个地方使用类型参数 <font color=green><em>T</em></font> ：</p>
<ul>
<li>函数的返回值类型（<font color=green><em>T</em></font>）</li>
<li>参数类型 （<font color=green><em>List<T></em></font>）</li>
<li>局部变量类型（<font color=green><em>T tmp</em></font>）</li>
</ul>
<p>关于泛型的更多信息参见<strong><a href="https://github.com/dart-lang/sdk/blob/master/pkg/dev_compiler/doc/GENERIC_METHODS.md" target="_blank" rel="noopener">Using Generic Methods</a></strong>。</p>
<br>

<h3 id="Library-and-visibility-库和可见性"><a href="#Library-and-visibility-库和可见性" class="headerlink" title="Library and visibility 库和可见性"></a>Library and visibility 库和可见性</h3><p><font color=green><em>import</em></font> 和 <font color=green><em>library</em></font> 指令能帮你创建模块和共享代码库。库不仅是提供API，而且还是个私域单元：以下划线（<font color=green><em>_</em></font>）开头的标识符仅对库内可见。<em>每一个Dart app都是一个库<em>，即使你没有使用 <font color=green></em>library</em></font> 指令。</p>
<p>库可以通过<strong><a href="https://dart.dev/guides/packages" target="_blank" rel="noopener">packages</a></strong>来分发。</p>
<br>

<h4 id="关于库的使用"><a href="#关于库的使用" class="headerlink" title="关于库的使用"></a>关于库的使用</h4><p>可以使用 <font color=green><em>import</em></font> 指定如何在一个库的范围内使用另一个库的命名空间。</p>
<p>比如，Dart web app通常会引用 <strong><a href="https://api.dart.dev/stable/dart-html" target="_blank" rel="noopener">dart:html</a></strong> 库，可以像下面这样来import：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'dart:html'</span>;</span><br></pre></td></tr></table></figure>

<p><font color=green><em>import</em></font> 需要的唯一参数是指向库的URI。对于内建库，URI会有一个特殊的scheme —— <font color=green><em>dart:</em></font>。对于其他的库，可以使用文件路径或者scheme <font color=green><em>package:</em></font>。scheme <font color=green><em>package:</em></font> 指定了所import的库是由包管理器（比如pub工具）提供的。例子如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'package:test/test.dart'</span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：URI全称Uniform Resource Identifier，统一资源标识符。URL（Uniform Resource Locator，统一资源定位器）是一种常见的URI。</p>
</blockquote>
<br>

<h4 id="指定库前缀"><a href="#指定库前缀" class="headerlink" title="指定库前缀"></a>指定库前缀</h4><p>如果导入的多个库有标识符冲突，你可以为其中一个或多个指定前缀。比如，库1和库2都有一个Element类，解决冲突的代码如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'package:lib1/lib1.dart'</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">'package:lib2/lib2.dart'</span> <span class="keyword">as</span> lib2;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Uses Element from lib1.</span></span><br><span class="line"><span class="built_in">Element</span> element1 = <span class="built_in">Element</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Uses Element from lib2.</span></span><br><span class="line">lib2.<span class="built_in">Element</span> element2 = lib2.<span class="built_in">Element</span>();</span><br></pre></td></tr></table></figure>

<br>

<h4 id="导入库的一部分"><a href="#导入库的一部分" class="headerlink" title="导入库的一部分"></a>导入库的一部分</h4><p>如果只需要使用库中的一部分内容，你可以像下面代码这样有选择的导入：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Import only foo.</span></span><br><span class="line"><span class="keyword">import</span> <span class="string">'package:lib1/lib1.dart'</span> <span class="keyword">show</span> foo;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Import all names EXCEPT foo.</span></span><br><span class="line"><span class="keyword">import</span> <span class="string">'package:lib2/lib2.dart'</span> <span class="keyword">hide</span> foo;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="库的懒加载"><a href="#库的懒加载" class="headerlink" title="库的懒加载"></a>库的懒加载</h4><p>延迟加载（也叫懒加载）允许web app按需加载库。下面是一些例子：</p>
<ul>
<li>减少web app的初始启动时间</li>
<li>执行A/B测试时，尝试算法的一个替代实现</li>
<li>加载很少使用的功能，比如可选的窗口和对话框</li>
</ul>
<blockquote>
<p>注：<strong>只有dart2js支持延迟加载</strong>。Flutter、Dart VM以及dartdevc都不支持。</p>
</blockquote>
<p>使用延迟加载一个库时，必须首先使用 <font color=green><em>deferred as</em></font> 来导入它。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'package:greetings/hello.dart'</span> <span class="keyword">deferred</span> <span class="keyword">as</span> hello;</span><br></pre></td></tr></table></figure>

<p>当你需要库时，使用库的标识符来调用 <font color=green><em>loadLibrary()</em></font> 方法。 </p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Future greet() <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">await</span> hello.loadLibrary();</span><br><span class="line">  hello.printGreeting();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的代码中，<font color=green><em>await</em></font> 关键字会让函数暂停执行，直到库加载完成为止。关于 <font color=green><em>async</em></font> 和 <font color=green><em>await</em></font> 的更多信息参见<strong><a href="https://dart.dev/guides/language/language-tour#asynchrony-support" target="_blank" rel="noopener">asynchrony support</a></strong>。</p>
<p>针对同一个库你可以调用 <font color=green><em>loadLibrary()</em></font> 很多次，都不会有问题。库只会被加载一次。</p>
<p>在使用延迟加载时，要记住下面几点：</p>
<ul>
<li>Deferred库的常量不是导入文件中的常量，这些常量在deferred库加载完成前是不存在的。</li>
<li>不能在导入文件中使用来自deferred库的类型，相反，可以考虑将接口类型移到由deferred库和导入文件共同import的库中。</li>
<li>Dart隐式地将 <font color=green><em>loadLibrary()</em></font> 插入到使用 <font color=green><em>deferred as namespace</em></font> 定义的命名空间中。<font color=green><em>loadLibrary()</em></font> 调用会返回一个<strong><a href="https://dart.dev/guides/libraries/library-tour#future" target="_blank" rel="noopener">Future</a></strong>对象。</li>
</ul>
<br>

<h4 id="关于库的实现"><a href="#关于库的实现" class="headerlink" title="关于库的实现"></a>关于库的实现</h4><p>参考<strong><a href="https://dart.dev/guides/libraries/create-library-packages" target="_blank" rel="noopener">Create Library Packages</a></strong>中如何实现library package的建议，包括了以下几点：</p>
<ul>
<li>如何组织库源码</li>
<li>如何使用 <font color=green><em>export</em></font> 指令</li>
<li>何时使用 <font color=green><em>part</em></font> 指令</li>
<li>何时使用 <font color=green><em>library</em></font> 指令</li>
<li>如何使用条件导入、导出来实现支持多平台的库</li>
</ul>
<br>

<h4 id="Asynchrony-support-异步支持"><a href="#Asynchrony-support-异步支持" class="headerlink" title="Asynchrony support 异步支持"></a>Asynchrony support 异步支持</h4><p>Dart库包含了很多函数，都是返回 <strong><a href="https://api.dart.dev/stable/dart-async/Future-class.html" target="_blank" rel="noopener">Future</a></strong> 和 <strong><a href="https://api.dart.dev/stable/dart-async/Stream-class.html" target="_blank" rel="noopener">Stream</a></strong> 对象。这些函数是<em>异步的</em>：它们在创建可能的耗时操作（比如I/O）之后，不用等待操作结束就即刻返回。</p>
<p> 关键字 <font color=green><em>async</em></font> 和 <font color=green><em>await</em></font> 用于支持异步编程，让你以同步代码的方式来编写异步代码。</p>
<br>

<h4 id="Handling-Future"><a href="#Handling-Future" class="headerlink" title="Handling Future"></a>Handling Future</h4><p>当你需要获取一个Future对象的完成结果时，可以有两个选择：</p>
<ul>
<li>使用 <font color=green><em>async</em></font> 和 <font color=green><em>await</em></font> </li>
<li>使用Future API，具体描述见 <strong><a href="https://dart.dev/guides/libraries/library-tour#future" target="_blank" rel="noopener">in the library tour</a></strong></li>
</ul>
<p>使用 <font color=green><em>async</em></font> 和 <font color=green><em>await</em></font> 的代码是异步的，但看起来像同步代码。比如下面的代码使用了 <font color=green><em>await</em></font> 来等待异步函数的执行结果：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">await</span> lookUpVersion();</span><br></pre></td></tr></table></figure>

<p>使用了 <font color=green><em>await</em></font> 的代码必须包含在 <font color=green><em>async</em></font> 函数中——以 <font color=green><em>async</em></font> 标记的函数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Future checkVersion() <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="keyword">var</span> version = <span class="keyword">await</span> lookUpVersion();</span><br><span class="line">  <span class="comment">// Do something with version</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：虽然 <font color=green><em>async</em></font> 函数可能执行耗时的操作，但是它不会等待这些操作执行，相反，<font color=green><em>async</em></font> 函数只执行到第一个 <font color=green><em>await</em></font> 表达式（<strong><a href="https://github.com/dart-lang/sdk/blob/master/docs/newsletter/20170915.md#synchronous-async-start" target="_blank" rel="noopener">细节介绍</a></strong>）为止。它会返回一个Future对象，仅在<font color=green><em>await</em></font> 表达式完成后再继续执行。</p>
</blockquote>
<p>使用 <font color=green><em>try</em></font>、 <font color=green><em>catch</em></font>和 <font color=green><em>finally</em></font>来处理使用<font color=green><em>await</em></font>代码中产生的错误和清除工作：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  version = <span class="keyword">await</span> lookUpVersion();</span><br><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">  <span class="comment">// React to inability to look up the version</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在一个 <font color=green><em>async</em></font> 函数中，可能会多次调用 <font color=green><em>await</em></font>。比如，下面的代码中为了函数结果要等待三次：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> entrypoint = <span class="keyword">await</span> findEntrypoint();</span><br><span class="line"><span class="keyword">var</span> exitCode = <span class="keyword">await</span> runExecutable(entrypoint, args);</span><br><span class="line"><span class="keyword">await</span> flushThenExit(exitCode);</span><br></pre></td></tr></table></figure>

<p>对于 <font color=green><em>await</em></font> 表达式（<font color=green><em>await expression</em></font>），<font color=green><em>expression</em></font>的值通常是一个Future对象。如果返回值是其他类型，则自动封装为一个Future对象。这个Future对象表明承诺返回一个对象，也就是整个<font color=green><em>await</em></font> 表达式（<font color=green><em>await expression</em></font>）的值。await表达式会使执行暂停，直到这个对象可用为止。</p>
<p>如果你在使用 <font color=green><em>await</em></font> 时得到了一个编译时错误，请检查 <font color=green><em>await</em></font> 是否被放到一个 <font color=green><em>async</em></font> 函数中。比如，如果在app的 <font color=green><em>main()</em></font> 函数中使用了 <font color=green><em>await</em></font> ，<font color=green><em>main()</em></font> 的函数体必须被标记为<font color=green><em>async</em></font>：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Future main() <span class="keyword">async</span> &#123;</span><br><span class="line">  checkVersion();</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'In main: version is <span class="subst">$&#123;await lookUpVersion()&#125;</span>'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="声明异步函数"><a href="#声明异步函数" class="headerlink" title="声明异步函数"></a>声明异步函数</h4><p>函数体被<font color=green><em>async</em></font>修饰符标记的函数被称为<font color=green><em>async</em></font>函数。</p>
<p>给函数添加<font color=green><em>async</em></font>关键字可以使该函数返回一个Future对象，比如，考虑一个返回字符串的同步函数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span> lookUpVersion() =&gt; <span class="string">'1.0.0'</span>;</span><br></pre></td></tr></table></figure>

<p>如果将其改为<font color=green><em>async</em></font>函数——因为未来的实现是一个耗时操作——则返回值为一个Future对象。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Future&lt;<span class="built_in">String</span>&gt; lookUpVersion() <span class="keyword">async</span> =&gt; <span class="string">'1.0.0'</span>;</span><br></pre></td></tr></table></figure>

<p>注意这里的函数体不需要调用Future的API，Dart会在必要的时候自动创建Future对象。如果函数不会返回可用值，则返回值的类型为<font color=green><em>Future<Void></em></font>。关于使用futures、 <font color=green><em>async</em></font> 和 <font color=green><em>await</em></font> 的交互介绍，可参考<strong><a href="https://dart.dev/codelabs/async-await" target="_blank" rel="noopener">asynchronous programming codelab</a></strong>。</p>
<br>

<h4 id="Handling-Streams"><a href="#Handling-Streams" class="headerlink" title="Handling Streams"></a>Handling Streams</h4><p>当需要从Stream中获取值时，可以有两个选择：</p>
<ul>
<li>使用<font color=green><em>async</em></font>和一个异步循环（<font color=green><em>await for</em></font>）</li>
<li>使用Stream API，具体描述见 <strong><a href="https://dart.dev/guides/libraries/library-tour#stream" target="_blank" rel="noopener">in the library tour</a></strong></li>
</ul>
<blockquote>
<p>注：在使用<font color=green><em>await for</em></font>前，要确保它能使代码更加清晰，并且确实希望等待所有流的结果。比如，通常<font color=green><em>await for</em></font>不会用于UI事件监听，因为UI框架的事件流会持续发送，不会结束。</p>
</blockquote>
<p>异步for循环的形式如下：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">await</span> <span class="keyword">for</span> (varOrType identifier <span class="keyword">in</span> expression) &#123;</span><br><span class="line">  <span class="comment">// Executes each time the stream emits a value.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中<font color=green><em>expression</em></font>的值必须是Stream类型。执行过程如下：</p>
<ol>
<li>等待直到stream发出一个值</li>
<li>将变量设置为stream发出的值，执行for-loop循环体</li>
<li>重复1和2直到stream关闭</li>
</ol>
<p>如果要停止对stream的监听，可以调用<font color=green><em>break</em></font>和<font color=green><em>return</em></font>语句跳出for循环，并取消对流的订阅。</p>
<p>如果你在实现异步循环时得到了一个编译时错误，请检查<font color=green><em>await for</em></font>是否被放到一个 <font color=green><em>async</em></font> 函数中。比如，如果要在app的<font color=green><em>main()</em></font> 函数中使用异步循环，<font color=green><em>main()</em></font> 的函数体必须被标记为<font color=green><em>async</em></font>：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Future main() <span class="keyword">async</span> &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="keyword">await</span> <span class="keyword">for</span> (<span class="keyword">var</span> request <span class="keyword">in</span> requestServer) &#123;</span><br><span class="line">    handleRequest(request);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关于异步编程的更多信息，可以参考<strong><a href="https://dart.dev/guides/libraries/library-tour#dartasync---asynchronous-programming" target="_blank" rel="noopener">dart:async</a></strong>部分。</p>
<br>

<h3 id="Generators-生成器"><a href="#Generators-生成器" class="headerlink" title="Generators 生成器"></a>Generators 生成器</h3><p>当需要延时创建一系列值时，可以考虑使用生成器函数（<em>generator function</em>）。Dart原生支持两种生成器函数：</p>
<ul>
<li>同步生成器（<strong>Synchronous generator</strong>）：返回一个<strong><a href="https://api.dart.dev/stable/dart-core/Iterable-class.html" target="_blank" rel="noopener">Iterable</a></strong>对象；</li>
<li>异步生成器（<strong>Asynchronous generator</strong>）：返回一个<strong><a href="https://api.dart.dev/stable/dart-async/Stream-class.html" target="_blank" rel="noopener">Stream</a></strong>对象；</li>
</ul>
<p>要实现一个同步生成器函数，需要将函数体标记为<font color=green><em>sync**</font>，并使用<font color=green>*yield</em></font>语句返回值:</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Iterable</span>&lt;<span class="built_in">int</span>&gt; naturalsTo(<span class="built_in">int</span> n) <span class="keyword">sync</span>* &#123;</span><br><span class="line">  <span class="built_in">int</span> k = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (k &lt; n) <span class="keyword">yield</span> k++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要实现一个异步生成器函数，需要将函数体标记为<font color=green><em>async**</font>，并使用<font color=green>*yield</em></font>语句返回值:</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line">Stream&lt;<span class="built_in">int</span>&gt; asynchronousNaturalsTo(<span class="built_in">int</span> n) <span class="keyword">async</span>* &#123;</span><br><span class="line">  <span class="built_in">int</span> k = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (k &lt; n) <span class="keyword">yield</span> k++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果生成器是递归式的，可以通过使用<font color=green><em>yield*</em></font>改进它的性能：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Iterable</span>&lt;<span class="built_in">int</span>&gt; naturalsDownFrom(<span class="built_in">int</span> n) <span class="keyword">sync</span>* &#123;</span><br><span class="line">  <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">yield</span> n;</span><br><span class="line">    <span class="keyword">yield</span>* naturalsDownFrom(n - <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h3 id="Callable-Classes"><a href="#Callable-Classes" class="headerlink" title="Callable Classes"></a>Callable Classes</h3><p>在Dart中，如果要允许类的实例能够像函数一样被调用，可以通过实现类的<font color=green><em>call()</em></font>来达到。</p>
<p>下面这个例子中，<font color=green><em>WannabeFunction</em></font>类定义了一个call函数，将三个字符串通过空格连接起来，并附加一个感叹号。（具体内容略）</p>
<br>

<h3 id="Isolates-隔离器"><a href="#Isolates-隔离器" class="headerlink" title="Isolates 隔离器"></a>Isolates 隔离器</h3><p>多数计算机，即使在移动平台上，都支持多核CPU。为了充分利用多核性能，开发人员通常使用并发运行的共享内存线程。然而，共享并发状态很容易出错，同时可能会导致代码过于复杂。而在Dart中，所有代码都运行在隔离器（<em>isolates</em>）内部，而不是线程中。每个隔离器都有它自己的存储堆，而确保隔离器的状态不会被其他隔离器访问到。</p>
<p>更多内容可以参考下面的链接：</p>
<ul>
<li><strong><a href="https://medium.com/dartlang/dart-asynchronous-programming-isolates-and-event-loops-bffc3e296a6a" target="_blank" rel="noopener">Dart asynchronous programming: Isolates and event loops</a></strong>；</li>
<li><strong><a href="https://api.dart.dev/stable/dart-isolate" target="_blank" rel="noopener">dart:isolate API reference</a></strong>，包括<strong><a href="https://api.dart.dev/stable/dart-isolate/Isolate/spawn.html" target="_blank" rel="noopener">Isolate.spawn()</a></strong>和<strong><a href="https://api.dart.dev/stable/dart-isolate/TransferableTypedData-class.html" target="_blank" rel="noopener">TransferableTypedData</a></strong>；</li>
<li>flutter网站上的<strong><a href="https://flutter.dev/docs/cookbook/networking/background-parsing" target="_blank" rel="noopener">Background parsing</a></strong> cookbook；</li>
<li><strong><a href="https://github.com/flutter/samples/tree/master/isolate_example" target="_blank" rel="noopener">Isolate sample app</a></strong>；</li>
</ul>
<br>

<h3 id="Typedef"><a href="#Typedef" class="headerlink" title="Typedef"></a>Typedef</h3><p>在Dart中，函数和字符串、数值一样都是对象。<em>typedef</em>或者函数类型别名，为函数提供了一个类型名，你可以在声明字段或者返回类型的时候使用。</p>
<p>当将函数类型赋值给变量时，typedef会保留相关的类型信息。</p>
<p>考虑下面这段代码，这里没有使用typedef：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SortedCollection</span> </span>&#123;</span><br><span class="line">  <span class="built_in">Function</span> compare;</span><br><span class="line"></span><br><span class="line">  SortedCollection(<span class="built_in">int</span> f(<span class="built_in">Object</span> a, <span class="built_in">Object</span> b)) &#123;</span><br><span class="line">    compare = f;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Initial, broken implementation.</span></span><br><span class="line"><span class="built_in">int</span> sort(<span class="built_in">Object</span> a, <span class="built_in">Object</span> b) =&gt; <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  SortedCollection coll = SortedCollection(sort);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// All we know is that compare is a function,</span></span><br><span class="line">  <span class="comment">// but what type of function?</span></span><br><span class="line">  <span class="keyword">assert</span>(coll.compare <span class="keyword">is</span> <span class="built_in">Function</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类型信息在将<font color=green><em>f</em></font>赋值给<font color=green><em>compare</em></font>时丢掉了。这段代码中<font color=green><em>f</em></font>的类型是<font color=green><em>(Object, Object)  → int</em></font>（这里→意思是返回）， 而<font color=green><em>compare</em></font>的类型是Function。如果更改代码以使用显式命名并且保留类型信息，开发者和工具就可以使用这些信息了。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> Compare = <span class="built_in">int</span> <span class="built_in">Function</span>(<span class="built_in">Object</span> a, <span class="built_in">Object</span> b);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SortedCollection</span> </span>&#123;</span><br><span class="line">  Compare compare;</span><br><span class="line"></span><br><span class="line">  SortedCollection(<span class="keyword">this</span>.compare);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Initial, broken implementation.</span></span><br><span class="line"><span class="built_in">int</span> sort(<span class="built_in">Object</span> a, <span class="built_in">Object</span> b) =&gt; <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  SortedCollection coll = SortedCollection(sort);</span><br><span class="line">  <span class="keyword">assert</span>(coll.compare <span class="keyword">is</span> <span class="built_in">Function</span>);</span><br><span class="line">  <span class="keyword">assert</span>(coll.compare <span class="keyword">is</span> Compare);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注：当前typedef仅限用于函数，我们期待后面有所改变。</p>
</blockquote>
<br>

<p>因为typedef只是简单的别名，它提供了一种检查任何函数类型的方法。比如：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> Compare&lt;T&gt; = <span class="built_in">int</span> <span class="built_in">Function</span>(T a, T b);</span><br><span class="line"></span><br><span class="line"><span class="built_in">int</span> sort(<span class="built_in">int</span> a, <span class="built_in">int</span> b) =&gt; a - b;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="keyword">assert</span>(sort <span class="keyword">is</span> Compare&lt;<span class="built_in">int</span>&gt;); <span class="comment">// True!</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h3 id="Metadata"><a href="#Metadata" class="headerlink" title="Metadata"></a>Metadata</h3><p>可以使用metadata为你的代码添加附加信息。一个metadata注解以<font color=green><em>@</em></font>开头，后面跟着对编译时常量的引用（比如<font color=green><em>deprecated</em></font>），或者对常量构造函数的调用。</p>
<p>两个对所有Dart代码有效的注解：<font color=green><em>@deprecated</em></font>和<font color=green><em>@override</em></font>。比如<strong><a href="https://dart.dev/guides/language/language-tour#extending-a-class" target="_blank" rel="noopener">Extending a class</a></strong>章节中对<font color=green><em>@override</em></font>的使用，下面是使用<font color=green><em>@deprecated</em></font>注解的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Television</span> </span>&#123;</span><br><span class="line">  <span class="comment">/// <span class="markdown"><span class="emphasis">_Deprecated: Use [turnOn] instead._</span></span></span></span><br><span class="line">  <span class="meta">@deprecated</span></span><br><span class="line">  <span class="keyword">void</span> activate() &#123;</span><br><span class="line">    turnOn();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/// <span class="markdown">Turns the TV's power on.</span></span></span><br><span class="line">  <span class="keyword">void</span> turnOn() &#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>你可以定义自己的metadata注解，下面是一个带有两个参数的自定义注解@todo的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">library</span> todo;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Todo</span> </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> <span class="built_in">String</span> who;</span><br><span class="line">  <span class="keyword">final</span> <span class="built_in">String</span> what;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> Todo(<span class="keyword">this</span>.who, <span class="keyword">this</span>.what);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是使用@todo注解的例子：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'todo.dart'</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Todo</span>(<span class="string">'seth'</span>, <span class="string">'make this do something'</span>)</span><br><span class="line"><span class="keyword">void</span> doSomething() &#123;</span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'do something'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>metadata可以放在库、类、typedef、类型参数、构造函数、factory、函数、字段、参数或者变量声明之前，也可以放在import和export指令之前。可以通过反射在运行时提取metadata信息。</p>
<br>

<h3 id="Comments-注释"><a href="#Comments-注释" class="headerlink" title="Comments 注释"></a>Comments 注释</h3><p>Dart支持单行注释、多行注释，以及文档式注释。</p>
<h4 id="单行注释"><a href="#单行注释" class="headerlink" title="单行注释"></a>单行注释</h4><p>单行注释以<font color=green><em>//</em></font>开头，自<font color=green><em>//</em></font>起到行末的所有字符都会被Dart编译器略掉。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="comment">// <span class="doctag">TODO:</span> refactor into an AbstractLlamaGreetingFactory?</span></span><br><span class="line">  <span class="built_in">print</span>(<span class="string">'Welcome to my Llama farm!'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="多行注释"><a href="#多行注释" class="headerlink" title="多行注释"></a>多行注释</h4><p>多行注释以<font color=green><em>/**</font>开头，以<font color=green>*\</em>/*</font>结尾，这之间的字符都会被Dart编译器略掉（除非是文档式注释，具体看下一章节）。多行注释支持嵌套。</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> main() &#123;</span><br><span class="line">  <span class="comment">/*</span></span><br><span class="line"><span class="comment">   * This is a lot of work. Consider raising chickens.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">  Llama larry = Llama();</span></span><br><span class="line"><span class="comment">  larry.feed();</span></span><br><span class="line"><span class="comment">  larry.exercise();</span></span><br><span class="line"><span class="comment">  larry.clean();</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<br>

<h4 id="文档式注释"><a href="#文档式注释" class="headerlink" title="文档式注释"></a>文档式注释</h4><p>文档式注释可以是单行，也可以是多行，以<font color=green><em>///</em></font>或者<font color=green><em>/***</font>开头。使用<font color=green></em>///*</font>对连续行注释，与多行doc注释有相同的效果。</p>
<p>在文档式注释中，Dart编译器会忽略掉所有文本，除非它被括在方括号中。使用方括号可以引用类、方法、字段、全局变量、函数以及参数。方括号中的名字会在文档化程序元素的词法范围内做解析。</p>
<p>下面这个文档化注释的例子引用了另外的类和参数：</p>
<figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// <span class="markdown">A domesticated South American camelid (Lama glama).</span></span></span><br><span class="line"><span class="comment">///</span></span><br><span class="line"><span class="comment">/// <span class="markdown">Andean cultures have used llamas as meat and pack</span></span></span><br><span class="line"><span class="comment">/// <span class="markdown">animals since pre-Hispanic times.</span></span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Llama</span> </span>&#123;</span><br><span class="line">  <span class="built_in">String</span> name;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/// <span class="markdown">Feeds your llama [Food].</span></span></span><br><span class="line">  <span class="comment">///</span></span><br><span class="line">  <span class="comment">/// <span class="markdown">The typical llama eats one bale of hay per week.</span></span></span><br><span class="line">  <span class="keyword">void</span> feed(Food food) &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/// <span class="markdown">Exercises your llama with an [activity] for</span></span></span><br><span class="line">  <span class="comment">/// <span class="markdown">[timeLimit] minutes.</span></span></span><br><span class="line">  <span class="keyword">void</span> exercise(Activity activity, <span class="built_in">int</span> timeLimit) &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在生成的文档中，<font color=green><em>[food]</em></font>会转换为一个链接，指向Food类的API文档。</p>
<p>如果要解析Dart代码，生成HTML文档，可以使用SDK中的<strong><a href="https://github.com/dart-lang/dartdoc#dartdoc" target="_blank" rel="noopener">documentation generation tool</a></strong>。具体生成文档的例子可以参考<strong><a href="https://api.dart.dev/stable" target="_blank" rel="noopener">Dart API documentation</a></strong>。关于如何结构化注释的建议，可以参考<strong><a href="https://dart.dev/guides/language/effective-dart/documentation" target="_blank" rel="noopener">Guidelines for Dart Doc Comments</a></strong>。</p>
<br>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>这篇文章总结了Dart语言中常用的特性，更多的新特性还在实现中，但我们希望新特性不会破坏现有的代码。更多内容可以参考<strong><a href="https://dart.dev/guides/language/spec" target="_blank" rel="noopener">Dart language specification</a></strong>和<strong><a href="https://dart.dev/guides/language/effective-dart" target="_blank" rel="noopener">Effective Dart</a></strong>。想要了解Dart核心库，可以参考<strong><a href="https://dart.dev/guides/libraries/library-tour" target="_blank" rel="noopener">A Tour of the Dart Libraries</a></strong>。</p>
]]></content>
      <categories>
        <category>编程艺术</category>
      </categories>
      <tags>
        <tag>Dart</tag>
        <tag>Programming Language</tag>
      </tags>
  </entry>
  <entry>
    <title>关于车载智能终端产品方向的一些思考</title>
    <url>/2020/01/11/infotainment/</url>
    <content><![CDATA[<blockquote>
<p><font size=3>这里讨论的范围局限于infotainment终端产品。一年多之前的一些想法，看看未来几年能否得到验证。</font></p>
</blockquote>
<h4 id="看法一：UI系统关注点在于基于硬件的基础能力，核心通用功能，以及极致体验。"><a href="#看法一：UI系统关注点在于基于硬件的基础能力，核心通用功能，以及极致体验。" class="headerlink" title="看法一：UI系统关注点在于基于硬件的基础能力，核心通用功能，以及极致体验。"></a>看法一：UI系统关注点在于基于硬件的基础能力，核心通用功能，以及极致体验。</h4><ul>
<li>所有智能终端的框架都是：硬件、系统平台、应用和服务。终端研发负责整合硬件和软件系统，并通过SDK的方式开放平台能力，吸引第三方开发者开发各种应用和服务，通过应用商店模式提供给终端用户下载使用。这里的关键是围绕系统平台和应用商店建立的生态圈。所以说苹果公司最伟大的发明不是iPhone而是App Store，苹果建立了智能硬件整个生态的商业模式。</li>
<li>UI系统在其中扮演的角色是向用户提供最基础的功能和平台能力，以及优秀的视觉和交互体验。</li>
<li>通过iPhone的整个产品发展过程来看，产品发展的两个推动点是硬件创新以及设计迭代。iOS研发团队在功能上秉着克制的原则，聚焦核心基础功能，并不会在功能上做过多的扩展，而把最大的发挥空间留给第三方开发者。其更多的是最大地发挥硬件和系统效能，基于硬件创新来强化体验，比如force touch、相机、人脸、指纹等。最近几年AI作为一个增长点逐渐在移动平台上落地，未来也会是一个创新推动力量。</li>
</ul>
<h4 id="看法二：车载智能系统无法替代手机，它们是互补的关系。"><a href="#看法二：车载智能系统无法替代手机，它们是互补的关系。" class="headerlink" title="看法二：车载智能系统无法替代手机，它们是互补的关系。"></a>看法二：车载智能系统无法替代手机，它们是互补的关系。</h4><ul>
<li>有种思路是将手机平板等移动设备的体验平移到车内，以在车内场景完全替代手机。这个观点最大的问题是忽视了移动设备和车载设备在应用场景和交互方式上的差异性。</li>
<li>手机等移动设备抢占的是用户的碎片时间，无论在家里、公司、路上各种场景都有可能会使用，而在使用习惯上，手机平板有着强交互的特点，即交互频繁，多数是操作后的被动反馈，特别针对在手机上看视频看电子书等弱交互场景来讲并不是最好的体验（个人认为看电子书最好的体验是kindle，看视频最好的体验是大屏电视）。</li>
<li>而在车内场景，这里主要考虑中控设备，重点是要考虑驾驶场景下的需求，即在驾驶安全的前提下便捷的完成操作或者少操作。细化之后有两点推论：一是要尽量支持盲操作，对应的就是语音交互。毋庸置疑地，语音交互是现阶段驾车场景下的第一交互方式，同时辅助提供方向盘或手势等操作方式；二是要加强主动反馈，即系统能根据用户当前所处的状态智能识别用户的实时需求并提供主动服务。这两点的技术落脚点都是AI。相反地，在手机等移动设备上，因为强交互的特点，语音功能多是鸡肋功能，只能去其他诸如智能音箱，智能电视，智能车机等遥控操作不便或场景需要的系统中发挥其长处。</li>
<li>车机无法取代手机的另外一点是，车机在很长一段时间内都无法提供手机生态中如此多的服务，个人认为未来也没有这个必要。这点归根结底仍然是应用场景问题。手机与车机相比较的弱势主要在于屏幕偏小，硬件功能不像汽车这么强大。从这两方面出发，车机应该发挥其强势的地方，比如提供副驾后排的大屏投射方案，提供硬件数据的部分开放，手机作为私人设备来进行操控或远程展示等。</li>
</ul>
<p>综上，在汽车场景下，车机不可能替代手机，而且也没有必要，应该考虑的是如何设计两者联动的功能，以达到车机和手机间的无缝体验。</p>
<p><strong>让手机的归手机，车机的归车机。</strong></p>
<h4 id="看法三：车载智能终端当前所处的还是完善体验的阶段，谈不上应用服务和生态。"><a href="#看法三：车载智能终端当前所处的还是完善体验的阶段，谈不上应用服务和生态。" class="headerlink" title="看法三：车载智能终端当前所处的还是完善体验的阶段，谈不上应用服务和生态。"></a>看法三：车载智能终端当前所处的还是完善体验的阶段，谈不上应用服务和生态。</h4><ul>
<li>当前车联网终端产品的发展状态还处于落地阶段，基本类似于手机产品从功能机向智能机发展的过程。</li>
<li>手机在Android和iOS这些革新性系统，以及app store这种创新商业模式出现后，基础框架已经完成，核心功能基本确定。从2007年一直到移动互联网爆发式增长的初期，在这个阶段中类miui的定制Rom产品的出现解决的核心问题主要就是用户体验，包括视觉、交互，附带部分功能性体验。这些产品起于Android早期体验的粗糙，终于后期体验的成熟。这个过程可以类比智能车机产品当前的发展状态。因为传统车厂对于车机体验的重视刚刚起步，存量车的车机只能说有功能无体验。因为智能手机的普及，用户对于体验的要求越来越敏感，这里有很大的提升空间和需要。</li>
<li>从功能需求的角度分析，类比打电话，发短信等手机上的基础功能或者说强需求，对应到车机就是导航、音乐、电话（微信）、车控几大明确的强需求，除去数据上报等与整车系统关联性比较强的功能外，其他很多功能特别是面向用户需求的必要性并不明晰。考虑之前提到的手机与车机场景中的差异，很多手机交互场景中的服务，并没有太大必要移植到车机上，比如洗车、保养、代驾等O2O服务。只有那些对驾驶场景或汽车硬件相关性比较强的功能，才适合放到车机上，比如停车场停车自动缴费、LBS出行相关的服务等。从这点出发，就会发现适合于车机上的服务相比手机少很多。想明白这些，才能明确功能范围的边界在哪里，做好有的放矢。</li>
</ul>
<h5 id="最终输出结论，产品策略应侧重这几点：1）整合资源；2）提升交互体验；3）围绕硬件和AI进行创新；"><a href="#最终输出结论，产品策略应侧重这几点：1）整合资源；2）提升交互体验；3）围绕硬件和AI进行创新；" class="headerlink" title="最终输出结论，产品策略应侧重这几点：1）整合资源；2）提升交互体验；3）围绕硬件和AI进行创新；"></a><strong>最终输出结论，产品策略应侧重这几点：1）整合资源；2）提升交互体验；3）围绕硬件和AI进行创新；</strong></h5>]]></content>
      <tags>
        <tag>车联网</tag>
      </tags>
  </entry>
</search>
